# 2026-07-04

## Collaudo app-cat in caricamento perenne — RISOLTO
Dopo aver reinstallato il workload wasm-tools (era assente, NETSDK1147), il publish del CAT usciva in ICU **sharded** (non deterministico: pack 8.0.28 regressione). Con gli shard l'app va in "caricamento perenne" perché al boot imposta la cultura → Blazor "culture change not supported with sharded globalization".
**Fix deterministico:** `deploy-cat-app.sh` = publish default (sharded, affidabile) + patch `boot.json` → full-ICU (`globalizationMode=all`, `icudt.dat` dal runtime pack + hash sha256, rimossi shard e .gz/.br stale). Verificato Playwright: boot 0 errori, pl/uk/ro/nl/ru ok. **Prod pipeline non impattata** (builda LoadAll sul runner).

## i18n: avvio in lingua browser/sistema, fallback inglese (richiesta 5129)
- **App** (`Program.cs`): se nessuna scelta salvata (localStorage `puntify_culture`), rileva `navigator.languages` → primo supportato (it/en/es/fr/de/nl/pl/ro/ru/uk), altrimenti **en**.
- **Vetrina** (`Program.cs`): `DefaultRequestCulture` it→en; il redirect root usa la lingua browser SOLO se **tradotta** (it/en/es/fr/zh/ar/hi/bn/pt), altrimenti **en**. Rimosso ramo `isVenuePath` ridondante.
- Verificato: App en→EN, pl→PL, it→IT, ja→EN. Vetrina redirect ja/ru/pl→/en, zh/es/it→loro.
- Commit `558ecc2`.

## Stato deploy
- Commit locali: `6687034` (logout), `558ecc2` (i18n). **Deploy prod IN ATTESA di OK Stefano.**
- Collaudo (app-cat + vetrina) già aggiornato e testato.

## Prossimi passi
- Attendere OK Stefano → deploy prod (App + Vetrina).
- Riparare propriamente il toolchain wasm-tools (versione allineata a SDK 8.0.128) per evitare la patch boot.json.
