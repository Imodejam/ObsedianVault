# Daily 2026-06-12

## Blog Puntify Vetrina · 10 articoli — FATTO (verificato), ma NON tracciato a suo tempo
Stefano (msg 3516/3518) ha chiesto se ho finito gli articoli del blog richiesti IERI (2026-06-11). In questa sessione non avevo traccia del task → contestazione giusta: **non avevo segnato nulla nel vault**.

### Verifica stato reale
- Tabella `puntify.blog_posts` (Supabase CAT, docker `ops-postgres`/`puntify_cat`) contiene **10 articoli, tutti `published=true`**, creati 2026-06-12 00:13.
- Contenuti reali e completi (8.751–11.539 caratteri ciascuno), con `excerpt`, `tags`, `author="Nemi AI"`, slug SEO.
- Pagine vetrina: `Puntify.Vetrina/Pages/Blog.razor` (/blog, /{lang}/blog) + `BlogPost.razor`; leggono via `SupabaseStatsService.GetPublishedBlogPosts()`. Modello `Punto.Shared/Models/BlogPost.cs` ([Table blog_posts]).
- LIVE verificato: `curl localhost:8003/it/blog` HTTP 200, mostra gli articoli (es. "paradosso del decimo", "no-show-saloni-come-azzerarli"). Vetrina gira sotto `puntify-vetrina.service` (Blazor Server, :8003).

### I 10 articoli (slug · target)
1. no-show-saloni-come-azzerarli · Saloni/Beauty (prenotazioni, no-show)
2. tessera-cartacea-ti-fa-perdere-clienti · Bar/Caffetterie (fidelizzazione)
3. prenotazioni-telefoniche-ristorante · Ristorazione
4. aumentare-scontrino-medio-senza-sconti · Retail (fidelizzazione)
5. ordini-ombrellone-lidi-chioschi · Stabilimenti balneari (menu/ordini)
6. da-cliente-occasionale-a-cliente-fisso · Marketing/fidelizzazione
7. digitalizzare-negozio-in-un-minuto · Per iniziare
8. agenda-online-studi-professionali · Studi/Professionisti (prenotazioni)
9. gestire-piu-punti-vendita · Multi-negozio
10. volantini-vs-fidelizzazione-digitale · Marketing/ROI

### Errore di processo (registrato in mistakes.md)
Il lavoro era stato fatto ma NON tracciato nel vault (nessuna riga in working-context/daily/log al momento dell'esecuzione). Violazione regola "tutto deve essere tracciato nel vault". Correzione applicata: tracciamento retroattivo + memoria rinforzata.

### Prossimi passi (da confermare con Stefano)
- Articoli sono in italiano: valutare traduzione nelle altre lingue della vetrina (vedi [[feedback_translate_all_languages]]).
- Eventuale revisione editoriale / immagini cover (oggi `cover_image` probabilmente vuoto → placeholder SVG).

## Blog Puntify · cover images — FATTO+LIVE
Stefano (msg 3523-3532) ha inviato 10 PNG (art1..art10). Mappate per CONTENUTO visivo agli slug (confermato leggendo ogni immagine, non fidandomi del numero): art1=no-show saloni, art2=tessera cartacea, art3=prenotazioni telefoniche, art4=scontrino medio (AVS +22%), art5=ombrelloni lidi (scan&order), art6=occasionale→fisso (Sunday customer), art7=digitalizzare 60s, art8=agenda studi 24/7, art9=multi-PV (3 negozi dashboard), art10=volantini vs digitale.
- Upload: MinIO bucket `shopimages-cat`, key `blog/<slug>.png`, ContentType image/png, via boto3 (endpoint files.puntify.it, creds Storage di Puntify.Server/appsettings.Development.json). Bucket public-read (verificato).
- DB: UPDATE `puntify.blog_posts.cover_image` = https://files.puntify.it/shopimages-cat/blog/<slug>.png per tutti e 10.
- Verifica: 10/10 URL HTTP 200; render confermato su /it/blog (puntify-vetrina :8003). Originali in /tmp/blogcovers/.

## msg 3533 — Card premi immagine a tutta ampiezza (FATTO+DEPLOY)
NegozioDetail.razor sez. Rewards: contenitore img da `h-36 object-cover` (banda croppata) → `w-full h-auto object-contain` (immagine intera, full-width card). Tailwind CDN JIT, nessun rebuild CSS. Build 0 err, vetrina riavviata, HTTP 200, 4/4 card aggiornate.

## msg 3541 — Menu burger mobile: chiusura al cambio pagina (FATTO+DEPLOY)
Pagine vetrina = SSR statico, solo Header.razor è isola InteractiveServer → OnLocationChanged inaffidabile su enhanced navigation (per questo lo scrollToTop era già fatto via JS blazor:navigated, non via LocationChanged). Fix: nuovo [JSInvokable] CloseMenusOnNavigation in Header (azzera IsMobileMenuOpen + lang dropdown/picker + dropdown aperti) richiamato dal listener JS blazor:navigated in App.razor (stesso punto dello scrollToTop, usa headerDotNetRef già creato in initHeaderScroll). Build 0 err, vetrina riavviata, HTTP 200, hook presente nel render. Blazor Server → no hard-refresh.

## msg 3543 — Mega-menu Funzionalità: freccia "tuttuno" col pannello + pannello sopra banner app (FATTO+DEPLOY)
1) Freccia staccata dal pannello (gap visibile). Causa: arrow era position:absolute ancorata al trigger (top:100%+margin), il pannello è position:fixed top:4.6rem → riferimenti diversi, gap variabile con altezza header/scroll. Fix (app.css .mega-arrow): position:fixed top:calc(4.6rem - 8px), 16px → il rombo straddla il bordo superiore del pannello. Stessa logica fixed del pannello → stesso containing block (viewport, o header quando .glass ha backdrop-filter; header è a top:0 quindi 4.6rem≈viewport in entrambi) → saldata in ogni stato di scroll. X = static position (sotto il trigger).
2) Pannello deve stare sopra il banner "Installa Puntify" (PwaInstallBanner fixed bottom-0 z-50). Causa: header è fixed z-50 → crea stacking context, il pannello (z-60) è capped a z-50 dell'header = pari al banner, e il banner è DOPO l'header nel DOM (MainLayout) → vinceva. Fix: HeaderClass z-50 → z-[60] (Header.razor) → tutto l'header (pannello incluso) sopra il banner. Bonus: anche drawer mobile ora sopra il banner.
CSS bump: app.css?v=20260603b → 20260612a (App.razor). Build 0 err, vetrina riavviata, /it HTTP 200, css v + z-[60] nel render. Verifica visiva freccia: a Stefano (hover desktop, non curl-abile).

## msg 3544 — Blog: area bianca in alto (horror vacui) (FATTO+DEPLOY)
Hero blog era solo h1+sottotitolo su gradient-hero quasi bianco con pt-32/lg:pt-40 → blocco bianco vuoto (più spoglio degli altri hero, che hanno badge+mockup). Fix (Blog.razor): aggiunto badge "Risorse e guide per esercenti" (icona libro), 2 blob morbidi brand-red/10 e /5 (blur-3xl, overflow-hidden) per riempire on-brand, padding ridotto pt-28 pb-12 / lg:pt-36 pb-16, fix accenti novità/attività. HTTP 200, badge+blur-3xl nel render.

## msg 3546/3547/3550 — Footer Vetrina: riorganizzazione gruppi + layout accostato (FATTO+DEPLOY)
Decisioni Stefano: Prodotto=solo servizi (Fidelizzazione[/fidelizzazione], Prenotazioni, Menu digitale[/menu], Nemi AI[/nemi], Social); Soluzioni(nuova)=Per clienti+Prezzi+Guadagna; Risorse=Blog+FAQ+Mappa del sito(/sitemap.xml); Legale invariato. "Per esercenti" rinominato Fidelizzazione → è un prodotto.
Footer.razor: nuove liste ProductLinks/SolutionLinks/ResourceLinks; rimossi link rotti (#) Guide/HelpCenter/Contatti. Layout (msg 3547 "accostate stile tablet"): da grid 4-col full-width → flex brand-sinistra + sub-grid 4 colonne raggruppate a destra (grid-cols-2 sm:grid-cols-4, gap 8/10/14) → colonne accostate quando c'è spazio.
i18n: 4 nuove chiavi (Footer_ColSolutions, Footer_Link_Menu, Footer_Link_Blog, Footer_Link_Sitemap) tradotte in 9 file resx (default IT + en/es/fr/pt/ar/hi/zh/bn; it.resx saltato → fallback su default). Reuse: Mega_Feat_Loyalty_Title=Fidelizzazione, Nav_Bookings, Nav_Social, Footer_Link_Nemi/Clients/Prices/Earn/FAQ.
Build 0 err, vetrina riavviata. Verificato render IT (tutte le voci) + EN (Solutions/Digital Menu/Sitemap → i18n ok).

## msg 3551 — Popup cookie Vetrina (branded Puntify) + Google Consent Mode v2 (FATTO+DEPLOY)
Mancava il consenso cookie (GA gtag caricava SENZA consenso = gap GDPR). Costruito da zero, brandizzato Puntify (no Cookiebot blu).
- Shared/CookieConsent.razor (@rendermode InteractiveServer, in MainLayout dopo PwaInstallBanner): card bianca fixed bottom centrata, rounded-2xl shadow, icona cookie brand-red, titolo+desc+link Cookie Policy. Bottoni: Rifiuta / Personalizza / Accetta tutti; in Personalizza compaiono 4 categorie con toggle iOS (Necessari bloccato ON, Preferenze, Statistiche, Marketing) + Accetta selezionati. z-[100] (sopra header z-[60] e pwa-banner z-50). Non modale (pointer-events-none outer).
- wwwroot/js/cookie-consent.js: window.cookieConsent {shouldShow, save, reopen} → localStorage `puntify_cookie_consent` {categories,ts,v}; applica Consent Mode v2.
- App.razor: gtag('consent','default', tutto denied tranne security_storage, wait_for_update:500) PRIMA del config; applica consenso salvato (visitatori di ritorno) prima del config. Mappatura: Statistiche→analytics_storage; Marketing→ad_storage/ad_user_data/ad_personalization; Preferenze→functionality_storage. Caricato js/cookie-consent.js.
- Footer.razor bottom bar: aggiunto "Preferenze cookie" (onclick=cookieConsent.reopen() → clear+reload) per riaprire il banner.
- i18n: 13 chiavi Cookie_* tradotte in 9 resx (default IT + en/es/fr/pt/ar/hi/zh/bn; it.resx fallback).
Build 0 err, vetrina riavviata. Verificato: cookie-consent.js HTTP 200, consent default+analytics_storage nel <head>, footer "Preferenze cookie"(IT)/"Cookie preferences"(EN). Banner = render client-side post-JS (non curl-abile) → verifica visiva a Stefano.

## msg 3554 (parte 2) — Login/Registrati unificati su Puntify.App (FATTO+DEPLOY)
Problema: /login e /register = due pagine quasi gemelle (441 righe ciascuna) → switch = cambio pagina ("troppi salti"). 
Soluzione: unificate in UNA pagina (Pages/Shared/Login.razor con @page "/login" + @page "/register"). Register.razor ELIMINATO (nessun riferimento al componente, solo route — coperte dalla pagina unica). 
- Switch Accedi/Registrati = segmented toggle con indicatore scorrevole (.auth-toggle in app.css), nessuna navigazione: cambia solo _signUp.
- Campi email+password mostrati subito (tolto lo step "Continua con email" → meno click). In Registrati compare anche il campo Referral. Google unico per entrambi. <form @onsubmit> → invio con Enter.
- Logica auth preservata VERBATIM: HandleEmailAuth (signin+cookie+RedirectFromCache con returnUrl), HandleSendMagicLink, HandleSignUp (signup+auto-login+create account+referral+fallback "email già in uso"→login), SignInWithGoogle, RedirectBasedOnRole (crea account+referral per nuovi OAuth), RedirectFromCache. _loading=schermata iniziale, _busy=operazione. Rimosso Apple (era dead code, non in UI).
- Modalità iniziale da rotta: /register→Registrati, /login→Accedi. NavigateToRoute("register") (11 usi nell'app) → apre la pagina unica in modalità Registrati.
Build 0 err (warning preesistenti), puntify-app riavviato, build-info auto-bump 20260612100922 (CSS cache-bust), /login + /register HTTP 200. WASM → Stefano deve Ctrl+F5 + test reale (non curl-abile la UI). 
APERTO: msg 3554 parte 1 (tasto Installa PWA) → chiesto a Stefano vincolo cross-origin + topologia domini prod (A: install dall'app / B: PWA vetrina con redirect). In attesa.
