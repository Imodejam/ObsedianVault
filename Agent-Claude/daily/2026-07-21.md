# Daily 2026-07-21

## [2026-07-21] Riapertura ordine servito: motivazione obbligatoria (scelta A di Stefano, 6962)
- Migration 2026-07-21_menu_order_reopen_reason.sql: colonna reopen_reason su menu_public_orders (+NOTIFY pgrst).
- Server: SetStatus/SetStationStatus con campo Reason; regressione da delivered NON pagato senza motivo -> 400 reason_required;
  con motivo -> salvata su reopen_reason + log "RIAPERTO". Regressioni da ready/preparing restano libere; pagato = solo avanti.
- Client: MenuOrdersApiService.SetStatusAsync/SetStationStatusAsync con parametro reason opzionale (UI attuale non ha
  percorsi di regressione; pronto per quando serviranno, pattern JS prompt gia' in uso in Cassa).
- E2E 5/5: senza motivo 400, con motivo 200+DB, stazione idem, regressione non-servito libera. Cleanup 0 residui.

## [2026-07-21] "Invia ricevuta" muto (6965)
- Caso Stefano: conto pagato SENZA email cliente -> ReceiptOrderId null -> bottone disabled ma visivamente
  identico -> click senza alcun feedback. (L'audit aveva fixato il caso "primo giro con email", non il caso zero-email.)
- Fix: bottone sempre attivo col conto; se manca l'email la CHIEDE al volo (JS prompt, pattern note cliente),
  la salva sull'ordine (PATCH /customer, permesso anche su pagato) e invia; toast esito sempre (z-index 1100 > modal 74).
  2 resx nuove (cassa_receipt_email_prompt/invalid) x10. Verificato server e2e: no-email sent:false -> PATCH email 200 -> sent:true. Cleanup 0.

## [2026-07-21] Entita' CONTO in archivio (6967) — server FATTO, UI delegata
- Migration 2026-07-21_menu_order_bill_id.sql: colonna bill_id + indice parziale.
- Modello: MenuPublicOrder.BillId (Punto.Shared).
- Server: /pay assegna bill_id (conto singolo); pay-table condivide UN bill_id su tutti i giri.
  Nuovo POST /api/menu/orders/bill/{billId}/receipt-email {email?}: ricevuta AGGREGATA del conto
  (righe per giro con intestazione codice, coperto, scorporo IVA, totale, metodi pagamento, data incasso);
  email fornita al momento o presa dal conto (no_email altrimenti).
- E2E: pay-table -> bill_id condiviso OK; ricevuta con email al volo sent:true; senza email no_email. Cleanup 0.
- UI archivio delegata ad agent: raggruppa per bill (storici senza bill = conto singolo), card conto + dettaglio,
  azioni Invia ricevuta (prompt email precompilata) e Stampa (print CSS), resx x10.

## [2026-07-21] Link Telegram al conto (6969/6970) + spec documento commerciale (6972-6974)
- Notifica "Nuovo ordine": ctaUrl ora deep-link alla Cassa del CONTO (/dashboard/Cassa/{tavolo-id} per dine_in,
  /{ordine-id} per asporto) invece della dashboard generica. NotifyMenuOrderAsync param cassaRef. Server buildato+riavviato.
- Stefano ha consegnato la spec COMPLETA del documento commerciale (scontrino): ReceiptDocument neutro, template JSON
  dichiarativo w32/w48, renderer EscPos+PlainText/HtmlPreview, IDocumentIdentityProvider ibrido (Device/Puntify+Pending),
  loyalty+taxBreakdown attivi, idempotenza, isReprint, template per PV versionato JSONB, test 32/48, 4 esempi output.
  IMPORTANTE (6974): la ricevuta email/stampa dell'archivio conti DOVRA' diventare questo documento (fase 2 integrazione).
  Si aggancia al bridge Android printRaw ESC/POS gia' pronto. Delegato ad agent dedicato (Punto.Shared/Receipts/).
- In parallelo: agent archivio conti UI ancora in corso.

## Archivio Conti chiusi (Puntify.App)
- Dashboard.razor: archivio menu = ordini PAGATI ultimi 7gg (no cutoff 30min), raggruppati client-side per bill_id (record ClosedBill); card per conto + modal dettaglio (ordini/items, coperti, pagamenti, badge Fattura) con azioni Invia ricevuta (prompt email precompilata; fallback per-ordine per storici senza bill_id) e Stampa (window.print + @media print scoped .bill-print-area in booking.css via body:has).
- MenuOrdersApiService: nuovo SendBillReceiptAsync(billId, email?) su POST /api/menu/orders/bill/{id}/receipt-email.
- resx x10: archive_bills_section/_bill_title/_bill_orders/_bill_paid_at/_bill_send_receipt/_bill_print/_bill_payments.
- Build 0 errori; puntify-app riavviata, :8002 → 200. No commit.

## [2026-07-21] Archivio CONTI chiusi — UI completata (agent)
- Tab Archivio = card per CONTO (raggruppa per bill_id, storici senza bill = conto singolo): incassato dd/MM HH:mm,
  codici giri, Tav./Diretto/Asporto, riepilogo pagamenti per metodo, totale. Ultimi 7 giorni (via cutoff 30min rimosso, filtro Paid).
- Modal dettaglio conto: giri con righe, coperti, pagamenti, badge Fattura, totale; azioni "Invia ricevuta"
  (prompt email precompilata -> bill endpoint; fallback per-ordine sui storici) e "Stampa" (window.print + @media print
  scoped con :has(.bill-print-area), stampa Cassa esistente invariata).
- SendBillReceiptAsync in ApiService; resx x10 (archive_bill*). Build 0 err, app 200.

## [2026-07-21] Spec fiscale completa (6977-6979) — piano 2 fasi
- Stefano ha inviato la sezione completa layer dispositivo fiscale: IFiscalDevice multi-vendor
  (Probe/Status/Emit(idempotencyKey)/QueryByIdempotencyKey/Void/DailyClosure/Journal), FiscalResult+FiscalReference,
  SimulatedFiscalDevice con fault injection, EpsonFpDevice XML/HTTP mapping reale, scheletri Custom/RCH/Olivetti,
  FiscalDeviceRegistry per PV da config, *DocumentMapper separato da ITransport, errori normalizzati enum comune,
  idempotency persistita PRIMA della chiamata, state machine Draft->PendingFiscalization->Fiscalized|FailedRecoverable|FailedManual
  + coda riconciliazione, wizard registrazione con matricola storicizzata (tabella validita' temporale),
  fallback non-fiscale via etichette. Test integrazione 3 scenari critici.
- Piano: fase 1 core (agent in corso, stub IFiscalDevice allineato al contratto EmitAsync);
  fase 2 layer fiscale con secondo agent sopra il core. Aggiornato l'agent core via SendMessage (2 delta).

## Receipt engine (documento commerciale) — subagent
- Creato Punto.Shared/Receipts: modello dati, template dichiarativo JSON (widthProfiles w32/w48, schema+default), layout engine, EscPosRenderer (CP858, QR GS(k, taglio), PlainText=preview, HtmlPreview, numerazione ibrida (DeviceIdentityProvider su FiscalResult stub IFiscalDevice, PuntifyIdentityProvider su RPC).
- Migration docs/DB Migrations/20260721_receipt_templates.sql applicata a puntify_cat: receipt_templates (JSONB versionato, 1 attivo/shop) + receipt_counters + RPC receipt_next_number (gapless per shop, testata 1,2 e indipendenza tra shop).
- Test standalone Puntify.Receipts.Tests: 37/37 verdi; 4 sample in Punto.Shared/Receipts/samples. Nessun servizio riavviato, niente commit, App/Server non toccati.
- Prossimi passi: integrazione Cassa/preview Blazor (fase successiva), layer IFiscalDevice completo (secondo agent).

## [2026-07-21] Receipt engine CORE consegnato + fase 2 avviata
- Core verificato indipendentemente: 37/37 test, tabelle receipt_templates/counters + RPC receipt_next_number su CAT.
- Struttura: Punto.Shared/Receipts/{Models,Templates,Layout,Rendering,Identity,Storage,samples}; layout engine unico
  (preview = stampa byte-per-carattere), importi mai troncati, CP858 encoder autonomo (EUR=0xD5), numerazione gapless
  con row-lock (no sequence), template default embedded + schema JSON. Test project standalone Puntify.Receipts.Tests.
- 4 sample inviati a Stefano (rich/minimal x 48/32).
- FASE 2 lanciata (agent): IFiscalDevice completo (Probe/Status/Emit/Query/Void/Closure/Journal), Simulated con
  fault injection, EpsonFpDevice XML/HTTP (fpmate.cgi) con mapper separato da ITransport, scheletri Custom/RCH/Olivetti,
  FiscalDeviceRegistry da config, FiscalTransactionStore + state machine + FiscalReconciliationService,
  matricole storicizzate (fiscal_device_registrations validita' temporale), test 3 scenari critici.

## Puntify receipts — FASE 2 (fiscal device layer)
- Contratto IFiscalDevice completo (Probe/Status/Emit/QueryByKey/Void/Closure/Journal), compat DeviceIdentityProvider
- Punto.Shared/Receipts/Fiscal/: SimulatedFiscalDevice (fault injection), EpsonFpDevice+Mapper XML fpmate.cgi, scheletri Custom/RCH/Olivetti, FiscalDeviceRegistry, ITransport (Http impl, Tcp/Serial stub)
- Idempotenza: FiscalTransaction store (chiave persistita PRIMA), state machine Draft->Pending->Fiscalized/FailedRecoverable/FailedManual, FiscalReconciliationService (query-before-retry, mai doppia emissione)
- Migration 20260721_fiscal_devices.sql applicata su CAT (fiscal_device_configs, fiscal_transactions unique key, fiscal_device_registrations storicizzate + RPC fiscal_register_device); prove psql OK
- Test: 84/84 (37 core + 47 nuovi: 3 scenari critici riconciliazione, registry, snapshot XML Epson, state machine, HttpTransport)
- Prossimi passi: implementazione store PostgREST lato server, scheduler riconciliazione, wizard UI registrazione dispositivo, validazione su hardware Epson

## [2026-07-21] Layer fiscale (fase 2) consegnato e verificato — 84/84
- Verifica indipendente: dotnet test 84/84; tabelle fiscal_device_configs/transactions/registrations + RPC
  fiscal_register_device su CAT; build Shared 0 err.
- Design notevole: errori a 2 livelli (Fail=non emesso certo; eccezione=incerto->Pending+Query);
  FiscalResult.NotFound distingue "riemetti sicuro"; classificazione centralizzata in FiscalEmissionService;
  idempotency key stampata nel trailer Epson PNTF:{key} per riscontro manuale.
- FASE 3 avviata: ricevuta email/stampa archivio conti = documento commerciale (mapping bill->ReceiptDocument,
  email HTML via HtmlPreviewRenderer, stampa via EscPos/bridge, preview identica).

## Fase 3 receipt engine → flusso live (Puntify CAT)
- Nuovo Punto.Shared/Receipts/Mapping/BillReceiptMapper.cs: conto pagato (1+ MenuPublicOrder) → ReceiptDocument (righe, coperto, scorporo IVA, pagamenti; numerazione Pending, no numero consumato).
- MenuController: entrambi gli endpoint receipt-email ora rendono il documento commerciale via HtmlPreviewRenderer (48 col) dentro EmailTemplates.Wrap; guardie e response invariate.
- Dashboard (archivio conti): modal dettaglio conto mostra/stampa la preview scontrino PlainTextRenderer 32 col (.receipt-slip), items list sostituita.
- E2E su pepto: ordine #A05 Tavolo 1 → covers → pay → receipt-email bill+ordine sent:true (Resend log OK); test ripulito (ordine+transazione fedeltà cancellati, count invariato 12).

## [2026-07-21] Fase 3 consegnata — sistema documento commerciale COMPLETO (3/3)
- BillReceiptMapper (Punto.Shared/Receipts/Mapping): bill->ReceiptDocument (righe, coperto come riga unica,
  scorporo IVA identico, payments, Pending numbering "DOC. IN ASSEGNAZIONE" — gapless riservata all'emissione fiscale).
- Server: entrambi gli endpoint receipt-email usano HtmlPreviewRenderer w48 (~140 righe HTML hand-made rimosse).
- App: dettaglio conto = preview scontrino w32 (receipt-slip), la stampa stampa quello. Mapper condiviso: email=preview=stampa.
- E2E reale: ordine #A05 (9,00 con coperto) pagato misto cash+card -> ricevuta bill sent:true (Resend). Cleanup 0 residui.
- Verifica indipendente: 84/84, servizi su, componenti in place. Aperto residuo di Stefano: #A02 (suo test, non toccato).
- Prossimi possibili (proposti a Stefano): emissione fiscale dalla Cassa (Simulated/Epson), wizard registrazione, template editor PV.

## [2026-07-21] Conto unico in archivio per storici (6990)
- #A10+#A20 pagati insieme 09:50 (PRIMA della migration bill_id 10:05) -> bill NULL -> 2 card divise con importi duplicati.
- Fix 1: backfill 2026-07-21_bill_id_backfill.sql su CAT — pagati senza bill raggruppati per (shop,tavolo,paid_at)
  stesso uuid (3 righe), singoli uuid proprio (5). Verificato: A10+A20 stesso bill c71ee0ae, 0 pagati senza bill.
- Fix 2 (robustezza prod): BillGroupKey client — BillId ?? legacy(tavolo+paid_at) ?? Id. Build+restart app (200).

## [2026-07-21] Fix formattazione preview scontrino (6993)
- Il <pre class=receipt-slip> ereditava text-align:center dal modal -> ogni riga corta (descrizioni item,
  date) veniva ricentrata dal browser sfasando il layout gia' impaginato dal renderer (che riempie a sinistra).
- Fix: text-align:left esplicito su .receipt-slip. Build bump + restart app (200), CSS servito verificato.

## [2026-07-21] Pagine Ordine + Conto — FATTE DA ME (agent bloccati da session limit fino 12:00 UTC)
- I 2 agent lanciati per detail endpoint/pagine sono morti subito (session limit); nessun lavoro parziale (verificato git).
- Fix 6997 (fatto prima): planimetria dashboard viewBox = stanza intera (tavoli piccoli, zoom out) + ChairPositions
  della dashboard sostituito col per-lato di TablesPlanimetry (quadrato da 3 = una sedia per lato, dritte).
- Endpoint (io): GET /api/menu/orders/{id}/detail {order,events} + GET /bill/{billId}/detail {orders,events,loyalty
  (credited=all LoyaltyCredited, points=floor(tot x Ratio), email)}. Smoke su bill reale c71ee0ae: 2 ordini, loyalty 19pt OK.
- App (io): OrderDetailPage /merchant/{shop}/order/{id} (hero, prodotti, TIMELINE con delta minuti, pagamento, link conto)
  + BillDetailPage /merchant/{shop}/bill/{id} (ordini cliccabili, pagamenti, punti, scontrino receipt-slip 32col +
  stampa/reinvio email con prompt). API client GetOrderDetailAsync/GetBillDetailAsync. CSS odp-* + badge globali in
  booking.css. resx 26 chiavi x10 lingue. Link: card archivio -> pagina conto (fallback modal per storici);
  modal dettaglio bacheca -> link pagina ordine. Build 0 err, app 200.
- NOTA: eventi presenti solo per ordini nuovi (trigger attivo da oggi ~11:50); storici mostrano timeline vuota.

## [2026-07-21] Conto chiuso: ordini spariti dal menu cliente (7000)
- MerchantMenuPreview: PruneClosedOrdersAsync al load — per ogni ordine salvato interroga /status e rimuove
  i pagati/annullati da "I tuoi ordini" + localStorage (fail-open su errori rete). In piu' rimozione LIVE nel
  poll quando delivered&&paid (senza aspettare il reload). Build 0 err, vetrina 302.

## [2026-07-21] Titolo conto + pill verde (7002) e falso "modificato da altro dispositivo" (7003)
- Archivio + pagina Conto: titolo "Conto — Tav. Y × coperti — dd/MM" (BillTitle/HeroTitle) e pill VERDE stile
  Stripe "Incassato il dd/MM HH:mm" (bill-paid-pill, resx archive_bill_paid_on x10). Build+restart app.
- BUG 7003 root cause: IsOrderClosed includeva delivered -> PATCH items su ordine SERVITO non pagato dava 409
  order_closed che il client mostra come "modificato da un altro dispositivo" (era il punto F1 dell'audit,
  rimasto fuori dal brief server per mia svista). Fix: IsOrderClosed = Paid || cancelled.
- Fix preventivo: RegisterOrderPush non bumpa piu' updated_at (la registrazione push del cliente faceva
  scattare falsi 409 in Cassa). Build+restart server.

## [2026-07-21] Restyle card conto archivio (7005)
- Card: titolo "Conto — Tav. Y × N — dd/MM" a sx + pill verde "Incassato il..." a dx (riga 1);
  ordini come HASHTAG chip in fondo a sx (bill-code-tag) + TOTALE GRANDE a dx (bill-total-big, 22px);
  rimossa suddivisione carta/contanti e data created ridondante (helper BillPaymentsSummary eliminato).
- Gotcha Razor: variabile foreach chiamata "code" confligge con direttiva @code -> RZ1017 (rinominata oc).
- Build 0 err, app 200.

## [2026-07-21] Filtri archivio stile Stripe (7007)
- Barra filtri sopra i conti chiusi: select Tavolo (dai tavoli reali), select Data (Tutti/Oggi/Ieri/Data specifica
  con input date), bottone Azzera quando attivi; si SOMMANO alla ricerca testuale esistente (estesa, non sostituita).
- BillMatchesFilters su tavolo (ordini del conto) e data incasso. resx x7 chiavi x10 lingue, CSS arch-filters.
- Build 0 err, app 200.
