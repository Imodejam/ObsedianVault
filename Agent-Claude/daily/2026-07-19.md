# 2026-07-19

## Puntify — Hardening H1-H4 (no commit)

### Risultati
- H1 (CONFERMATO): log token exchange OAuth non espone piu' il body grezzo. GoogleBusinessService.cs + GenericOAuthProvider.cs (2 siti) -> helper OAuthErrorSummary che estrae solo status + error/error_description; gestisce anche il caso Graph/Facebook dove "error" e' un OGGETTO (loggato solo type+code, mai message). Trovato e fixato un bug nel mio primo fix (e.ToString() dumpava l'oggetto error intero).
- H2 (CONFERMATO): AdminEventsController valida email (formato) + esistenza account/shop. unknown_recipient 400 sui flussi con account esistente; invalid_email 400 su formato; user-registered/merchant-registered = solo formato (account non ancora esiste). Test endpoint imodejam@gmail.com intatti. X-API-Key gating preservato.
- H3 (CONFERMATO): OAuth state ora con nonce 128-bit. Store IN-MEMORY (OAuthStateStore, ConcurrentDictionary+TTL 10min, pattern GodModeGrantStore) registrato SINGLETON. Consumo one-shot in callback. CAVEAT: per-processo (multi-istanza = spostare in tabella/Redis; deploy attuale single-instance).
- H4a (NON-issue ma guardato): aiScore gia' non-NaN (count checked); aggiunto ClampScore difensivo (NaN/Inf->0, clamp 0..100).
- H4b (CONFERMATO): CHECK constraint su social_reviews.sentiment + social_posts.sentiment (positivo/neutro/negativo/polemico/aggressivo/ironico/entusiasta o NULL). Migration 20260719_social_sentiment_check.sql applicata a CAT. Normalizzati 3 valori legacy 'positive'->'positivo'.
- H4c (CONFERMATO): SocialStudioController.UploadImage ora sniffa magic bytes (riuso MenuOcrSanitizer.SniffContentType), accetta solo jpeg/png/gif/webp, usa il content-type sniffato. StorageController usa presigned URL (client->storage diretto) quindi non applicabile li'.

### Test (2 round, live su :8001)
Round1: unknown email->400 unknown_recipient; email valida->200; auth-url facebook include nonce; state forgiato->rifiutato; upload fake .jpg->400, PNG reale->200.
Round2: email malformata->400 invalid_email; X-API-Key gating->401; sentiment allowed->ok, disallowed->rifiutato dal constraint; AI score zero-data->0/100; replay nonce->rifiutato.

### Note
- Email test 'pepto@gmail.com' (account CAT reale) ricevuta email "10 punti" reale durante il test valido; queue row eliminata (invio non annullabile).
- 1 PNG 1x1 di test resta nel bucket shopimages-cat (innocuo).
- NO commit/push (come richiesto).

### File
- Puntify.Server/Services/SocialStudio/GoogleBusinessService.cs
- Puntify.Server/Services/SocialStudio/GenericOAuthProvider.cs
- Puntify.Server/Services/SocialStudio/OAuthStateStore.cs (nuovo)
- Puntify.Server/Services/SocialStudio/SocialStudioService.cs
- Puntify.Server/Controllers/SocialStudioController.cs
- Puntify.Server/Controllers/AdminEventsController.cs
- Puntify.Server/Program.cs
- docs/DB Migrations/20260719_social_sentiment_check.sql (nuovo, applicato a CAT)

## R3 — Storno punti su rimborso Stripe (Puntify)
- File: Puntify.Server/Controllers/StripeController.cs — nuovo branch `charge.refunded` nel webhook + metodo `ReverseLoyaltyOnRefundAsync`.
- Verificato: stripe_payment_id salvato = payment_intent id (PaymentBreakdown.PaymentId = pi.Id in StripeService.GetPaymentBreakdownAsync). Enum reason = TransactionReason.Earn(1)/Redeem(2)/PointsExpired(3).
- Match: righe accredito reason=1 con stripe_payment_id=charge.PaymentIntentId → per ognuna inserisce debito reason=2 stessi punti, source="refund", stripe_payment_id=refund_id.
- Idempotenza: prima di inserire, GetAsync transactions stripe_payment_id=eq.{refundId}&reason=eq.2 → se esiste no-op.
- v1 solo rimborso totale (charge.Refunded && AmountRefunded>=Amount); parziale loggato/ignorato (follow-up).
- Firma webhook (ConstructEvent) invariata. API Stripe.net 51.2.0 verificate via reflection. NO build/restart/commit (orchestrator).
- Follow-up: rimborso parziale (storno proporzionale); saldo negativo se punti gia spesi.
