# 2026-07-19

## Puntify - Nemi proposte marketing autonome (previsione low-traffic -> flash da approvare)

### Fatto (server-side, no commit)
- LowTrafficAnalyzer (+ILowTrafficAnalyzer) - Puntify.Server/Services/Marketing/LowTrafficAnalyzer.cs
  - expectedTomorrow = bookings confirmed/pending domani (fuso negozio, start_at range UTC)
  - avgBaseline = somma tx stesso weekday nelle 8 settimane precedenti / 8 (giorni vuoti = 0)
  - IsLowTraffic = avgBaseline>=3.0 (floor) AND expectedTomorrow < threshold(0.6, config Marketing:LowTrafficThreshold)*avgBaseline
- MarketingProposalPayload - marker [MKT_PROPOSAL] (mirror MenuOcrImportPayload) su NemiTask.OriginalRequest
- NemiMarketingProposalService : BackgroundService - Puntify.Server/Services/BackgroundServices/
  - gate Marketing:NemiProposalsEnabled default FALSE; una passata/giorno fascia 8-11 Roma
  - per shop HasMarketing()+audience email consent>0: analizza, se low e nessuna proposta flash nemi oggi ->
    crea Campaign flash pending_approval created_by=nemi (15% / 2gg / 10 lingue) + nemi_task waiting_user + msg chat
- NemiChatService: branch HandleMarketingProposalConfirmAsync (dopo il branch OCR, prima del resume LLM):
  affermativo -> ICampaignService.ApproveAsync (pending_approval->scheduled, approved_at set); negativo -> CancelAsync
- Program.cs: registrati ILowTrafficAnalyzer + hosted service

### Anti-auto-send
Invariato e verificato: CampaignSendService.IsApproved rifiuta nemi con approved_at null (RefusedNotApproved=true).
La proposta e' solo pending_approval + domanda; nessun invio senza approvazione.

### Trace (CAT, con cleanup completo)
Seed 2 shop test (baseline 8 tx/lunedi x8 sett): LOW 1 booking domani -> IsLowTraffic=True; HEALTHY 8 -> False.
Harness sui servizi compilati reali: proposta pending_approval; send rifiutata (RefusedNotApproved=True);
ApproveAsync -> scheduled+approved_at -> send NON piu' rifiutata; CancelAsync -> cancelled. Tutti i dati test rimossi (0 righe).

### Build/restart
Build Server 0 errori; restart puntify-server ok; /api/menu 401; gate log "proposte: disattivate". No commit.

### Non toccati
frontend, MenuController, StripeController, BookingServiceImpl, CampaignSchedulerService (verificato via mtime).

## Fix bug audit-log admin (pre-esistente, fermo dal 14/07)
CAUSA REALE (non la teoria "details jsonb->stringa"): `AdminUser` aveva solo `[Column]`, niente `[JsonPropertyName]`. `SupabaseClient._readJsonOptions` e' STJ **case-sensitive senza naming policy** (solo 2 converter DateTime) -> `"id"` non mappa su `Id` -> `admin.Id = Guid.Empty` -> `LogAsync` esce alla guardia `admin.Id == Guid.Empty` (aggiunta per evitare flood FK): nessun errore loggato, nessuna riga scritta. Combacia col sintomo (200 ma 0 righe, 0 log errore).
BONUS SICUREZZA: anche `"profile"` non deserializzava -> restava default `AdminProfiles.Super` -> un admin support/finance sarebbe stato trattato come super (privilege escalation latente). Oggi solo Stefano (super), nessun exploit vivo.
FIX: `[JsonPropertyName]` su tutti i campi di `AdminUser` (id, email, display_name, profile, is_active, created_at, last_login_at) + commento monito.
VERIFICA empirica 2x: restart puntify-server pulito -> 2x GET /api/admin/me (JWT HS256 email=stefano.gitto@hotmail.com + X-API-Key) -> HTTP 200; `admin_activity_log` 241->243 righe; ultima riga admin_id=ed145ccb-... (Stefano reale, NON Empty), admin_email/action=login corretti. Build Server 0 errori.
NOTA: bug era latente ma count fermo dal 14/07 -> regressione da un deploy quel giorno (AdminUser perse le annotazioni o _readJsonOptions perse case-insensitive). Il fix e' corretto a prescindere.

## Piracity Vetrina — Newsletter reale (subagent Next.js, CAT, non committato)
Sostituito il form newsletter finto con flusso reale end-to-end.
- Migration 019 (piracity.newsletter_subscribers + newsletter_dispatches, RLS deny-all, NOTIFY pgrst) applicata su piracity_cat.
- Endpoint: POST /api/newsletter (iscrizione idempotente, email lowercase, token base64url); GET /api/newsletter/unsubscribe?token (pagina conferma localizzata); POST /api/newsletter/dispatch (protetto x-cron-secret==NEWSLETTER_CRON_SECRET, idempotente su period YYYY-MM, ?period + ?dryRun).
- Contenuto dispatch: nuove mappe (is_official+published, created_at mese precedente, cap 12) + top 5 plays_count; email per-iscritto nella sua lingua (newsletter-strings.ts 21 lingue, native 15 + fallback EN per bn/ur/sw/mr/te/ta); desc da map_descriptions public lang iscritto fallback IT; link vetrina {SITE_URL}/{lang}/mappe/... + disiscrizione col token. Invio via Resend (sendNewsletterEmail in lib/email/send.ts), degradazione morbida se manca RESEND_API_KEY.
- Self-test reale su :6010 (a-d) tutti verdi; residui test = 0. tsc 0 errori, next build OK.
- Env nuovi: NEWSLETTER_CRON_SECRET (+ documentati RESEND_API_KEY/RESEND_FROM in .env.example).
- PENDENTE: trigger cron mensile (orchestratore chiama /dispatch col secret). Commit/push al via di Stefano.

## BookingAgenda.razor - nuova prenotazione + date picker header
- Tipo prenotazione: mostra solo tipi disponibili (service se >=1 servizio attivo, resource se >=1 risorsa attiva); nascosto se uno solo; empty-state se zero; default a tipo reale (resource in planning se c'e').
- Campo Persone (int, default 1) in entrambi i branch; salvato in Participants solo branch service (CreateBookingRequest lo supporta; CreateManualResourceAsync no -> limite).
- Filtro risorse per capienza: Capacity/CapacityMin (NON max_seats/min_seats: quelli sono su BookingEntry/addon). Deseleziona risorsa non idonea al cambio persone; empty-state "Nessuna risorsa per N persone".
- Date picker header: label calendario apre popover input date -> imposta _currentDate + LoadAgenda (allinea lunedi in week). Label min-width 15.5rem (mobile min-width:0).
- resx x10 nuove: bkgagenda_people_tip, bkgagenda_no_resource_for_people, bkgagenda_no_bookable, bkgagenda_pick_date. Build 0 errori, puntify-app 200.

## Puntify — Stato per-stazione ordini menu (CAT, non committato)
Subagent dev su collaudo. Cucina e bar avanzano indipendenti; overall = stazione meno avanzata (delivered solo se tutte delivered).
- Migration 20260720_menu_order_station_status.sql applicata su puntify_cat (kitchen_status/bar_status + backfill + NOTIFY pgrst).
- Modello MenuPublicOrder: +KitchenStatus/+BarStatus. Helper condiviso MenuOrderStations.ComputeOverall.
- Server MenuController: creazione (public+merchant) inizializza stazioni presenti; nuovo endpoint PATCH /station-status; endpoint globale + pay allineano stazioni; email 'pronto' differenziata dine_in vs takeaway (6 lingue); public status endpoint ora ritorna anche order_mode+table_label.
- Client: MenuOrdersApiService.SetStationStatusAsync; KitchenDisplay/DrinksDisplay filtrano/avanzano per stazione + "Tav. N" (chiave display_table_prefix 10 lingue).
- Dine_in "delivered" NON chiude/nasconde ne libera il tavolo finche non pagato: Dashboard Ordini/Risorse rivisti (occupato = !paid && !cancelled); colonna "Servito - da incassare" (dashboard_col_served_to_cash 10 lingue).
- Build server+app 0 errori; restart ok (:8001/:8002=200). 10 unit test ComputeOverall PASS + self-test DB round-trip su Pepto (0 residui).
