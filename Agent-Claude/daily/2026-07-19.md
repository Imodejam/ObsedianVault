
## Error feedback prenotazione manuale (Agenda Puntify)
- Server: CreateBookingAsync ora lancia codici stabili (service_not_found/booking_in_past/shop_closed/slot_taken/create_failed); BookingController /book risponde 400 {error:"<code>"}.
- App: BookingApiService.CreateBookingAsync → tupla (result, error); BookingAgenda mappa i codici su toast specifici (bkgagenda_err_past/_closed/_slot_taken) in 10 lingue.
- Test live su pepto: booking_in_past/shop_closed/slot_taken/service_not_found verificati + create 200 → booking e closure di test rimossi. Build 0 errori, servizi riavviati. Nessun commit.

## Puntify — fix critici sistema fedeltà (subagent)
- F1: nuovo endpoint POST /api/receipts/decide (ReceiptsController) — punti ricalcolati server-side da total_amount × ratio corrente, claim atomico idempotente; ReceiptApproval.razor non scrive più via PostgREST.
- F2: formula canonica floor(importo€×ratio) in Punto.Shared/Services/LoyaltyPoints.cs; fixato ScanReceipt.razor (floor prima della moltiplicazione), unificati Scan.razor e StripeController.
- F3: età scontrino wall-clock nel fuso del negozio (TimeZoneHelper.AsWallClock); prima UTC vs wall-clock (~2h grazia indebita su host UTC).
- F4: webhook Stripe NON gestisce refund (charge.refunded assente) — gap documentato, nessuna implementazione (design da approvare).
- F5: card approvazione mostra "Saldo attuale: X → dopo approvazione: Y" (resx ×10 lingue).
- Test: 21 unit (LoyaltyReceiptTests) + live CAT su /api/receipts/decide (forgiatura 9990→9, idempotenza, ratio 0, amount 0, 403 non-owner); dati test deadbeef ripuliti. Build 0 errori, servizi riavviati sequenziali, 401/200 ok. NO commit.

## POS/order hardening (subagent)
- P1 concorrenza conto: PATCH /items ora accetta updatedAt opzionale -> 409 conflict + toast/reload in Cassa (Dashboard.razor); retrocompatibile.
- Fix collaterale: UpdateOrderItems usava due DateTime.UtcNow (risposta != DB, falso 409) -> timestamp unico.
- P2: GenerateOrderCode POS senza fallback count(*) (retry RPC 1x, poi 503).
- P3: policy rate-limit "ocr" 5/min per IP (XFF ultima voce) su /api/receipts/analyze.
- P4: prezzo mancante evidenziato in POS/Cassa/listino (pos_price_not_set, 10 lingue).
- Adjacent (non fixati): PATCH items su ordine pagato = 200, doppio /pay = 200, dishes non filtrati per shop, stesso fallback COUNT nel MenuController pubblico.
- Test: 200/409/legacy ok, 3 create parallele -> codici distinti, 429 + reset finestra ok. Ordini test eliminati, contatore Pepto ripristinato.
