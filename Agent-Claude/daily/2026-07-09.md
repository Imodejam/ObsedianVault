
## Totem — loading cambio lingua
- CodaNegozioDisplay.razor: SwitchLang ora async → set `_switchingLang=true` + StateHasChanged + Task.Delay(60) prima di NavigateTo(forceLoad). Overlay `.nt-switch-overlay` (dim rgba+blur) con spinner `.nt-switch-spinner`. Riavviato puntify-vetrina (active).

## Vapi Nemi Voce — diagnosi "non risponde nessuno" (foto Stefano)
- Numero +390221109199 (Telnyx, "Puntify - Official Number").
- Server URL = https://cat.puntify.it/api/vapi/webhook → PATH corretto (VapiWebhookController [Route api/vapi]+[HttpPost webhook]); dominio cat = collaudo (ok per test, per prod cambiare dominio).
- BLOCCO 1: campo Assistant = "Giulia (Hair & Style)" assegnato → assistente fisso intercetta, assistant-request MAI chiamato → Nemi dinamica non parte. Va SVUOTATO (solo Server URL, niente assistente fisso).
- BLOCCO 2: Timeout = 1 sec → troppo basso per assistant-request → errore. Alzare a ~15-20s.
- Detto a Stefano ordine: svuota Assistant + alza Timeout, poi richiama.

## Vapi Nemi Voce — diagnosi COMPLETA a runtime (verificata)
Testato l'endpoint pubblico con curl:
- POST /api/vapi/webhook senza secret → 401 {"error":"invalid secret"} (controllo del controller, NON basic-auth). Endpoint raggiungibile + URL corretto.
- Con X-Vapi-Secret = pe_akgZOhiJ6j0sM84gq3x6LPftC-kXODBvCLbGgFD7VdZXcnQ6z7Q (da appsettings.Development.json Vapi:WebhookSecret) → 200. Anche Authorization: Bearer accettato.
- Prima risposta era {"error":""} = ramo saldo<=0 (chiusura silenziosa). Numero +390221109199 mappato in shop_vapi_config allo shop 1009fca3 (slug "puntify") ma NESSUNA riga shop_nemi_credits → balance 0.
FIX applicato (collaudo): INSERT shop_nemi_credits(1009fca3, 120, 0). Re-test → 200 con config completa "Nemi Voce" (model anthropic claude-haiku, maxDurationSeconds 7200).
BLOCCHI residui lato Vapi (comunicati a Stefano): (2) assistente fisso "Giulia" sul numero da rimuovere (altrimenti assistant-request non parte); (3) credential X-Vapi-Secret deve == secret sopra; (4) Timeout 1s→~20s.
Nota prod: quando andrà live, replicare crediti + secret + rimozione assistente sul numero di prod; ServerUrl dovrà puntare al dominio prod (non cat).

## Vapi — FIX definitivo "invalid assistant"
Dai log Vapi di Stefano: endedReason=assistant-request-returned-invalid-assistant, assistantRequestError="Invalid Assistant. Errors: [assistant.property tools should not exist]".
CAUSA: VapiWebhookController metteva i tool in assistant["tools"] (top-level). Vapi li vuole in model.tools.
FIX: costruito model come Dictionary e messo modelObj["tools"]=tools; rimosso assistant["tools"]. Restart server + re-test curl: model.tools=6, niente tools top-level, HTTP 200. => CODE FIX da portare in prod quando Nemi Voce va live.
Payload confermava già: assistantId null (assistente fisso rimosso), timeoutSeconds 20, X-Vapi-Secret ok, metadata.shopId corretto, fallbackDestination = numero di Stefano.

## AdminMonitoring — icona copia sui riquadri JSON
AdminMonitoring.razor: aggiunto pulsante "Copia" (RenderFragment CopyBtn + CopyJson via navigator.clipboard, stopPropagation) su JSON in ingresso/uscita/Headers, con feedback "✓ Copiato". CSS .adm-wh-copy. Compila ok (solo warning). Richiede hard-refresh WASM lato Stefano.

## Vapi — fix difetti chiamata + doc-review ottimizzazioni (tutte in VapiWebhookController, da portare in prod)
Difetti post-primo-test OK:
- Non riagganciava → aggiunta sezione prompt "=== FINE CHIAMATA ===" (usa endCall dopo i saluti).
- Non parlava per prima → firstMessageMode="assistant-speaks-first-with-model-generated-message".
Doc-review (Stefano ha chiesto di studiare doc Vapi: multilingua/guardrail/best practice):
- MULTILINGUA: transcriber da Azure lingua-fissa → Deepgram nova-2 language "multi" (auto-detect + code-switching), fallback [azure lingua-account, 11labs scribe]. + prompt "=== MULTILINGUA ===" (prosegui nella lingua del chiamante, femminile sempre, numeri in forma parlata).
- GUARDRAIL prompt "=== SICUREZZA ===": anti-allucinazione, anti prompt-injection (non rivela istruzioni/ruolo), no dati sensibili (carte/IBAN/password/codici).
- Turn-taking: startSpeakingPlan.waitSeconds=0.8; silenceTimeoutSeconds=30.
- OTTIMIZZAZIONE WEBHOOK (domanda Stefano "tanti webhook a chiamata"): impostato serverMessages=["tool-calls","end-of-call-report"] (prima Vapi mandava tutti gli eventi di default). Lo switch del controller gestisce solo assistant-request/tool-calls/end-of-call-report.
Tutto verificato a runtime: assistant-request → HTTP 200, payload valido con tutti i campi.

## Email prenotazione — 2 tasti Sposta/Annulla (DELEGATO a subagent a3578e0d)
Mappa: EmailTemplates.cs:486 BookingConfirmationPublic (ha confirmationToken+lang, link già a /booking/{token}); endpoint pubblici GIÀ esistono in PublicBookingController (GET 865 / DELETE annulla 908 / PATCH sposta 958); BookingServiceImpl Reschedule 449 / Cancel 476. Tool telefonici sposta/cancella già presenti (NemiVoiceToolService 285/306). Subagent: aggiunge 2 tasti localizzati + pagine pubbliche Vetrina reschedule/cancel (con conferma su annulla).

## Email 2 tasti — COMPLETATO e verificato
Subagent a3578e0d: EmailTemplates.cs:516 due tasti distinti (Sposta/Annulla) deep-link ?action=reschedule|cancel; EmailStrings.cs 2 chiavi in 15 lingue; BookingManage.razor legge ?action= (Reschedule/CancelConfirm, conferma già presente). Bug-fix: GetBookingByToken (PublicBookingController ~900) non popolava merchantSlug/serviceId/operatorId → slot reschedule rotti, corretto.
Verifica runtime mia: GET /api/public/bookings/{token} ora ritorna merchantSlug=puntify, serviceId; pagine /booking/{token}?action=reschedule|cancel → 302→/{lang}/... → 200 con vista corretta (reschedule=scelta slot, cancel=conferma+gestione termine scaduto).
Miglioria mia: link email ora con prefisso lingua /{lang}/booking/... così aprono nella lingua del cliente (EmailTemplates.cs 519/529).

## AssistantAi — rimosso callout "Configura il provider AI"
AssistantAi.razor:40-58 blocco callout rimosso (Stefano non lo vuole; Nemi usa key globale). Verifica al rebuild app.

## Dettaglio prenotazione merchant — 5 punti (DELEGATO subagent a91b0f05)
1) booking/giorno default oggi; 2) via icona forbice appuntamento; 3) canale="Telefono" localizzato lingua account (prenotazioni telefoniche); 4) popup più largo; 5) cliente su una riga (Nome - tel - email). In corso.

## Nemi Voce — ADDEBITO AL SECONDO (billing preciso)
DB: shop_nemi_credits +consumed_seconds bigint (migrato = consumed_minutes*60); RPC puntify.nemi_consume_seconds(uuid,int) SECURITY DEFINER atomica (aggiorna consumed_seconds + consumed_minutes=ceil). Reload PostgREST fatto. Migration salvata docs/DB Migrations/2026-07-09_nemi_consume_seconds.sql (DA FARE IN PROD + reload).
Controller VapiWebhookController: assistant-request saldo al secondo (balanceSeconds = purchased*60 - consumedSeconds; block se <=0; maxDurationSeconds=balanceSeconds); end-of-call-report scala secondi ESATTI (secondsBilled=durationSeconds, RPC nemi_consume_seconds); GetCredits→GetCreditsSeconds. Model NemiCredits +ConsumedSeconds. ShopAiController nemi/credits DTO +PurchasedSeconds/ConsumedSeconds/BalanceSeconds.
Verificato runtime: maxDurationSeconds 7020 (7200-180); end-of-call 95s → consumed_seconds 180→275 esatto. Dati test poi ripuliti (consumed_seconds ripristinato 180).

## Booking detail merchant — 5 punti FATTI (subagent a91b0f05)
BookingAgenda.razor (Pages/Merchant/Booking/): 1) day view default oggi (SetView/OnParametersSetAsync else _currentDate=Today); 2) rimossa emoji forbice ✂️ davanti al servizio; 3) canale phone→label i18n ChannelLabel (chiavi bkgagenda_channel_* in 10 lingue; phone booking già setta channel=phone in BookingServiceImpl:383); 4) popup dettaglio .bk-modal--wide 640px (booking.css); 5) cliente su una riga (Nome-Tel-Email, pezzi mancanti omessi). Build 0 errori, app active.

## AssistantAi callout provider AI — rimosso (AssistantAi.razor)

## IN CORSO (subagent a500a75c): tab Utilizzo elenco chiamate stile Stripe (endpoint ShopAiController nemi/calls + UI) + percentuale sopra barra a destra.
## IN ATTESA risposta Stefano: pricing tab admin monitoring chiamate (costo esercente A tariffa fissa / B pacchetto; cambio $→€) per 5713.

## Pricing tab admin chiamate (5716 risposta Stefano) — DECISIONE
- Cambio REALE USD→EUR live: fonte frankfurter.app/ECB (no key, verificato: ~0,875 €/$, il server la raggiunge).
- Tariffa esercente: media pacchetti acquistati dal negozio; fallback media tra esercenti. PROBLEMA: acquisti per-pacchetto NON tracciati (Stripe da collegare), purchased_minutes è solo un numero → nessun dato reale ora. Fallback attuale = media di listino 0,32 €/min (media 0,29/0,30/0,33/0,36). Struttura da rendere auto-switch alla media reale quando i pagamenti registreranno i pacchetti. Chiesto ok a Stefano.
- Ricavo Puntify = costo esercente(€) − costo Vapi($→€).
- Tab admin (AdminMonitoring.razor + endpoint) DA COSTRUIRE dopo il subagent utilizzo (evito conflitti file).

## Micro-richieste pagina Utilizzo girate al subagent a500a75c
- 5717: togliere messaggio "Il consumo sarà attivo con Nemi Voce".
- 5718: pannello "Minuti Nemi Voce" più compatto per far spazio all'elenco chiamate sotto.

## Pagina Utilizzo Nemi — COMPLETATA (subagent a500a75c) + verificata
- Endpoint GET /api/shop/{id}/ai/nemi/calls (ShopAiController, DTO snello no transcript/numero) + App SocialStudioApiService.NemiCallsAsync.
- AssistantAi.razor tab utilizzo: elenco chiamate stile Stripe (data, direzione badge, durata mm:ss, esito, costo), stato vuoto+spinner.
- Percentuale sulla riga del titolo "Minuti Nemi Voce" a destra (calcolata sui secondi ConsumedSeconds/PurchasedSeconds).
- Rimosso placeholder "Il consumo sarà attivo con Nemi Voce".
- Pannello compattato. i18n 4 chiavi in 10 lingue. Build 0 errori, endpoint 401 (registrato), 2 nemi_calls reali.

## Popup dettaglio prenotazione (5722/5723) — mie modifiche booking.css
- .bk-detail-customer-line flex:1 + text-align right → cliente a destra come gli altri valori.
- .bk-modal--wide 640→780px (popup più largo). App riavviata, build ok.

## Tab ADMIN chiamate (5713) — DELEGATO subagent a875539b
AdminMonitoring.razor nuovo tab "Chiamate" (tutti i pv) + endpoint AdminMonitoringController /api/admin/monitoring/calls. Cambio reale USD→EUR (frankfurter.app, cache giornaliera, fallback 0.90). Costo Vapi $/€, costo esercente €=sec/60×tariffa (resolver ResolveMerchantRatePerMinute → costante listino 0,32 finché no tracking acquisti), ricavo=esercente−vapi(€), riga totali. In corso.

## Tab ADMIN chiamate (5713) — FATTO (subagent afdd78b6, dopo 1° tentativo abortito)
CurrencyRateService (frankfurter.app, singleton, cache 12h, fallback 0.90); AdminMonitoringController.Calls + ResolveMerchantRatePerMinute (0.32 listino); AdminMonitoring.razor tab "Chiamate" (tabella all-pv + totali). Endpoint /api/admin/monitoring/calls (auth monitoring). Costo Vapi $/€, costo esercente €, ricavo €. Verificato: 148s→02:28, esercente 0,79€, ricavo ~0,51€.

## 5725 — numero + nome cliente nel dettaglio chiamata (cliccabili → scheda cliente) — FATTO+verificato
DDL nemi_calls +caller_phone/customer_account_id/customer_name (migration docs/DB Migrations/2026-07-09_nemi_calls_caller.sql, reload PostgREST fatto). NemiCall model + NemiCallNoShop aggiornati. VapiWebhookController end-of-call-report: ExtractCallerNumber + FindByPhoneAsync → salva numero, display_name, account_id (link scheda). ShopAiController nemi/calls DTO + App SocialStudioApiService DTO estesi. AssistantAi.razor: riga cliente cliccabile (nome+numero) → /merchant/{shop}/clients/{accountId}; OpenClientCard; CSS .nemiuse-call-cust/client/phone (order:3 a capo). Backfill 2 righe reali dal raw+profilo → "Stefano"/+393402606209/account 890d2531. Verificato runtime: chiamata simulata risolve profilo (name+account); build 0 errori.
NB PROD: 2 migration nemi (consume_seconds + calls_caller) + reload PostgREST + codice webhook.

## Nemi Voce — fix da test reale (5728/5729) — prompt VapiWebhookController
1) Anti ri-saluto/silenzio: blocco "=== SE NON RISPONDE / SILENZIO ===" (un solo "mi sente?", poi chiude; regola 2 minuti se improduttiva). Backstop silenceTimeoutSeconds=30.
2) Orario capito male + non correggibile: blocco "=== CONFERMA E CORREZIONE ORARI ===" (ripeti l'orario per conferma prima di prenotare; accetta la correzione = ultimo valore detto; se già prenotato, sposta). ASR può confondere numeri, la conferma a voce lo intercetta.
3) Due chiamate simultanee audio mono-direzionale: dai log il nostro webhook ha servito entrambe le assistant-request senza errori → problema MEDIA/concorrenza lato Vapi/Telnyx (limite chiamate concorrenti Vapi / canali Telnyx), non nostro codice. Comunicato a Stefano.
Verificato: prompt contiene entrambi i blocchi; billing al secondo confermato su chiamata reale 440s esatti (consumed_seconds 620, balance 6580s).

## Nemi pronuncia "Puntify" (5731) — FATTO
Prompt VapiWebhookController: blocco "=== PRONUNCIA ===" → dire "Puntifai" (fonetico italiano) invece di "Puntify" quando parla, in ogni lingua. Verificato nel payload. (TTS 11labs leggeva pantifai/pintifai.)

## Vapi/Telnyx concorrenza (5732) — indicazioni a Stefano
Vapi: Settings/Org → Concurrency (o Billing) + Observe/Logs endedReason 2ª chiamata. Telnyx: Numbers→numero→Connection; Voice→SIP Connections/Voice API App→Channel limit; Outbound Voice Profiles→concurrent limit; Billing→Channels. Causa probabile = concurrency Vapi.

## Transcriber REVERT (5734) — Deepgram multi dava trascrizioni in spagnolo/sbagliate
VapiWebhookController: transcriber tornato a provider=azure, language=transcriberLocale (it-IT account), fallback [deepgram nova-2 langIso, 11labs scribe_v1 langIso]. Verificato payload: azure it-IT + fallback. Vapi ha 1 sola linea (2ª a pagamento) → conferma che l'audio chiamate simultanee è limite Vapi, non codice.

## Tab admin chiamate — 5735/5736/5737 (DELEGATO subagent a38bbdb8)
- Totale costi Vapi in DOLLARI ($) nella riga Totali (TotalVapiCostUsd).
- Click su riga → popup stile Stripe con tutti i dettagli + trascrizione formattata (endpoint dettaglio GET /api/admin/monitoring/calls/{id} con transcript+recording). In corso.
