# 2026-06-22

## Piracity-web — riprogettazione homepage (landing luminosa familiare)
- Stefano: brief dettagliato (14 sezioni, copy completo, prompt immagini) per pivot da tema dark/pirata a landing luminosa premium familiare (Apple+Disney).
- Decisioni: foto le passa lui man mano (io placeholder + manifest prompt); tema chiaro su home+navbar+footer, pagine dark restano per ora.
- Avviato subagent build in background. Tema chiaro isolato per non rompere pagine legali/blog. i18n 5 lingue.
- Next: review build + screenshot + report a Stefano prima di pubblicare.

### Piracity-web homepage — build completato (review pendente)
- 14 sezioni luminose live su dev (http://piracity-dev-web.duckdns.org/). tsc+lint ok, responsive, 5 lingue.
- App reale collegata alle CTA principali: https://app-cat.piracity.app/ (target _blank). Gestione href esterni in CtaPrimary/Secondary.
- Gotcha: animazioni framer whileInView non visibili in screenshot headless full-page (ok per utenti reali). Hero verificato via bypass temporaneo poi ripristinato.
- Foto: Stefano le carica man mano in /public/assets/photos/ (12 slot). Prompt in docs/image-prompts.md.
- Pendenti conferme: footer Missioni/Contatti, CTA eventi/famiglia interne o app.

### Piracity-web — foto integrate
- Stefano ha inviato 12 foto (numerate 1-12 = suoi prompt). Mappate per aspect ratio e cablate in tutte le sezioni. Aggiunta foto a Cos'è e Per chi è. Live su cat.piracity.app.
- Step "Come si gioca" rinominati digitali (5 lingue). Caption allineate a smartphone (no oggetti fisici).
- Restano placeholder: timeline + tesoro (Stefano deve fornire/scegliere 2 foto). Inviate hero + panoramica completa.

### Piracity-web — riposizionamento INCLUSIVO (in corso)
- Stefano: spostare da "famiglie/bambini" a "avventura urbana per chiunque" (single/coppie/amici/famiglie/turisti/colleghi/studenti/gruppi). Spec sez 1-8 + scrub linguaggio family ovunque.
- Modifiche struttura: Cos'è 3→4 card (+"Vivi la tua trasformazione"), Per chi è 5→6 target (Da solo/In coppia/Con amici/In famiglia/In viaggio/Per eventi), Eventi 3→5 card, step "Leggi la pergamena"/"Completa l'avventura", sez Family→"trasformazione", Adults→"Non solo famiglie".
- Delegato a subagent (ab09211...). 2 foto nuove attese da Stefano: hero gruppo misto + "trasformazione" ciurma epica.

### Piracity-web — slideshow sezione "Per ogni tipo di ciurma"
- Nuovo componente AudienceCarousel.tsx: slideshow 16:9 auto (4.2s) + frecce + puntini, 6 card cliccabili sincronizzate (highlight attiva), pausa su hover, reduced-motion. Audience.tsx semplificato.
- Slot foto: audience-{solo,couple,friends,family,travel,events}.png (placeholder onError finché Stefano non manda le 6).
- i18n: aggiunte chiavi audience.prev/next in 5 lingue. tsc+lint ok, live.

### Piracity-web — FAQ inclusive + CTA finale
- FAQ: 4 voci inclusive (solo per famiglie? / giocare da solo? / coppia? / adulti?) in 5 lingue, Faq.tsx KEYS a 9.
- CTA finale: "Raduna la tua ciurma. O parti da solo." + testo nuovo. Foto finale da sostituire (no famiglia).
- Parità 137 chiavi, tsc+lint ok.

### Piracity-web — foto carosello + trasformazione
- Caricate 6 foto carosello "Per ogni tipo di ciurma": audience-{solo,couple,friends,family,travel,events}.png (verificate visivamente, 16:9). Slideshow ora attivo.
- gruppopirati → family.png (sezione "Per un momento diventi un pirata"/trasformazione, sostituisce la famiglia).
- Ancora senza foto: hero (gruppo misto), finale (no famiglia), timeline, tesoro.

## Marketplace Piracity-web — build completata (subagent)
File creati: app/[locale]/marketplace/page.tsx; app/api/checkout/route.ts; app/api/webhooks/stripe/route.ts; app/api/admin/login|logout/route.ts; app/[locale]/admin/{page,login,orders,orders/[id]}.tsx; lib/supabase/{admin,orders,auth}.ts; lib/stripe/server.ts; components/marketplace/{BuyButton,StatusBanner}.tsx; components/admin/{AdminLoginForm,LogoutButton,StatusBadge}.tsx; supabase/migrations/016_marketplace_orders.sql.
Modificati: lib/supabase/maps.ts (fetchSellableMaps); Navbar/Footer (link Marketplace); mappe/[citySlug]/[mapSlug]/page.tsx (BuyButton); content/i18n/*.json (namespace marketplace + nav.marketplace + footer.marketplace, 5 lingue); .env.example/.env.local (SERVICE_ROLE copiato + placeholder Stripe).
Pacchetti: stripe@22 + @stripe/stripe-js + @supabase/ssr@0.12.
Email guest: modale inline raccoglie email prima del redirect; se loggato usa email/id sessione (server); fallback Stripe Checkout raccoglie email + recupero da webhook customer_details.email.
Auth admin: route /api/admin/login → signInWithPassword (ssr cookie) + check piracity.users.role='admin' (service-role); se non admin signOut+403. Pagine admin server-side getAdminUser() (getUser valida token) → redirect /admin/login se non admin.
Verifica: tsc 0 err, next lint 0, curl /marketplace+/{it,en,es,de,fr} = 200, home/static 200, /api/checkout 503 graceful, parita i18n 357. DB irraggiungibile dal tooling (fetch failed anche su home): sellable count NON verificato, empty-state 200.
Migration NON applicata. Comando (self-hosted, schema piracity): psql "$DATABASE_URL" -f supabase/migrations/016_marketplace_orders.sql  oppure docker exec -i <postgres-container> psql -U postgres -d <db> < ...016.sql  oppure Supabase MCP apply_migration.
RESTA per attivare: chiavi Stripe (STRIPE_SECRET_KEY/NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY/STRIPE_WEBHOOK_SECRET), applicare migration 016, configurare webhook su dashboard Stripe → https://cat.piracity.app/api/webhooks/stripe. NON committato.

### Piracity-web — marketplace + Stripe + admin COSTRUITO
- /marketplace + checkout Stripe (guest+login) + webhook + orders/order_events (migration 016 file) + admin protetto (supabase user/pwd + role). tsc/lint ok, route 200, 503 graceful senza chiavi, i18n 357 chiavi ×5. NON committato.
- ⚠️ DB supabase-cat GIÙ (TLS rc=35) → niente mappe sul sito ora. Infra. Da riavviare lato server.
- Attivazione pendente: chiavi Stripe + applicare migration 016 + webhook Stripe (cat.piracity.app/api/webhooks/stripe).

### Piracity-web — DB riconnesso
- Inserite nuove chiavi (anon+service-role) per api-cat.piracity.app in .env.local, servizio riavviato. DB ok: pagine città mostrano mappe.
- Nuova home non elenca mappe (by design, solo landing). Marketplace VUOTO perché le 3 mappe hanno price=NULL → chiesti i prezzi a Stefano (li scrivo io via service-role).
- Restano: chiavi Stripe + applicare migration 016. Note minori: cover mappa rotte (host vecchio), pagine città dark.

### Piracity-web — prezzi mappe BLOCCATI da trigger legacy
- Prezzi pronti (listino vault): Roma 11,99 / Cosenza 11,99 / Shanghai 14,99 €. Da scrivere via service-role.
- BLOCCO: UPDATE maps → "relation piratopoly.cities does not exist" (trigger legacy piratopoly sul DB CAT). Va corretto da Stefano in DbGate (SQL fornito). Poi scrivo i prezzi.

### Piracity-web — SBLOCCO marketplace (box=pro-open, docker exec)
- Mia assunzione errata corretta: il box claudebot È pro-open; sudo docker exec ops-postgres psql → DDL su piracity_cat.
- Fixati 2 leftover piratopoly: update_city_maps_count (trigger maps) + update_user_score → piracity.
- Prezzi scritti (Roma/Cosenza 11,99, Shanghai 14,99). Marketplace mostra le 3 mappe con Acquista. Screenshot inviato.
- Restano: chiavi Stripe + migration 016 (posso applicarla io via psql). Cover mappa rotte (host vecchio).

### Piracity-web — cover fix + migration 016 applicata
- Cover mappe: base PWA_ASSET_BASE in maps.ts → app-cat.piracity.app (vecchio piracity-dev-app giù). Cover ora visibili nel marketplace (next/image ottimizza da app-cat, già in remotePatterns).
- Migration 016 APPLICATA a piracity_cat (sudo docker exec): tabelle orders + order_events, RLS, policy admin. Marketplace completo (3 mappe cover+prezzo+Acquista).
- Resta SOLO Stripe: chiavi (sk/pk/whsec) + webhook su dashboard Stripe (cat.piracity.app/api/webhooks/stripe). Checkout=503 finché mancano.

### Piracity-web — pagina dettaglio mappa ristrutturata
- /mappe/[city]/[map] riscritta: FOTO città banner in cima (foto-first, richiesta Stefano), poi titolo+descrizione + card acquisto sticky (prezzo/Acquista/Anteprima) + "Cosa scoprirai" 3 punti. Tema luminoso (landing primitives), SEO/JSON-LD invariati. tsc/lint ok.

### Piracity-web — filtro marketplace stile Airbnb
- Nuovo MarketplaceClient.tsx (client): barra pillola Dove (dropdown città + conteggio) + Date (Dal/Al facoltative). Filtro client-side istantaneo, contatore, azzera, outside-click. Date filtrano SOLO le stagionali (overlap validFrom/validTo); permanenti sempre visibili. Responsive, 5 lingue (marketplace.filter.*), tsc/lint ok.
- page.tsx: calcola city list, delega a MarketplaceClient; rimosso console.log debug.

### Piracity-web — filtro marketplace: calendario + fix allineamento + header
- Pannelli dropdown ancorati al proprio segmento (relative wrapper) → niente più sfasamento.
- RangeCalendar.tsx: calendario mensile navigabile, selezione intervallo from→to, range evidenziato, localizzato (Intl). Sostituisce gli input date.
- Navbar: "Marketplace" spostato in prima posizione + enfasi (coral + 🗺️), desktop+mobile.
- tsc/lint ok. Verificato via screenshot (apertura pannelli forzata temporaneamente poi ripristinata).
- Marketplace filtro: dopo selezione città → chiude 'Dove' e apre auto pannello date (flusso guidato). setOpen('dates') nei 2 bottoni città.

### Piracity-web — recensioni (display) + rimosso Anteprima
- Rimosso bottone "Anteprima" dalla pagina dettaglio mappa.
- Sezione RECENSIONI sulla pagina mappa: lib/supabase/reviews.ts (fetchMapReviews, embed reviewer→users) + MapReviews.tsx (media stelle + card autore/data/voto/testo + galleria foto quando presenti). i18n mapPage.reviews ×5. Reviews table senza RLS → anon legge. Aggiunta colonna reviews.photos text[].
- ESISTE GIÀ backend recensioni (tabella reviews stars/body, una per utente/mappa, solo chi ha completato; endpoint PUT review in maps.routes.ts) + meccanismo upload immagini (base64→webp app server). MANCA email.
- Proposte a Stefano 4 decisioni: provider email (Resend?), dimensioni voto (complessivo+Divertimento+Enigmi+Percorso), form nell'app di gioco, moderazione foto. In attesa.
- Avviato subagent: conversione tema luminoso di tutte le pagine dark restanti (citta, blog index+post, privacy, cookie, termini, chi-siamo, partner, per-aziende). Riferimento: marketplace/mappa. Mantiene contenuti/SEO/i18n.

### Piracity-web — conversione tema luminoso completata (tutte le pagine)
- Convertite: citta/[slug] (griglia mappe stile marketplace), blog index, privacy/cookie/termini/chi-siamo/partner/per-aziende (componente condiviso InfoPage.tsx). blog/[slug] era placeholder notFound. tsc/lint/curl 200.
- REGRESSIONE intercettata e corretta: il subagent aveva perso chiavi i18n nav (audience/events/faq/marketplace) e footer (missions/events/faq/contact/marketplace) → navbar/footer mostravano chiavi grezze su TUTTO il sito. Ripristinate in 5 lingue, parità ok.
