# Daily 2026-06-06

## Puntify Admin · ingresso da pagina Account (msg 3348) FATTO+DEPLOY
- Rimosso tasto Amministrazione dalle home (Wallet.razor + Shops.razor) — revert completo (inject/campo/check/markup).
- Aggiunta sezione "Amministrazione" (cfg-card con riga cliccabile → /admin) in ClientAccount.razor (/account) e MerchantAccount.razor (/merchantAccount), visibile solo se AdminApi.GetMeAsync()!=null.
- Confermato: stefano.gitto@hotmail.com unico admin attivo.
- Build 0 err, puntify-app riavviato (Ctrl+F5).

## Puntify Admin · fix visibilita + tasto "Cambia ad amministratore" (msg 3350/3351) FATTO+DEPLOY
- BUG: la sezione admin non compariva perche `puntify-server` era avviato il 4 giu 23:41, PRIMA del build con AdminController (5 giu 06:18) → /api/admin/me dava 404 → GetMeAsync null → niente voce. Riavviato puntify-server: ora /api/admin/me = 200 (profile super, 4 sezioni). Verificato con JWT HS256 forgiato (JwtSecret) + X-API-Key reale dell App.
- Richiesta msg 3351: tasto "Cambia ad amministratore" (cfg-btn-outline, icona scudo → /admin) sotto "Cambia a cliente" nella card Navigazione di MerchantAccount; su ClientAccount sezione Amministrazione con stesso bottone. Rimosse le card/righe precedenti.
- MEMO: dopo modifiche al SERVER va riavviato puntify-server (non solo app). I servizi erano disallineati.
- Build 0 err, app+server riavviati.

## Puntify Area Admin F2-F5 (msg 3353) FATTO+DEPLOY
- SERVER: AdminService.Queries.cs (partial) + AdminRows.cs (DTO snake_case) + AdminDtos.cs (Punto.Shared, DTO risposta). Endpoint in AdminController via helper Guarded(section, action, ...) = ResolveAdmin + CanSee(sezione) + LogAsync + esecuzione. Endpoint: /clients[?q], /clients/{id}, /points, /appointments, /transactions, /merchants[?q], /merchants/{id}, /shops/{id}, /payments, /config. Aggregazione in-memory (dati piccoli) via PostgREST. Verificati tutti 200 con JWT forgiato.
- APP: AdminApiService esteso (metodi tipizzati). Pagine: AdminClients, AdminClientDetail (tab), AdminMerchants, AdminMerchantDetail, AdminShopDetail, AdminPayments (KPI+grafico SVG inline), AdminConfig + componente AdminTopBar. Card HOME ora navigano (tolto toast). admin.css esteso (liste, tab, KPI, grafico barre, chip, badge). v1 SOLA CONSULTAZIONE.
- Mappature dati: punti=sum(transactions.points); ricavi Puntify=commission_cents (stripe); volume=gross_paid_cents; erogato=net_merchant_cents; ownership esercente↔shop via account_shops; appuntamenti cliente via bookings.customer_id; fee per-PV=application_fee_bps; scaglioni 80/65/50 bps; feature bits 1/2/4/8/16/32.
- GOTCHA risolto: nomi classi-pagina (AdminConfig/AdminClientDetail/AdminMerchantDetail/AdminShopDetail) collidevano coi DTO omonimi → qualificare i campi con Puntify.Shared.Models.Admin.*
- Build 0 err, app+server riavviati. Ctrl+F5.
