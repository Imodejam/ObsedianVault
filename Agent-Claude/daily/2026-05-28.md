# 2026-05-28

## Puntify — link condivisi booking senza prefisso lingua
- `BookingOptions.razor` (App merchant): `PublicBookingUrl` (`/m/{slug}`) e `DirectBookingUrl` (`/book/{slug}`) generavano l'URL con prefisso `/{CurrentLang}/`.
- Stefano: il link da condividere non deve contenere la lingua → lingua automatica.
- Fix: rimosso `/{CurrentLang}/` e la proprietà `CurrentLang` (dead code). Il middleware Vetrina (Program.cs) già redirige `/m/*` e `/book/*` senza prefisso alla lingua del browser (Accept-Language).
- Build App OK (0 errori), restart `puntify-app.service` → active.

## Puntify — orari Agenda non devono mostrare orari operatori
- Stefano: configurando gli orari dell'agenda comparivano anche gli orari degli operatori.
- Causa: tabella `booking_availability` contiene sia righe PV (`operator_id` NULL) sia righe operatore (`operator_id` valorizzato).
  - **Load**: `BookingSchedule.razor` filtrava solo per `shop_id` → pescava anche righe operatore. Fix: skip righe con `OperatorId != null`.
  - **Save (bug correlato grave)**: `BookingController.SaveAvailability` faceva `DeleteAsync("booking_availability","shop_id=eq.{shopId}")` → cancellava ANCHE la disponibilità degli operatori a ogni salvataggio agenda. Fix: filtro `&operator_id=is.null` (pattern già usato in TableBookingController:231).
- Build App + Server OK (0 errori). Restart puntify-app + puntify-server.
- NOTA infra: i servizi girano con `dotnet watch`; il restart via systemctl manda in timeout lo stop del vecchio processo (SIGKILL) → stato "failed". Workaround: `systemctl reset-failed` + `start`. In dev il watch ricompila comunque al salvataggio del file.

### Prossimi passi
- Eventuale verifica E2E su device: copiare link da BookingOptions e confermare redirect alla lingua browser; verificare che salvando l'agenda gli orari operatori restino intatti.

## Puntify Menu prodotti/servizi — FASE 1 COMPLETA (commit d5144be era il pre-lavoro)
- Classificazione: `Punto.Shared/Models/CategoryMenuKind.cs` (enum MenuKind, set categorie a servizi: 3,4,5,19,20,21,25,26,38,39,40,41,43,44,46,50,52-65). `ShopExtensions.IsServiceBased()/MenuKind()`.
- DB (ops-postgres, docker exec -i): `shop_services` + colonne `photo_url`,`video_url`,`tags text[]`; nuova tabella `shop_menu_section_services` (section_id,service_id,sort_order) per Fase 2; grants anon/authenticated/service_role; NOTIFY reload.
- Modello `BookingService`: aggiunti PhotoUrl, VideoUrl, Tags.
- Server `PublicMenuController.GetActive`: ritorna `services` per shop a servizi; se nessun menu prodotti attivo ma ci sono servizi → 200 services-only.
- Vetrina `MerchantMenuPreview.razor`: blocco "I nostri servizi" (ServiceCardFragment: foto, nome, tag, descrizione, durata `FormatDuration`, prezzo `ServicePriceLabel` rispetta price_display show/from/hide, link video). 2 chiavi i18n in tutte le 20 lingue. CSS `.mp-service-*` in menu-public.css (?v=20260528a).
- `Merchant.razor` + `SupabaseStatsService.HasActiveServicesAsync`: link "Vedi il menu" raggiungibile per shop a servizi senza menu prodotti.
- Build Server+Vetrina+App OK. Restart server+vetrina. Verificato live: `GET /api/public/menu/puntify` ritorna service; pagina `/it/m/puntify/menu` prerenderizza "I nostri servizi".
- IMPORTANTE: lezione DDL → serve `docker exec -i` (senza -i lo stdin non arriva a psql).

## BACKLOG (da Stefano 2026-05-28, da affrontare dopo)
- Comprimere le foto PRIMA dell'upload su MinIO (se MinIO non lo fa già): con centinaia di foto rischio spazio. Strategia da concordare con Stefano.

## FASE 2 (editor) COMPLETA
- Modello `ShopMenuSectionService` (MenuModels.cs). Server: `GetMenu` ritorna `availableServices`+`sectionServices`; endpoint `POST /api/menu/sections/{id}/services` (link, dedup, sort), `DELETE /api/menu/section-services/{id}`, `PATCH .../{id}` (sort). `GetActive` ritorna `sectionServices`; PostgREST non supporta subquery in in.() → prima prendo gli id sezione poi i link.
- App: `MenuApiService.LinkServiceAsync/UnlinkServiceAsync/UpdateServiceLinkAsync`; `MenuDetailsResponse` esteso (AvailableServices, SectionServices nullable). `MenuEditor.razor`: lista servizi nelle sezioni (read-only nome/durata/prezzo + "modifica in Prenotazioni"), bottone "+ Aggiungi servizio" + picker, gate `IsServiceBased && canAddDish`; `SectionCountLabel`. CSS `.me-service-*` in booking.css.
- Vetrina `MerchantMenuPreview`: parse `sectionServices`; `ServicesInSection`/`UnlinkedServices`; servizi mostrati nella sezione (dopo i piatti), non assegnati nel blocco home; `CountOfSection` conta anche i servizi (sezioni solo-servizi appaiono nel nav).
- Verificato E2E (link via API, raggruppamento pubblico, editor GET) con menu temporaneo su `puntify`, poi cancellato (CASCADE).
- TODO prossimo: UI per editare i campi servizio del menu (foto/video/tag) dove si definiscono i servizi (Prenotazioni → Servizi). Poi FASE 3 (cascata) e FASE 4 (cross-sell).
