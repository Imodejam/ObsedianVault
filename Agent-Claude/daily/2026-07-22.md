# 2026-07-22

## Ripresa sessione (msg 7089 "prosegui")
Ieri notte interrotta per limite sessione durante il task 19. Verificato: nessuna modifica task 19 nel tree,
app allo stato task 18 (build 0 err, deployato). Ripreso con subagent fresco.

## Stato deploy CAT fino a task 18 (ieri, msg 7014-7086)
Rename->POS, pos-lock tutti i tab, Cassa (full-height/no-scroll/badge card+chip+giri/Asporto senza Tav/font 15px),
pagina Conto Stripe/Apple, Vetrina modal piatto orizzontale, Configurazione POS (Subito/Dopo x4 + Cassa pay-first),
refactor stati ordine (colonne lavorazione + pill pagamento, zero server), tooltip fix strutturale (position fixed
+ field-info.js), rimossa opzione pagamento Account cliente, separatori portata banda grigia.

## Task 19 in corso (msg 7087): Cassa vista focalizzata conto aperto
- CassaFocused (tab cassa + OrderRef/_cassaTableId/_cassaOrderId): via tab bar, back=history.back JS con
  fallback, titolo h1 "Tav. X - Y Pax - Stato" (stato = giro piu indietro per multi-giro), popup coperti al
  primo ingresso se non settati (flag _coversPromptedForRef anti-loop). Delegato a subagent fresco.

## Task 19 consegnato e deployato (msg 7087 -> 7091)
- CassaFocused = tab cassa && (OrderRef||_cassaTableId||_cassaOrderId). Tab bar in @if(!CassaFocused);
  BackToHome async: se focused history.back JS (torna da provenienza), altrimenti merchant/{id}/home.
- Titolo h1 CassaFocusedTitle(): dine_in "Tav X . Y Pax . Stato", altri "{modalita} . Stato" no Pax;
  .ord-search nascosta in focus. Stato multi-giro = collo di bottiglia (StatusRank min; gate coperti vince).
- Popup coperti: MaybePromptCoversOnEntry in ApplyOrderRefAsync ramo tavolo, flag _coversPromptedForRef anti-loop,
  OpenGuestsForConfirm se gate / OpenGuests se conto nuovo senza coperti. resx cassa_pax=Pax x10.
- Build 0 err, deploy CAT, report 7091.

## Bug A07 (msg 7093): pagamento forza delivered -> sparito da bacheca
- Dato reale: #A07 = e1e804a7..., status=delivered, paid=true, dine_in, covers NULL.
- Causa: pay-table (MenuController ~2630) chiude TUTTO con status/stazioni=delivered; anche /pay singolo
  fa ready->delivered. Col refactor archivio=delivered&&paid -> ordine pagato ma mai preparato sparisce.
- Fix proposto (7094, attesa conferma): pay setta SOLO paid (+incasso/scontrino), NON avanza lavorazione;
  archivio=servito&&pagato; tavolo si libera a Servito non al pagamento (chiesto ok esplicito). Poi correzione
  mirata record A07 -> riportarlo a received (In cucina) mantenendo paid. Tocca server -> restart.
## In coda (msg 7092): popup pagamento -> svg conferma prenotazione + stampa/invio = scontrino digitale
- Digital receipt engine gia esiste: BillReceiptMapper.Map + PlainTextRenderer().RenderText (vedi BillDetailPage
  ReceiptText, w32). PayPrintAsync oggi fa solo window.print della pagina, non lo scontrino. SVG conferma da trovare.

## Task 7092 popup pagamento consegnato+deployato (7097)
- SVG check-circle verde #2E9E5B 52px sopra "Importo pagato", anim scale/opacity 320ms (reduced-motion ok).
- Stampa: elemento print-only .receipt-print-area con testo da RenderReceiptText(orders) (BillReceiptMapper+
  PlainTextRenderer w32), snapshot in PayPrimaryAsync post-pagamento (tavolo=_cassaTableOrders, singolo=_menuOrders
  by id); @media print visibility trick -> stampa solo scontrino. Invio ricevuta gia' corretto (server), intatto.
- Build 0 err, deploy CAT.
## ANCORA BLOCCATO: fix A07/refactor pagamento (attesa OK Stefano su comportamento tavolo, msg 7094/7096/7097)

## Fix A07/refactor pagamento (msg 7098 confermato) — FATTO
- Server MenuController: /pay singolo e pay-table NON forzano piu delivered. Nuova regola: pagamento
  ortogonale, status produzione INVARIATO; eccezione awaiting_payment->received (parte produzione).
  Commenti doc aggiornati. Build server 0 err, restart puntify-server (:8001 200, log puliti).
- Client GIA coerente (nessuna modifica app): bacheca fetcha per status senza filtro paid + filtro
  delivered||!paid; archivio fetcha solo delivered && paid. Ordine received+paid -> resta in bacheca con
  pill Pagato, non in archivio.
- Correzione record A07 (e1e804a7): UPDATE status delivered->received (stazioni/coperti NULL invariati) ->
  ricompare in "Da confermare" con Pagato (era mai confermato: stato onesto). paid=true mantenuto.
- Ora: pagare un conto/tavolo lo lascia in lavorazione fino a Servito; tavolo si libera al Servito.
