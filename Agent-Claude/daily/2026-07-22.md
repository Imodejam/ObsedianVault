# 2026-07-22

## Ripresa sessione (msg 7089 "prosegui")
Ieri notte interrotta per limite sessione durante il task 19. Verificato: nessuna modifica task 19 nel tree,
app allo stato task 18 (build 0 err, deployato). Ripreso con subagent fresco.

## Stato deploy CAT fino a task 18 (ieri, msg 7014-7086)
Rename->POS, pos-lock tutti i tab, Cassa (full-height/no-scroll/badge card+chip+giri/Asporto senza Tav/font 15px),
pagina Conto Stripe/Apple, Vetrina modal piatto orizzontale, Configurazione POS (Subito/Dopo x4 + Cassa pay-first),
refactor stati ordine (colonne lavorazione + pill pagamento, zero server), tooltip fix strutturale (position fixed
+ field-info.js), rimossa opzione pagamento Account cliente, separatori portata banda grigia.

## Task 19 in corso (msg 7087): Cassa vista focalizzata conto aperto
- CassaFocused (tab cassa + OrderRef/_cassaTableId/_cassaOrderId): via tab bar, back=history.back JS con
  fallback, titolo h1 "Tav. X - Y Pax - Stato" (stato = giro piu indietro per multi-giro), popup coperti al
  primo ingresso se non settati (flag _coversPromptedForRef anti-loop). Delegato a subagent fresco.

## Task 19 consegnato e deployato (msg 7087 -> 7091)
- CassaFocused = tab cassa && (OrderRef||_cassaTableId||_cassaOrderId). Tab bar in @if(!CassaFocused);
  BackToHome async: se focused history.back JS (torna da provenienza), altrimenti merchant/{id}/home.
- Titolo h1 CassaFocusedTitle(): dine_in "Tav X . Y Pax . Stato", altri "{modalita} . Stato" no Pax;
  .ord-search nascosta in focus. Stato multi-giro = collo di bottiglia (StatusRank min; gate coperti vince).
- Popup coperti: MaybePromptCoversOnEntry in ApplyOrderRefAsync ramo tavolo, flag _coversPromptedForRef anti-loop,
  OpenGuestsForConfirm se gate / OpenGuests se conto nuovo senza coperti. resx cassa_pax=Pax x10.
- Build 0 err, deploy CAT, report 7091.

## Bug A07 (msg 7093): pagamento forza delivered -> sparito da bacheca
- Dato reale: #A07 = e1e804a7..., status=delivered, paid=true, dine_in, covers NULL.
- Causa: pay-table (MenuController ~2630) chiude TUTTO con status/stazioni=delivered; anche /pay singolo
  fa ready->delivered. Col refactor archivio=delivered&&paid -> ordine pagato ma mai preparato sparisce.
- Fix proposto (7094, attesa conferma): pay setta SOLO paid (+incasso/scontrino), NON avanza lavorazione;
  archivio=servito&&pagato; tavolo si libera a Servito non al pagamento (chiesto ok esplicito). Poi correzione
  mirata record A07 -> riportarlo a received (In cucina) mantenendo paid. Tocca server -> restart.
## In coda (msg 7092): popup pagamento -> svg conferma prenotazione + stampa/invio = scontrino digitale
- Digital receipt engine gia esiste: BillReceiptMapper.Map + PlainTextRenderer().RenderText (vedi BillDetailPage
  ReceiptText, w32). PayPrintAsync oggi fa solo window.print della pagina, non lo scontrino. SVG conferma da trovare.

## Task 7092 popup pagamento consegnato+deployato (7097)
- SVG check-circle verde #2E9E5B 52px sopra "Importo pagato", anim scale/opacity 320ms (reduced-motion ok).
- Stampa: elemento print-only .receipt-print-area con testo da RenderReceiptText(orders) (BillReceiptMapper+
  PlainTextRenderer w32), snapshot in PayPrimaryAsync post-pagamento (tavolo=_cassaTableOrders, singolo=_menuOrders
  by id); @media print visibility trick -> stampa solo scontrino. Invio ricevuta gia' corretto (server), intatto.
- Build 0 err, deploy CAT.
## ANCORA BLOCCATO: fix A07/refactor pagamento (attesa OK Stefano su comportamento tavolo, msg 7094/7096/7097)

## Fix A07/refactor pagamento (msg 7098 confermato) — FATTO
- Server MenuController: /pay singolo e pay-table NON forzano piu delivered. Nuova regola: pagamento
  ortogonale, status produzione INVARIATO; eccezione awaiting_payment->received (parte produzione).
  Commenti doc aggiornati. Build server 0 err, restart puntify-server (:8001 200, log puliti).
- Client GIA coerente (nessuna modifica app): bacheca fetcha per status senza filtro paid + filtro
  delivered||!paid; archivio fetcha solo delivered && paid. Ordine received+paid -> resta in bacheca con
  pill Pagato, non in archivio.
- Correzione record A07 (e1e804a7): UPDATE status delivered->received (stazioni/coperti NULL invariati) ->
  ricompare in "Da confermare" con Pagato (era mai confermato: stato onesto). paid=true mantenuto.
- Ora: pagare un conto/tavolo lo lascia in lavorazione fino a Servito; tavolo si libera al Servito.

## A07 ancora sparito (7100/7101): filtro stale 5h
- Causa 2: A07 creato ieri 21:30 (16h fa) + ora pagato -> il filtro stale (>5h nascondi) aveva eccezione
  solo per NON pagati, quindi il pagato-non-servito veniva nascosto. Bug emerso col modello nuovo.
- Fix Dashboard.razor LoadOrders: aggiunto `|| o.Paid` all'eccezione stale (un pagato non-servito e' lavoro
  attivo; il delivered&&paid e' gia' tolto prima). Build 0 err, deploy CAT. Report 7102.

## Cassa POS restyle (stile Stripe) — CAT
- Pagina /merchant/{id}/POS/Cassa: distinzione pannelli + meno rosso a sinistra.
- Conto ora card sollevata: border #E3E8EE, box-shadow morbida, bianco su canvas grigio → si stacca dal pannello prodotti piatto.
- Rosso ridotto: .cassa-line-qty, .cassa-qbtn-add, .cassa-course-head.is-current → neutro near-black #1A1F36. Header/foot band #F6F8FA, testo secondario #697386.
- Rosso mantenuto SOLO su .cfg-btn-primary (Incassa/Invia in cucina) e badge stato/amber.
- File unico: Puntify.App/Pages/Merchant/Dashboard.razor (markup+CSS inline). is-current override vince su config.css (page-style dopo nel DOM).
- Build Release 0 errori, deploy-cat-app.sh → :8002 risponde 200. Screenshot headless non fattibile (pagina auth merchant). Serve hard-refresh (Ctrl+F5) lato Stefano per vedere il WASM aggiornato.

## Fix Cassa POS: tab Cassa presenta pagina vuota + "Nuovo ordine" ripristinato
- File: Puntify.App/Pages/Merchant/Dashboard.razor (unico).
- SwitchTab -> async Task; su tab "cassa" azzera stato focalizzato (StartNewCassaOrder(null)) e carica menu (EnsureCassaMenuAsync).
- ApplyOrderRefAsync: guardia sul ramo OrderRef vuoto/non valido -> se _tab=="cassa" e _cassaTableId/_cassaOrderId residui, StartNewCassaOrder(null)+EnsureCassaMenuAsync+StateHasChanged (copre back browser / nav diretta). Draft valido (stato gia null) NON azzerato.
- Aperture reali (OnTableClick / OpenOrderInCassa / deep-link /POS/Cassa/{id}) invariate: restano focalizzate.
- Build Release 0 errori, deploy-cat-app.sh :8002 HTTP 200. Serve hard-refresh (Ctrl+F5) lato Stefano.

## Fix "Diretto" wizard Nuovo ordine (Puntify.App CAT)
- File: Puntify.App/Pages/Merchant/Dashboard.razor, metodo NoSelectDirectAsync (~3134)
- Problema: cliccando "Diretto" impostava stato vendita diretta ma NON navigava in Cassa (chiudeva solo il modal) -> restando su Ordini non si vedeva nulla.
- Fix: dopo StartNewCassaOrder(null)+_cassaOrderMode="dine_in", aggiunta la stessa navigazione di NoConfirmAsync: _showNewOrder=false; WriteCassaRefToUrl(null); _tab="cassa"; StateHasChanged().
- Guardia reset ApplyOrderRefAsync (riga 2241: `if (_tab=="cassa" && (_cassaTableId is not null || _cassaOrderId is not null))`) NON scatta: entrambi null dopo StartNewCassaOrder(null) -> vendita diretta preservata. Inoltre WriteCassaRefToUrl(null) setta _appliedOrderRef=null quindi ApplyOrderRefAsync fa early-return (OrderRef==_appliedOrderRef).
- dine_in + _cassaTableLabel vuoto => header mostra cassa_direct_sale ("Vendita diretta"). Coerente.
- Build Release 0 errori. Deploy deploy-cat-app.sh su :8002 OK (HTTP 200). Nessuna modifica alla guardia necessaria.

## Guard coperti pagamenti dine_in (Puntify CAT)
- MenuController.cs: guard covers_required su /pay e /pay-table (eccezione giro-successivo tavolo).
- Build ok, restart puntify-server ok, A07 verificato (guard scatta).

## Cassa Puntify - Gate coperti (Dashboard.razor)
- OpenGuests: modale parte da 0 se coperti non definiti (_guestsInput = (_cassaCoversSet && _cassaCovers>=1) ? val : "0").
- GuestsConfirm ramo DRAFT: rimosso default v=1 indovinato; se 0 chiude + toast cassa_covers_required, _cassaCoversSet resta false (invio bloccato da CassaDineInNeedsCovers).
- Nuovo flag _coversPromptedForDraft (anti-loop draft nuovo, OrderRef null) resettato in StartNewCassaOrder.
- NoConfirmAsync ramo dine_in con tavolo: apre OpenGuests una volta all'ingresso in Cassa. Esclusi Diretto/takeaway/delivery.
- _cassaCovers resta default 1 (safe per CassaCoverTotal/pay-split); solo la MODALE mostra 0.
- Build Release 0 errori, deploy CAT :8002 HTTP 200.

## Fix reminder fuso orario (Puntify CAT)
- File: Puntify.Server/Services/BackgroundServices/BookingReminderService.cs
- Bug provato: prenotazione Pepto 22/07 20:30 -> DB start_at=2026-07-22 20:30:00+00 (wall-clock offset0). Reminder confrontava con UtcNow reale -> partiva alle 20:30 locali invece che 18:30.
- Fix: NowInZone(shop.Timezone) + StartLocal, filtro ±5m per-shop. Testo "2 ore" e' fisso (hoursLabel), problema era solo scheduling.
- Nota aperta: nessuna colonna reminder_sent_at -> rischio doppio invio se booking cade in 2 poll (preesistente, non introdotto).
- Deploy: build OK, restart puntify-server OK (200).

---
## Puntify Social — regia editoriale (2026-07-22)
Creata strategia + calendario 30gg (23/07-21/08) in /home/progetti/puntify-social/planning/.
- strategy.md: posizionamento (OS del negozio locale), 10 pilastri mappati ai topic, do/dont bright/rosso, mix 12 formati, cadenza 1/gg, KPI.
- calendar.json: 30 entry, 10 topic x3, nessun topic adiacente, bg=light, copy fresco ogni giorno, weekday-aware (lun=insights, ven/wknd=prenotazioni/code).
- README.md: runner legge entry per data e chiama skin/run_draft.py con override CLI.
Prossimo: il runner giornaliero consuma calendar.json.
