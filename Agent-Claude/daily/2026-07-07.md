# 2026-07-07

## Puntify — Architettura clienti segregata FASE 1
- Implementata su CAT/collaudo (NON prod, NON committato/deployato).
- Owner esercente: aggiunto shops.owner_account_id (nessun owner pulito in account_shops che mescola owner+clienti); backfill dal legame piu vecchio con ruolo esercente (2/3), 58/58 shop.
- customer_profiles (owner_type shop|account, owner_id, phone_norm, unique) RLS PII solo service_role. Verificato anon negato.
- CustomerProfileService (ResolveOwner/FindByPhone/Upsert) agganciato nel booking (UpsertCustomerAccountAsync). Toggle scope in ShopEdit + i18n 10 lingue. Build 0 errori.
- Follow-up FASE 2: refactor Clients.razor/ShopCustomersController, migrazione dati storici, tool voce Vapi, applicare migration in PROD.

## Nemi Voce FASE 2 — tool vocali prenotazioni + riconoscimento (collaudo)
- Implementati 6 tool Vapi (elenca_servizi, verifica_disponibilita, crea_prenotazione, trova_mie_prenotazioni, sposta_prenotazione, cancella_prenotazione) in nuovo NemiVoiceToolService + handler evento tool-calls in VapiWebhookController (risposta {results:[{toolCallId,result}]}, HTTP 200 sempre, errori parlabili).
- Prenotazione voce: CreateVoiceBookingAsync (nome+telefono obblig., email opz, GdprConsent verbale+nota, SourceChannel=voice, upsert profilo segregato) + RescheduleBookingAsync in BookingServiceImpl.
- Riconoscimento chiamante in assistant-request (customer.number → FindByPhoneAsync → riga "CHIAMANTE RICONOSCIUTO" nel prompt). Mini-informativa GDPR nel prompt vocale.
- Build Puntify.Server 0 errori. NON committato/deployato. Aperti: date relative gestite dall'LLM, testo GDPR hardcoded, test dal vivo con numero Vapi, no risorse/tavoli/asporto.

## 21:35 — UI Flotta (Devices/Monitors) FATTA + doc app Android ricevuta
- UI flotta: Screens.razor→pagina 2 tab (Monitors anteprima iframe + custom URL; Devices assegna monitor/kiosk/PIN/azioni remote/claim). FleetApiService. 76 chiavi ×10 lingue. Build ok, app riavviata (app-cat 200). Non committato.
- Doc app Android (Stefano): app it.puntify.appcat PRONTA, stampa USB ESC/POS confermata. Bridge window.PuntifyNative (printRaw base64). Creata wiki/projects/puntify-android-app + memoria.
- TODO lato web (io): generare ESC/POS comande cucina + biglietti totem e chiamare printRaw (completa stampa end-to-end).
