# 2026-07-23

## Task: noindex negozi demo (isfake=true) su Vetrina CAT

### Obiettivo
I negozi con isfake=true non devono essere indicizzati da Google: meta robots noindex,nofollow sulle loro pagine pubbliche (pagina negozio + pagina prenotazione /book). Sitemap gia li escludeva (non toccata).

### Cosa fatto
- Modifica UNICA in `Puntify.Vetrina/App.razor`:
  - inject `SupabaseStatsService`
  - `@code`: `OnInitializedAsync` chiama `ShopIsFakeForPublicRoute(path)` che parsa la route (togliendo prefisso lingua e sotto-rotta nota book/menu/risorse), risolve lo shop via `GetShopBySlug(city, slug)` e ritorna `shop.IsFake`
  - meta robots: `noindex,nofollow` se `__isStaging || _shopRouteIsFake`, altrimenti `index,follow,...`
- App.razor scelto come fonte UNICA perche Book.razor e interattivo (`prerender:false`): il suo `<HeadContent>` non raggiunge l'HeadOutlet statico -> il robots per-pagina non renderizzava mai (bug pre-esistente: anche title/canonical/og della book erano inerti). L'head statico dell'host e l'unico punto affidabile.
- Approccio iniziale (campo `MerchantPublicDto.IsFake` + controller + edit per-pagina in Book/NegozioDetail) SCARTATO e revertato: la HeadContent inerte lo rendeva inutile per la book, e su NegozioDetail creava doppio/conflitto di meta. Meno codice, una sola fonte.

### Verifica reale (prova)
Host `www.puntify.it` (staging=false, comportamento prod):
- FAKE negozio frutta-fresca -> noindex (count 1, no doppioni)
- FAKE book -> noindex; FAKE scarpe-stile-eur / barbiere-testaccio -> noindex
- NON-FAKE lido-del-sole negozio+book -> index,follow; madrigalas-madrid -> index,follow
Su CAT reale (127.0.0.1, staging=true): tutto noindex (atteso, staging globale App.razor).

### Gotcha
- dotnet watch: dopo add+revert del campo DTO IsFake il server dava `MissingMethodException` (hot-reload sporco) -> restart pulito `puntify-server.service`.
- Restart servizi uno alla volta; watch a volte resta in "deactivating" se il restart collide con un hot-reload -> ripetere il restart.

### Stato lasciato
Solo CAT/collaudo. Niente prod, niente sitemap, niente commit. Servizi puntify-vetrina e puntify-server up e sani.

## 09:50 — KPI API prod (nuova richiesta)
Stefano: vuole API con API key per consultare KPI prod. Foto = GA4 prop 524577455 + SA search-console-reader-claude ora Viewer su GA4.
Proposto: GET /api/kpi su puntify-server, X-API-Key da /opt/ops/.env, read-only, JSON aggregato da Postgres prod + GA4 Data API (via SA).
Attendo: conferma set KPI + ok flusso CAT->prod.

## 10:12 — ClawRoom rename ruolo
Massimo: Role "Sales Manager" -> "Direttore Commerciale". Editato data/team.json (sorgente) + bin/publish/data/team.json (runtime, WorkingDirectory del service). clawroom.service riavviato, active. Team ClawRoom: Alfred(Chief of Staff), Carlo(Full Stack), Luca(Social), Massimo(Direttore Commerciale), Lidia(Design System Lead).

## 10:15 — Puntify CAT: endpoint GET /api/kpi
Implementato su collaudo (DB puntify_cat, 127.0.0.1:8001). File: Services/Analytics/KpiService.cs (aggregazione globale in-memory sul periodo, COUNT via CountAsync), Controllers/KpiController.cs (thin, days clamp 1..365 def 30), DI Scoped in Program.cs. JSON snake_case via [JsonPropertyName] (server senza naming policy globale). Auth gia' data da ApiKeyAuthMiddleware (X-API-Key). Build OK, restart solo puntify-server. Test 200/200/401 verificati. Nota: questo e' il gemello CAT dell'API KPI prod scoped stamattina (pending Stefano dati prod).

## 10:35 — Analisi GA4 per Stefano
Pull 28gg + insight/raccomandazioni inviati. Report completo in wiki/projects/puntify-ga4-analisi-20260723.md. Priorita proposta: E) abilitare conversion tracking prima di ottimizzare. In parallelo subagent a205c050 sta aggiungendo sezione ga4 a /api/kpi su CAT.

## GA4 in /api/kpi (Puntify.Server CAT)
- Nuovo `Services/Analytics/Ga4Service.cs`: legge GA4 Data API v1beta via HttpClient, bearer da SA JSON (Google.Apis.Auth 1.68.0, scope analytics.readonly). Property 524577455, key /home/claudebot/.secrets/gsc-key.json.
- KpiService inietta Ga4Service e popola `snap.Ga4`; KpiSnapshot ha `[JsonPropertyName("ga4")]`.
- Program.cs: registrato `AddScoped<Ga4Service>()`. appsettings.Development.json: sezione `GA4`.
- Sezione ga4: totals/channels/top_landing_pages/top_pages/devices/top_countries. Tutto in try/catch → available:false su errore, KPI Postgres invariati.
- Build ok, restart puntify-server.service, curl reale: 30gg 316 sessioni, 7gg 122 sessioni; 401 senza X-API-Key.

## 11:42 — Commit+push master (richiesta "tutto")
Stefano: "committa e pusha tutto". Working tree aveva 84 file (WIP dal 19/07, non solo KPI/GA4). Confermato A (unico commit). git add -A + commit fd0f96c (146 file, +15528/-1159) + push origin master. appsettings.Development.json gitignored -> chiavi CAT non committate. Avvisato: 9 migrazioni SQL nel repo ma NON ancora applicate al DB.

## 13:11 — Fix UI bug booking pubblico (CAT)
File: Puntify.Vetrina/Components/Booking/QuickTableBooking.razor, metodo Submit() (~riga 569).
Bug: su _result.Error=="no_table_available" veniva impostato _error ma NON _step (restava =2 Conferma) -> banner errore + card "Conferma prenotazione" mostrati insieme (contraddittorio).
Fix minimale nel ramo !_result.Success: dopo lo switch dell'errore, if(Error=="no_table_available"){ _step=0; _selectedTime=""; _=PersistAsync(); }. Nomi reali: step=_step, orario=_selectedTime, persist=PersistAsync() (chiamata fire-and-forget "_ = PersistAsync();" come nel resto del file).
Gemello takeaway QuickTakeawayBooking.razor (slot_full ~riga 225): NON e' step-based, gia' resetta _selectedSlotAt=null + LoadSlots() -> sezione conferma gated su _selectedSlotAt.HasValue si nasconde. Gia' corretto, NON toccato.
Build: dotnet build Puntify.Vetrina -> Build succeeded, 0 Error (19 warning preesistenti). Restart SOLO puntify-vetrina.service -> active (running), HTTP 200 su 127.0.0.1:8003/it. PROD non toccato.

## PhotoPickerModal — loader AI + frasi rotanti
- File: Puntify.App/Pages/Merchant/Menu/PhotoPickerModal.razor (modale "Scegli foto", tab Genera con AI)
- Loader: sostituito `spinner--inline` (inesistente → base .spinner rossa invisibile su bottone rosso) con `spinner--btn` (bianco, ben visibile). CSS nuovo in wwwroot/css/config.css (globale, scoped .razor.css non linkato in WASM).
- Frasi a rotazione (System.Timers.Timer 2,5s, InvokeAsync+StateHasChanged, IDisposable/Dispose): "Sto immaginando il piatto…" ecc. Chiavi photopicker_gen_status_1..4 in AppResource.resx + tutte le 9 lingue.
- Build 0 errori; restart SOLO puntify-app.service → active, :8002 → 200. Serve hard-refresh browser per WASM.

## PhotoPicker "Suggerite" — browse libreria condivisa con tag filter
Feature Stefano: nel popup "Scegli foto" tab Suggerite, oltre ai best-match, sfogliare TUTTI i cataloghi condivisi filtrando per tag.

Fatto (CAT only):
- DB: colonna `tags text[]` su puntify.product_catalog + GIN index; backfill automatico tag da nome/ingredienti (regole keyword). Nuove RPC: browse_catalog_photos, catalog_photo_tags; match_catalog_photos ora ritorna tags. Migration docs/DB Migrations/2026-07-23_catalog_photo_tags.sql, applicata a puntify_cat.
- Backend: MenuController → GET /api/menu/catalog/photos (browse tag+q+paging) e /api/menu/catalog/photo-tags; CatalogPhotoDto +Tags.
- Client: MenuApiService +CatalogPhotosBrowseAsync, +CatalogPhotoTagsAsync, DTO +Tags.
- UI: PhotoPickerModal → best-match in cima, poi chip filtro tag + ricerca + grid sfogliabile con "Carica altro"; tag chips sotto ogni card.
- i18n: 15 chiavi photopicker_* su tutte e 10 le locale App.
- Build App+Server OK. Endpoint testati e2e (restart pulito puntify-server per watch token-corruption).

Tag taxonomy da rivedere con Stefano: pizza/calzone/pasta/arancino/focaccia/fritti/contorno/bevanda/birra (tarata su dati pizzeria attuali; solo 4 voci con foto oggi).

## Puntify CAT — 3 richieste Telegram (orchestrazione subagent)

### 1. POS/Configurazione — FATTO
- Toggle ON/OFF (.cfg-toggle) per vendita diretta/sul posto/asporto/delivery; timing Subito/Dopo solo se ON.
- DB: enable_* su shop_pos_settings (migration 2026-07-23_pos_service_enable.sql, CAT). Model +4 bool.
- Opzione A: asporto = sorgente unica → sync booking_modes. Tab Finestre-asporto spostato sotto POS (bk-tabs), rimosso da BookingHub, redirect legacy URL.
- Estensione: gate picker "Nuovo ordine" (Dashboard + MerchantPos legacy) su enable_*; empty state se tutti OFF. Scan service-selection = loyalty, non toccato.
- Res: pos_cfg_enable, neworder_no_service (10 lingue). Build 0 err.
- Da decidere: MerchantPos.razor /neworder sembra morta → eliminare?

### 2. Scan.razor — FATTO
- Migrata a design system cfg-page (come pagine servizi). Build 0 err. Preservate classi scanner/camera.

### 3. PhotoPickerModal (Suggerite) — FATTO
- Tab a 3 blocchi: Dal tuo menu / Suggerite per il piatto / Sfoglia la libreria (intera product_catalog).
- DB: tags text[] + backfill categorie (pizza/pasta/bevanda/...); RPC match+browse+tags. Migration 2026-07-23_catalog_photo_tags.sql (CAT). Backend endpoint browse+tags. Client browse+tags. UI chip filtro + ricerca debounce + load-more + chip su card.
- Res: 15 photopicker_* (10 lingue). Build 0 err App+Server. Endpoint testati 200 su CAT (restart puntify-server per token-corruption watch).
- Nota: solo 4/70 voci catalogo hanno foto oggi; tassonomia tag tarata su dataset pizzeria attuale.

### Watchdog robustness (domanda Stefano)
- I run della sessione muoiono al restart servizio. Proposto: (1) job fuori processo (systemd-run) + (2) ledger di ripresa. In attesa OK.

### Prossimi passi
- Attendo verifica visiva Stefano (hard-refresh WASM). Eventuale eliminazione MerchantPos.razro. Deploy prod = manuale, NON fatto.

### POS/Configurazione redesign (feedback Stefano) — FATTO
- Split in 2 card: "Modalità" (4 servizi, toggle VERDE iOS .switch/.slider #34c759 come altre pagine, rimosso .cfg-toggle rosso) + "Quando incassare" (Subito/Dopo solo per modalità attive, hint se nessuna attiva).
- Res: pos_cfg_modes_title (Modalità), pos_cfg_no_active_mode — 10 lingue. Build 0 err. Solo CAT.

### POS/Risorse vista Sala — FATTO (build ok, CAT)
- Griglia card compatte per zona + chair marks; day-strip 7gg + meal-tabs Pranzo/Cena (fisse) + ricerca; prenotato blu #635BFF (nome+ora+coperti), occupato rosso (oggi, ordine dine_in aperto), libero bianco. Gestione preservata (click=edit, add). 10 chiavi ×10 lingue. Verifica visiva Stefano pending.

### Fix Cassa Coperti — FATTO (in attesa A/B)
- Header non fabbrica più "1 pax" placeholder; editor prefilla coperti definiti; single source of truth. Scelta prodotto A(da definire)/B(default 1) da Stefano.

### Gestione portate (course firing) — DESIGN CONFERMATO, mapping in corso
- 1ª portata auto-mandata + bar subito, resto held; cameriere manda manualmente; lock su portata in preparazione; KDS mostra hold; no auto-avanzamento. Feature grossa DB+server+Cassa+KDS. Explore mapping flusso conferma/KDS/stati avviato.

### Correzione Risorse + nuove clausole ordini
- Risorse: avevo modificato /merchant/shop/{id}/risorse (RisorseTab, gestione) = pagina SBAGLIATA. Ripristinata via git checkout HEAD. Target giusto = tab Risorse dentro POS (Dashboard _tab==risorse, planimetria+griglia). In attesa scelta A/B (griglia sostituisce vs toggle mappa+griglia).
- Fix back Cassa ingresso diretto -> Ordini (FATTO).
- Gate coperti su Incassa (R4) FATTO.
- Clausole ordini in docs/order-rules.md (R1-R5). R5 pagato-immutabile e R1 lock portata da implementare col firing.
- Course-firing: backend agent in corso (course_status jsonb, fire-course endpoint, kitchen advance course-aware, lock edit).
- Ambiguità "B": chiesto se coperti o risorse.

### Batch finale POS (tutto FATTO, build ok, CAT)
- Course-firing FRONTEND: Cassa manda-portata + badge stato + lock R1(portata mandata)/R5(pagato) + KDS hold. Server R5 order_paid guard.
- Stray "}" Risorse rimossa.
- Mappa: tavoli 64x48 compatti, colori stato, toggle Mappa/Griglia top-left sulla riga sala.
- Griglia: sedie 4 lati (2T/2B per 4 posti), pari=simmetrico.
- Search unica "cerca tavolo o prenotazione" top-right, tolta doppia in griglia, su tutte le tab POS.
- Coperti popup all'ingresso ordine singolo; Manda-portata per ordini singoli.
- Menu M1: MenuController.GetActive -> attivo/prossimo/primo (sempre un menu). No schema.
- Server CAT riavviato (systemd puntify-server, dotnet watch) per caricare R5+M1.
- App (WASM) via puntify-app watch -> Stefano Ctrl+F5.
- Aperti: scelta Coperti A/B (tengo A). Da rivedere: regola sedie 4 posti (2T/2B), densità mappa (64x48), MerchantPos.razor pagina morta.

## POS Risorse toolbar restructure (COLLAUDO)
- File: Puntify.App/Pages/Merchant/Dashboard.razor + wwwroot/css/booking.css
- Estratta `.rz-topbar` condivisa: `.rz-viewtoggle` (Mappa|Griglia) a sinistra, `.rz-daystrip-row` (giorni + Oggi), `.rz-meal-tabs` (Pranzo/Cena). Shown quando `_tables.Count > 0`.
- Rimosso toggle+day-strip+meal-tabs da Griglia (`.rz-toolbar`/`.rz-toolbar-row2` eliminati); resta `.rz-body` + zone grid.
- Rimosso toggle da Mappa (`.pl-map-topbar` eliminato); resta solo `.pl-map-filter` overlay top-right (selettore sala/piano).
- CSS: aggiunto `.rz-topbar`; rimosso `.pl-map-topbar`/`.pl-map-toggle`/`.rz-toolbar`/`.rz-toolbar-row2`; `.pl-map-filter` ora absolute top-right.
- Day-strip guida colorazione prenotato/occupato in mappa+griglia (handler chiamano LoadRisorseBookings). Build: 0 errori.

### Watchdog run durevoli — FATTO+testato
- /home/claudebot/durable-jobs/: durable-run.sh (sudo systemd-run, cgroup separato → sopravvive restart claude-rc.service), durable-status.sh (riconcilia), durable-logs.sh, README.
- Hook SessionStart in ~/.claude/settings.json → durable-status.sh --startup (ripresa automatica).
- Test: job sleep in /system.slice/durable-testjob-*.service, exit 0, unit auto-collected. Meccanismo provato (cgroup separato = non killato dal watchdog).
- Uso futuro: build/deploy/migrazioni lunghe + claude -p headless durevole.

### Completeness audit (richiesta Stefano) — inviato
- Tutte le richieste della sessione risultano FATTE e compilano. Elenco inviato su Telegram.
- Aperti solo: coperti A/B (tengo A), MerchantPos /neworder dead page (eliminare?), tassonomia tag foto.

## Persistenza draft POS Cassa (localStorage)
Richiesta Stefano: aggiungendo prodotti, un reload non deve perderli. Draft non inviato (_cassaLines + _cassaCurrentCourse) ora persistito.
- Nuovo wwwroot/js/cassa-draft.js (save/load/clear), incluso in index.html (?v=__buildVersion).
- Chiave puntify.cassaDraft.{ShopId}.{tableId|orderId|"new"} per isolamento contesto.
- Save su ogni mutazione; restore in ApplyOrderRefAsync + ingresso diretto; clear su invio/incasso/annullo/abbandono/nuovo ordine.
- Guardie: staleness 12h, no clobber (restore solo se righe vuote), no restore su pagato.
- Build verde. Doc: puntify/docs/Requests/20260723.6.md.
Prossimi passi: verifica manuale in CAT (reload durante costruzione conto tavolo/nuovo ordine; dopo invio nessun draft duplicato).
