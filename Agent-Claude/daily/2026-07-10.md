
## 2026-07-10 mattina — richieste Stefano
- 5753 PRESENTAZIONE Nemi: prompt VapiWebhookController blocco "=== PRESENTAZIONE (OBBLIGATORIA) ===" → dichiara sempre assistente DIGITALE + chiamata TRASCRITTA, in lingua del chiamante. Verificato payload. Restart server.
- 5755/5756 CORS/pagine vuote: NON bug config (CORS ok, preflight 204). Era il server in restart (miei riavvii ripetuti). Vedi mistakes.md 2026-07-10. Accorpare i restart.
- 5751/5754 chiamante nella LISTA tab Chiamate: ridisegnato CSS (.nemiuse-call-cust order:-1 → titolo riga in alto; nome grassetto scuro var(--cfg-text), numero piccolo grigio; niente blu default, evidenziato+underline su hover, resta link scheda). config.css. Restart solo app (server intatto → niente finestra CORS).
- Pending: 5751 conferma estetica da Stefano.

- 5758 lista Chiamate TABELLARE (Stripe, colonne fisse) + 5759 esiti rinominati leggibili — DELEGATO subagent a16794cc. Tabella <table> data-l responsive (riuso chiavi nemicall_f_*); helper endedReason→etichetta amichevole (merchant i18n tutte lingue nemicall_outcome_*; admin IT) su lista+dettaglio merchant e admin. Un solo restart a fine (evitare finestra CORS).

## 5758+5759 FATTO (subagent a16794cc)
Lista Chiamate merchant → <table class=nemicall-table> Stripe con colonne fisse + responsive data-l (::before etichette <640px); rimosse classi card .nemiuse-call* inutilizzate (tenuto .nemiuse-call-dir badge). Helper Puntify.App/NemiCallOutcome.cs (Classify/Localized(merchant i18n)/Italian(admin)/Prettify fallback) usato in lista+dettaglio merchant e lista+modale admin. i18n 10 chiavi nemicall_outcome_* ×10 lingue. Solo app riavviata (server intatto, no finestra CORS). Build 0 errori.

## 5762 badge appuntamenti + 5763 nome rinominato in lista clienti — FATTO (io)
5762: ClientDetail.razor tab "appuntamenti" badge .cdt-tab-badge con UpcomingAppointments = _bookings.Count(status confirmed/pending && StartAt.ToLocalTime()>=now). CSS config.css.
5763: la lista /clients (Clients.razor) mostrava account.DisplayName globale ignorando la rinomina. FIX: nuovo endpoint GET /api/shop/{shopId}/customers/profiles (ShopCustomersController, ResolveOwnerAsync scope + customer_profiles account_id/display_name not null); BookingApiService.GetCustomerProfilesAsync; Clients.razor override c.DisplayName col display_name profilo dopo il load. Verificato DB: profili rinominati presenti (b8f0084e→Liliia, 890d2531→Stefano). Restart server+app (batch), endpoint 401, CORS 204, build ok.

## [routine 9:00] Stato traduzioni Vetrina + menu
VETRINA SharedResource.*.resx: base(it)=5377, en=5241, uk=5241; es/fr/pt/zh/ar/hi/bn=3750 (tutte identiche → ~1627 chiavi mancanti per lingua, fallback IT). 7 lingue secondarie indietro, da riallineare.
MENU realtime: BLOCCATO. Chiave Anthropic verificata OGGI → 400 "credit balance too low" (già nei log 2026-07-08, EnsureMenuTranslatedAsync pepto-roma lang=uk fallita). Decisione pendente: A ricaricare credito Anthropic / B usare key LLM del merchant. Report inviato a Stefano (msg 5765).

## 5766 popup piatto più largo desktop — FATTO (io)
DishEditor.razor modale +classe .dish-editor-modal; config.css @media(min-width:1024px) .dish-editor-modal.cfg-modal--wide{max-width:1080px} (specificità doppia classe, non tocca booking.css né cfg-modal--wide condiviso). Solo app riavviata, build ok.

## 5767 popup Scegli foto: tab "Da URL" + rimuovi URL esterno + tasto accanto alla foto — DELEGATO subagent ab601d9b
PhotoPickerModal +Tab.Url (input url + "Usa" → OnPicked(url,"url")); DishEditor rimuove .dish-photo-url esterno + bottone Scegli foto accanto al thumb; MenuEditor da verificare; i18n 2 chiavi ×10 lingue. Solo app restart.

## 5767 photo picker URL tab — FATTO (subagent ab601d9b)
PhotoPickerModal Tab.Url (input url + "Usa" → OnPicked(url,"url")); DishEditor rimosso .dish-photo-url esterno, bottone Scegli foto accanto al thumb; MenuEditor sezioni già senza campo url esterno (coperte); i18n disheditor_photo_tab_url/url_use ×10 lingue; config.css override .dish-photo-row (booking.css intatto). App restart, 0 errori.

## 5769 Part A — link "Vai all'agenda" mail merchant RIPARATO (io)
PublicBookingController:820 agendaUrl: /merchant/shop/{id}/booking/agenda → /merchant/shop/{id}/booking (route /agenda inesistente). AppUrl per-ambiente (collaudo=app-cat, prod=app.puntify.it già in appsettings). Solo occorrenza. Server restart, CORS ok.

## 5769 Part B — pagina client "Appuntamenti" (menu Altro) con cancella/sposta — DELEGATO subagent ad7c808e
Nuova Pages/Client/Appointments.razor; endpoint server client /api/client bookings (auth utente, normalizzato tz via server); cancella (conferma)/sposta via token pubblico o endpoint client; voce nel menu Altro; i18n. In corso.

## 5771 — link Menu+Prenotazione su pagina client /shop/{id} (se presenti) — ACCODATO dopo Part B
ShopDetail.razor client: aggiungere link a menu (se ha menu) e prenotazione (se attiva), gated su feature. Regola client-shop-mirrors-public. Da fare dopo subagent ad7c808e (stessa area).

## 5771/5774 — pill Menu/Prenota NON compaiono su /shop/{id} client (BUG, non feature mancante)
Screenshot Stefano (pepto-roma): solo Chiama+Mappa, niente Menu/Prenota. DB+PostgREST: enabled_features=127 (bit16 menu ON), booking_enabled=true, booking_modes=7 (3 modi) → HasMenu()+showBooking dovrebbero essere true. ShopDetail.razor:125-129: showMenu=HasMenu()&&slug; showBooking=BookingEnabled&&GetBookingModesCount()>0&&slug. GetShopById = .From<Shop>().Single() (select *). IPOTESI principale: _shop.Slug vuoto sul client (Chiama/Mappa non usano slug → visibili; Menu/Prenota lo richiedono → nascosti), oppure feature int non deserializzate. Da diagnosticare a runtime (temp Console.WriteLine slug/EnabledFeatures/BookingModes + verifica) DOPO Part B (evitare conflitti area client). Accodato.

## Puntify — pagina cliente "Appuntamenti"
Nuova feature area cliente (app WASM :8002).
- SERVER: `Puntify.Server/Controllers/ClientBookingsController.cs` — `api/client/bookings` auth Bearer JWT Supabase (account.id == sub == bookings.customer_id, anti-IDOR).
  - GET: appuntamenti in corso (confirmed/pending, end_at>=now) su tutti i negozi, DTO normalizzato (shop/servizio/operatore/orari/deadline/status).
  - DELETE {id}: verifica ownership + deadline CancelBeforeHours → IBookingService.CancelBookingAsync (invia email).
  - PATCH {id} {NewStartAt}: verifica ownership → IBookingService.RescheduleBookingAsync (valida min/max/slot).
- SHARED: `Punto.Shared/DTOs/BookingDtos.cs` — ClientBookingDto + ClientRescheduleRequest.
- APP: `Pages/Client/Appointments.razor` (route `/appointments`); metodi in `Services/BookingApiService.cs` (GetMyBookings/CancelMyBooking/RescheduleMyBooking/GetPublicAvailability).
  - Sposta = date picker + slot da /api/public/merchants/{slug}/availability; Annulla con popup conferma (regola nota).
  - Ora corretta via endpoint server + `.ToLocalTime()` (come ClientDetail/agenda merchant); slot mostrati wall-clock (offset zero, no ToLocalTime).
- MENU: voce "Appuntamenti" (icona calendario) nel burger "Altro" di `Components/ClientBottomNav.razor`, accanto a Transazioni.
- i18n: 20 chiavi `appointments_*` in tutte le 10 lingue (it/en/es/fr/de/nl/pl/ro/ru/uk).
- CSS: `.appt-*` in `wwwroot/css/app.css`.
- Esito: build server+app 0 errori; restart server→app; endpoint registrati (401/403, non 404).

## 5769 Part B — pagina cliente "Appuntamenti" FATTO (subagent ad7c808e)
Server ClientBookingsController (GET/DELETE/PATCH /api/client/bookings, JWT auth, ownership anti-IDOR, tz normalizzato server, riusa IBookingService Cancel/Reschedule); App Appointments.razor (/appointments, lista in corso, Sposta+Annulla con conferma); voce "Appuntamenti" nel drawer ClientBottomNav accanto a Transazioni; BookingDtos ClientBookingDto/ClientRescheduleRequest; i18n 20 chiavi ×10 lingue. Build 0 errori, endpoint 401.

## 5771/5774 — debug pill Menu/Prenota: aggiunto Console.WriteLine [shopdbg] in ShopDetail.razor:600 (slug/feat/hasMenu/bookEnabled/modes). App restart. Chiesto a Stefano di incollare la riga console per diagnosi. Da rimuovere dopo fix.

## 5771/5774 pill — DIAGNOSI: console [shopdbg] slug=pepto-roma feat=127 hasMenu=True bookEnabled=True modes=7 modesCount=3 → valori TUTTI corretti → showMenu/showBooking=true → pill devono renderizzare; .shop-embed-actions=display:flex (nessun CSS li nasconde). Conclusione: screenshot 5774 era WASM cache vecchia. Chiesto a Stefano conferma se ora li vede; se sì rimuovo debug. (404 "Failed to load resource" = risorsa separata, prob. immagine.)
## 5778 — cliente "eliminare" appuntamento: la pagina Appuntamenti ha già Annulla (con conferma) → sparisce dagli in corso. Spiegato che non si fa hard-delete (audit + preavviso). Chiesto se intende altro punto.

## 5782 — Appuntamenti mancava nella SIDEBAR DESKTOP (subagent l'aveva messo solo nel burger drawer mobile). FIX: aggiunto nav-item nav-secondary "appointments" in ClientBottomNav nav-items-top accanto a Transazioni.
## 5771 pill — screenshot conferma NON compaiono (non cache). Console dice hasMenu/bookEnabled True → contraddizione. Aggiunto DEBUG VISIBILE (div rosso DBG menu/book/queue/slug/feat/hasM/bE/mc) sopra il blocco pill in ShopDetail per leggere i valori a render-time. Chiesto screenshot a Stefano. App restart.

## 5771/5774/5781/5784 pill Menu/Prenota — RISOLTO
DBG visibile (screenshot Stefano): menu=True book=True queue=False slug=pepto-roma feat=127 hasM=True bE=True mc=3 → @if true, pill renderizzati ma INVISIBILI. CAUSA: regola CSS residua app.css:3272 `.shop-action-pill-red { display:none; }` (la regola a 7059 dava colore ma non resettava display → display:none vinceva). NON era il "chiuso". FIX: rimosso il display:none (sostituito con commento). Rimossi entrambi i debug (console [shopdbg] + div rosso). App restart, 0 errori. Pill ora visibili.

## 5786 pill Menu/Prenota affianco a Chiama/Mappa — FATTO
ShopDetail.razor: spostati pill queue/menu/booking dentro .shop-action-row (prima del points-badge); rimosso vecchio blocco .shop-embed-actions duplicato; app.css .shop-action-row flex-wrap nowrap→wrap, justify-content flex-start.
## 5787 colore merchant su /shop — FATTO
ShopDetail: container root style +--accent-red:@Accent; Accent=ShopExtensions.SafeHexColor(_shop?.PrimaryColor) (fallback #B80000). Override cascade ricolora tutti gli elementi --accent-red della pagina col colore del negozio. pepto-roma primary_color=#2d3319 → pulsanti verde oliva. App restart, 0 errori.

## 5789 uniformare tasti /shop + colore scontrino — FATTO
app.css: .shop-action-pill-gray e .shop-action-pill-red → stile OUTLINE (background transparent, color+border 1.5px var(--accent-red); pill-red flex 0 0 auto). Tutti i 4 tasti uniformi outline nel colore merchant. .receipt-scan-btn e .receipt-loyalty-badge: background #E8453C → var(--accent-red) (solo queste 2 occorrenze, nessun altro uso di #E8453C). Su pepto-roma tutto verde oliva #2d3319. Solo app restart, 0 errori.

## 5791 — /shop: sezioni "Qualcosa su di noi"+"I nostri orari" + stato orari cliccabile — FATTO
ShopDetail.razor: (1) blocco orari top ora è button .shop-hours-clickable che mostra solo badge Aperto/Chiuso → @onclick ScrollToHours (Navigation fragment #shop-orari). (2) dopo Storico Transazioni: sezione "Qualcosa su di noi" (_shop.Description, se presente) + "I nostri orari" (id=shop-orari, OpeningHours split per virgola in righe). CSS .shop-info-card/-text/.shop-hours-row/.shop-hours-clickable in app.css. i18n shop_about_title/shop_hours_section_title ×10 lingue. Solo app restart, 0 errori. (OpeningHours è testo unico; eventuale formato giorno-per-giorno da valutare.)

## 5793 — ombra rossa badge punti → colore merchant. app.css:3321 points-circle-badge box-shadow rgba(184,0,0,.38) → color-mix(in srgb, var(--accent-red) 38%, transparent). Su /shop diventa alone verde, default rosso altrove. (shutter-btn 5880/5905 = pagina fotocamera, non toccato.) App restart 0 errori.

## 5795 — /notifications mostra notifiche admin invece del cliente impersonato — FIX
Notifications.razor:114 usava Supabase.CurrentUser!.Id (utente auth=admin). FIX: iniettato ImpersonationState; userId = Impersonation.IsActive && AccountId ? impId : CurrentUser.Id (minimal-risk: cambia solo in impersonazione). Altre pagine client usano GetCurrentAccount() che già gestisce impersonazione. Osservato in PROD → DA PORTARE IN PROD col deploy. App restart 0 errori.

## 5797 — riconciliazione chiamate Nemi (recupero dati da Vapi se manca end-of-call-report) — PIANIFICATO, BLOCCATO su Vapi private key
Oggi nemi_calls creato solo a end-of-call-report → se non arriva, nessuna traccia. Piano: (1) registrare riga pending all'assistant-request (call.id già disponibile); (2) BackgroundService che dopo ~20min (+ passata giornaliera) chiama GET https://api.vapi.ai/call/{id} per recuperare costo/durata/trascrizione/esito, completa record + scala minuti (idempotente col report tardivo, no doppio addebito); restructure end-of-call-report da skip-if-exists a update. Pattern hosted service: es. EmailProcessorService/NotificationProcessorService.
BLOCCO: serve Vapi PRIVATE API KEY (org) — NON in config (solo WebhookSecret); il controller nota "private key che per ora non usiamo". Chiesta a Stefano (msg 5798) + confermata tempistica. Posso costruire pending+servizio e agganciare la key dopo.

## 5799 "Ok procedi" — riconciliazione chiamate Nemi: DELEGATO subagent ae5a8eea
Design: tabella pending SEPARATA nemi_pending_calls (non tocca ciclo nemi_calls); assistant-request(serve path) inserisce pending (fail-safe); end-of-call-report cancella pending; BackgroundService NemiCallReconciliationService (ogni ~5min) → pending >20min → GET api.vapi.ai/call/{id} (Vapi:ApiKey) → build nemi_calls + scala secondi UNA volta (idempotente vapi_call_id UNIQUE) → cancella pending; rinuncia dopo attempts/24h. Key mancante = warning+skip (inerte). Migration + reload PostgREST + file prod. In attesa Vapi:ApiKey da Stefano per test reale.

## 5797/5799 riconciliazione chiamate Nemi — FATTO+VERIFICATO (subagent ae5a8eea)
Tabella nemi_pending_calls + modello NemiPendingCall; VapiWebhookController RegisterPendingCallSafe(assistant-request serve path)/DeletePendingCallSafe(end-of-call-report) fail-safe; INemiCallFinalizer condiviso (idempotente, scala una volta); VapiApiClient GET api.vapi.ai/call/{id} Bearer Vapi:ApiKey (NotConfigured se vuota); NemiCallReconciliationService BackgroundService (5min loop, soglia 20min ReconcileAfterMinutes, maxAttempts 12, rinuncia dopo 24h) registrato Program.cs. Migration docs/DB Migrations/2026-07-10_nemi_pending_calls.sql (reload PostgREST fatto).
VERIFICATO runtime: assistant-request→200+config+pending; end-of-call-report→nemi_calls+scala esatto +77s+cancella pending; report DUP→no doppio addebito (saldo invariato); no-key→"in attesa" senza danni. Cleanup + saldo ripristinato 803.
DA FARE: Vapi:ApiKey (org) da Stefano per attivare (msg 5801); PROD: creare nemi_pending_calls + reload PostgREST + codice.

## 5802 — riconciliazione: MAI rinunciare, riprovare almeno 1/giorno (correzione). Delegato resume subagent ae5a8eea: rimuovere rinuncia (attempts/24h), +colonna last_attempt_at, cadenza a 2 fasi (frequente prima soglia, poi giornaliera per sempre), 404 non è più rinuncia. Pending si cancella solo se finalizzata o già in nemi_calls. In corso.

## 5802 correzione riconciliazione — FATTO+VERIFICATO (subagent ae5a8eea resume)
NemiCallReconciliationService: rimossa ogni rinuncia (no GiveUp/Outcome.GaveUp); pending cancellata SOLO se finalizzata o già in nemi_calls. +colonna last_attempt_at (migration idempotente + reload PostgREST + modello). Cadenza 2 fasi: created_at<now-20min AND (last_attempt_at null OR (attempts<12 AND last>ciclo) OR (attempts>=12 AND last<now-24h)) → almeno 1/giorno per sempre. 404/transitori/in-corso = ritenta, non rinuncia. Config Vapi:ReconcileDailyAfterAttempts(12). Idempotenza/no-doppio-addebito invariata.
Verificato: colonna presente; log "cadenza-giornaliera dopo 12 tentativi — non rinuncia mai"; smoke ciclo AR→pending, EOC→nemi_calls+saldo esatto+30+pending cancellata. Cleanup saldo 803.
DA FARE prod: migration con last_attempt_at + reload; Vapi:ApiKey per attivare.

## 5805 — Vapi:ApiKey impostata (Stefano) → RICONCILIAZIONE ATTIVA, TEST REALE OK
Key in appsettings.Development.json Vapi:ApiKey (36 char). Validata: GET api.vapi.ai/call → 200. Trovate chiamate reali orfane (in Vapi, non in nemi_calls): 019f4900-75be, 019f489a, 019f07b6 (vecchie, senza pending → non auto-recuperate).
TEST E2E: inserita pending per 019f4900-75be (created_at -1h), restart server (carica key). Servizio: "1 pending da verificare" → GET Vapi → "Nemi finalize (reconcile): 50s addebitati" → nemi_calls creato (50s, cost 0.1577, customer-ended-call, caller +393402606209, transcript 279 char) + saldo 803→853 + pending cancellata. Log ok, 0 ancora in attesa. Feature ATTIVA per chiamate future. Saldo baseline ora 853 (chiamata reale recuperata, lasciata).
DA FARE prod: migration nemi_pending_calls (+last_attempt_at) + reload PostgREST + Vapi:ApiKey nel config prod.

## 5807 — minuti al secondo nel display (era ceil). AssistantAi.razor Chiamate: riga 395 + summary 326 + dettaglio 453 → minuti = DurationSeconds/60 (N2), non MinutesBilled(ceil). Utilizzo stats consumato/residuo → consumedSec/60 e BalanceSeconds/60 (N2). Consumo saldo già al secondo; costo già preciso (sec/60×0.32). Ora coerenti. App restart 0 errori.
## 5808 — testo Knowledge Base per shop Puntify (Nemi Voce): fornito su Telegram (msg 5810) — cos'è Puntify / servizi (fedeltà, prenotazioni, menu, code, Nemi, app) / opportunità esercente. Basato su feature reali, tono startup. Chiesto a Stefano se salvarlo io nella KB o incollarlo lui.

## 5811 — abilitare box "Chiamate Nemi Voce" in panoramica (era placeholder "in arrivo") — DELEGATO subagent a6b26c34
Panoramica.razor BOX1 (~503): sostituire placeholder con dati reali aggregati su scope (ShopBundle pattern come BOX2 Richieste AI): n° chiamate + minuti AL SECONDO (DurationSeconds/60) + costo, via Social.NemiCallsAsync per shop con HasNemi(), + link a /assistantai/chiamate, stato vuoto/skeleton. i18n riuso nemicall_sum_*. Solo app. In corso.

## Panoramica — Chiamate Nemi Voce (BOX1 live)
- ShopBundle: +NemiCallsCount/NemiCallsDurationSec/NemiCallsCost (lazy con storico Nemi in LoadNemiForActiveAsync via Social.NemiCallsAsync, fail-safe 0)
- Recompute: aggrega sui PV in scope con HasNemi()
- BOX1: skeleton mentre lazy, 3 stat (chiamate / minuti precisi sec/60 N2 / costo N2 €) + "Vedi tutte" -> /merchant/{id}/assistantai/chiamate, empty nemiuse_calls_empty, no-Nemi pan_ai_requests_empty
- i18n: riuso nemicall_sum_calls/duration/cost, nemiuse_minutes_unit, nemiuse_calls_empty, socialstudio_see_all; aggiornato pan_tip_calls x10 lingue (rimosso "in arrivo")
- Build 0 errori (46 warning), riavviato SOLO puntify-app.service

## 5811 — box Chiamate Nemi Voce panoramica ATTIVATO (subagent a6b26c34)
ShopBundle +NemiCallsCount/DurationSec/Cost (lazy in LoadNemiForActiveAsync, per shop HasNemi via Social.NemiCallsAsync, fail-safe). BOX1 markup: skeleton lazy / 3 stat (n° chiamate, minuti al secondo DurationSec/60 N2, costo €) + link "Vedi tutte"→/assistantai/chiamate / stato vuoto nemiuse_calls_empty. Aggregazione _callsCount/DurationSec/Cost su scope. i18n riuso (nessuna nuova chiave) + pan_tip_calls aggiornata 10 lingue. Solo app restart, 0 errori. Shop puntify: 5 chiamate/821s da mostrare.
