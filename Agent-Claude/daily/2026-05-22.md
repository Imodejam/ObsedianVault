# 2026-05-22

## Sessione: Puntify — back button, info panel servizi, magic link

### Fatto
- **Migration `puntify.account` birthday**: corretto schema da `public.account` a `puntify.account` in `/home/progetti/puntify/docs/DB Migrations/20260522_account_add_birthday.sql`. Stefano ha eseguito la migration aggiornata su Supabase SQL editor.
- **BookingHub.razor**: sostituito `appt-back-btn` (icona-arrow only) con `cfg-back` standard (chevron + "Indietro") per uniformare con tutte le altre pagine merchant.
- **ShopServices.razor**: aggiunto `bk-info-panel` "A cosa serve definire un servizio?" sopra la lista servizi, stile coerente con BookingManualBlocks (Blocchi). Spiega durata, buffer, attivazione, prezzo opzionale.
- Build `dotnet build Puntify.App` verde (0 errori, 37 warning preesistenti).

### Aperto — in attesa decisioni Stefano
- ~~Magic link~~ → fatto.

### Magic link (decisioni Stefano: magic link only, via ResendEmailService, /auth/callback esistente)
- Server: `Controllers/AuthController.cs` con POST /api/auth/magic-link.
  - Chiama Gotrue admin `/auth/v1/admin/generate_link` (type=magiclink, redirect_to=AppUrl/auth/callback).
  - Spedisce via `ResendEmailService.SendAsync` (immediato, non in coda).
  - Risposta sempre `{sent:true}` per non leakare esistenza account.
- Templates: `EmailTemplates.MagicLink(name, link)` + `EmailType.MagicLink`.
- App: `Services/AuthApiService.cs` registrato in Program.cs; in `Pages/Shared/Login.razor` aggiunto link "Accedi senza password (link via email)" sotto Accedi, con `HandleSendMagicLink`.
- ⚠️ **Config Gotrue lato Supabase self-hosted**: serve `GOTRUE_URI_ALLOW_LIST` con `https://app-cat.puntify.it/auth/callback` altrimenti il redirect_to viene rifiutato.

### Note tecniche
- Tabelle Puntify stanno nello schema `puntify` (NON `public`). Sempre `puntify.account`, `puntify.account_shops` ecc.
- Tutte le pagine merchant usano pattern `cfg-page` > `cfg-header` > `cfg-back` + `cfg-title` > `cfg-main`. Eccezione era BookingHub, ora normalizzato (solo il back button — il layout `appointments-page`/`appointments-header` resta per via dei tab e action button).
