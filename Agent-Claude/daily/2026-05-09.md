# 2026-05-09

## Sessione: vetrina Piratopoly-web → Supabase

Stefano ha chiesto di collegare la vetrina (`/home/progetti/piratopoly-web`) allo stesso Supabase della PWA così che le mappe del marketplace appaiano automaticamente sul sito vetrina con le stesse descrizioni i18n.

### Cosa fatto
1. **Migration** `piratopoly/supabase/migrations/015_marketplace_slugs_seasons.sql`: aggiunge `cities.slug`, `maps.slug` (UNIQUE per city_id), `maps.season_tag`. Da applicare via Supabase Studio / `supabase_admin` (Stefano).
2. **Script backfill** `piratopoly/backend/scripts/backfill-marketplace-slugs.ts`: slugify NFKD da `city.name`/`map.title`, gestisce duplicati con suffisso `-2`, `-3`. Eseguibile via service role.
3. **Backend mapper** (`maps.routes.ts`): aggiunti `slug`, `citySlug`, `seasonTag` al payload. Shared type `GameMap` esteso.
4. **Vetrina**: rimosso Sanity (deps + dirs + env + sample-data), aggiunto `@supabase/supabase-js`, creato `lib/supabase/*` (client, maps, cities, format, types). Rotta dettaglio cambiata da `/mappe/[slug]` a `/mappe/[citySlug]/[mapSlug]`. ISR 60s su tutte le rotte data-driven. i18n esteso (5 lingue) con `cityPage.description`, `cityPage.empty`, `seasonal.until`. Build verde.

### Decisioni di Stefano (in conversazione)
- Slug derivato dal nome della mappa, **unique per città**.
- Durata derivata dal numero di tappe (1-2→Demo, 3-4→24h, 5-7→48h, 8-10→72h) in attesa del campo `duration_hours` esplicito della Fase 1 Pricing V1.
- Sanity tolto del tutto (anche per blog/testimonials).
- Cover image: per ora puntare al server PWA (`piratopoly-dev.duckdns.org`); poi S3.
- Solo mappe `is_official = true` sulla vetrina.
- Prezzi formattati lato vetrina con Intl (EUR per ora).
- Mappe stagionali = mappe con `season_tag` valorizzato (Halloween, Natale, Harry Potter, ecc.). Indipendente dal `type='temporary'` delle tappe.

### Bloccanti per smoke test live
- Migration non applicata sul DB → fino a quando `cities.slug` e `maps.slug` non esistono, le query fanno warn + ritornano `[]` e la home/sezioni mostrano niente. Build comunque passa.
- Anche dopo migration: serve eseguire backfill script per popolare gli slug, altrimenti tutte le righe hanno `slug=NULL` e non finiscono nei filtri vetrina.

### Prossimi passi
- Stefano applica migration 015 via Supabase Studio.
- `cd /home/progetti/piratopoly && npx tsx backend/scripts/backfill-marketplace-slugs.ts` (gira da claudebot, usa service role già nel `.env`).
- (Opzionale) valorizzare `maps.season_tag` su qualche mappa per testare la sezione `SeasonalMaps` della home.
- `cd /home/progetti/piratopoly-web && npm run dev` per smoke locale.
