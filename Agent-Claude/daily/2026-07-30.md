# 2026-07-30

## Apple RIFIUTA di nuovo (build 7, submission 8d6fd216) — 4 rilievi
Review 30/07 su iPhone 17 Pro Max (+ iPad Air per il punto mappa).

### COME HANNO BUCATO IL 3.1.1 (prova dai log Nemi, screenshot Stefano)
30/07 08:03 "Utente accesso" yvbrk4w4g7@privaterelay.appleid.com -> 08:04 "Nuovo negoziante registrato" (Ragione sociale "Test", Referente "Will Ehite") -> 08:04 "Nuovo negozio creato".
=> Il reviewer ha usato SIGN IN WITH APPLE: il login social su GoTrue CREA l'account automaticamente, poi /role-selection e /merchant/registration erano SENZA gating. Il gate esistente copriva solo il form email/password di Login.razor (toggle nascosto).

### MAPPA DEI 4 PUNTI (subagent, path:linea)
1. 3.1.1 registrazione business: BUCHI = OAuth Google/Apple senza gating (Login.razor 485-545 + AuthCallback.razor 43-85, CreateOrUpdateAccount crea account), /role-selection APERTO (RoleSelection.razor, SelectMerchantRole), /merchant/registration APERTO, /merchant/add-shop APERTO. NB: MerchantRegistration scrive DIRETTAMENTE su PostgREST (_client.From<MerchantProfile>().Insert) -> il blocco deve stare nel client WASM, il mio server non intercetta.
2. Guideline 4 SIWA: AuthCallback/SupabaseService.CreateOrUpdateAccount (~1031) salva SOLO l'email, NON legge user_metadata.full_name -> il nome viene richiesto dopo (il reviewer l'ha inserito in MerchantRegistration: "Will Ehite").
3. Guideline 4 iPad mappa: Home.razor:164 div #map con height FISSA 400px inline, nessuna media query tablet; app.css .container max-width 680px da 768px; #map ha solo z-index. Serve anche Leaflet invalidateSize() su resize/orientationchange.
4. 5.1.2 tracking: NON e' il banner cookie della Vetrina (gia' sistemato) ma Pages/Shared/Consent.razor (/consent) mostrata dopo la registrazione cliente: voci "Offerte personalizzate" (profilazione su gusti/acquisti) e "Contatto diretto merchant" = tracking per Apple; email/SMS/push sono marketing diretto.

### AZIONI
- Fix 1 (3.1.1) delegato: NativeAppService guard + gating RoleSelection/MerchantRegistration/AddShop/Login + AuthCallback, con blocco anche del submit e degli URL diretti. Utenti esistenti (anche esercenti) NON devono essere impattati.
- Fix 2+3 (SIWA name + mappa iPad) delegati in parallelo (file disgiunti per evitare conflitti).
- Fix 4 (tracking): CHIESTA DECISIONE A STEFANO -> A) nascondere su iOS solo "Offerte personalizzate"+"Contatto merchant"; B) nessun consenso promozionale su iOS, solo privacy obbligatoria. Raccomandato B (2 rifiuti alle spalle).

## Fix Apple Guideline 4 — nome Sign in with Apple + mappa iPad (subagent)
- `Puntify.App/Services/SupabaseService.cs`: nuovo `GetDisplayNameFromAuthMetadata()` (legge `Dictionary<string,object>? User.UserMetadata`, ordine full_name > name > given_name+family_name > preferred_username, mai eccezioni); usato sia sull'insert di un account nuovo sia come backfill se `displayname` e' vuoto. Nessuna validazione sulle email `@privaterelay.appleid.com`.
- `Puntify.App/Pages/Shared/Home.razor`: div mappa senza style inline -> `class="explore-map"`.
- `Puntify.App/wwwroot/css/app.css`: `.explore-map` 400px mobile, `min(600px, calc(100dvh - 220px))` + min-height 400px da 768px.
- `Puntify.App/wwwroot/js/map.js`: `refreshSize()` (invalidateSize) chiamato su init (rAF + 300ms), window resize, orientationchange (debounce 150ms) e ResizeObserver sul contenitore; cleanup in destroy().
- Build Debug 0 errori. Non committato, non deployato. Nota: `js/map.js` in index.html non ha cache-busting `?v=`.

## 4 FIX APPLE IMPLEMENTATI E VERIFICATI (commit 047dfea)
Nuovo requisito Stefano: dentro l'app iOS SOLO LOGIN, nessuna registrazione ne' esercente NE' CLIENTE (la review testera' l'esercente con demo@demo.it).

### Implementazione
- NUOVO Puntify.App/Services/NativeAppService.cs: unico punto di verita' IsNativeAppAsync() su puntifyNativeAuth.isNative; cache statica che NON memorizza i fallimenti interop (un render precoce non spegne il gating). Registrato in Program.cs:78.
- AuthCallback.razor:69-74: login social/email di utente SENZA account app -> NIENTE CreateOrUpdateAccount, SignOut, redirect /login?signup_blocked=1 con avviso. Retry 800ms prima di bloccare (un errore di rete non deve sloggare un utente reale).
- Login.razor: EffectiveSignUp => _signUp && !_isNativeApp (markup+submit), SetMode(true) ignorato, guard in HandleSignUp, BlockNativeAccountCreationAsync prima di BuildSessionEntry.
- RoleSelection.razor: card esercente non renderizzata (check prima di _loading=false, nessun flash) + guard in SelectMerchantRole.
- MerchantRegistration.razor / AddShop.razor: pagina e submit bloccati anche via URL diretto, messaggio "non disponibile nell'app".
- SupabaseService.cs: CreateOrUpdateAccount legge user_metadata (full_name -> name -> given+family -> preferred_username) e valorizza DisplayName; backfill se vuoto, mai sovrascrittura. Email privaterelay accettate.
- Home.razor + app.css: #map -> classe .explore-map, mobile 400px, >=768px min(600px, calc(100dvh-220px)); map.js: refreshSize() con invalidateSize su resize/orientationchange/ResizeObserver + cleanup in destroy. index.html: map.js con ?v=__buildVersion (senza, il device serviva il file vecchio).
- Consent.razor (fatto da me): su iOS nascoste le sezioni Comunicazioni e Personalizzazione, resta solo l'accettazione privacy (scelta B di Stefano). I consensi dati dal web NON vengono azzerati (il salvataggio reinvia i valori caricati).

### VERIFICA REALE (playwright-core, UA app iOS vs UA browser, account creati ad hoc su CAT)
APP iOS: /login e /register -> nessun elemento cliccabile di registrazione, submit "Sign in", solo hint "Sign up at puntify.it"; role-selection -> SOLO "I'm a Customer"; /merchant/registration e /merchant/add-shop -> "Not available in the app", nessun form; /consent -> 1 solo toggle (privacy); esercente esistente -> entra su /merchant-shops regolarmente.
TEST CRITICO (simula il primo Sign in with Apple): utente GoTrue SENZA riga account, login da iOS -> /login?signup_blocked=1 e **0 righe account create** (verificato su DB). Lo stesso utente dal browser -> prosegue a role-selection (registrazione web funzionante).
BROWSER: /register con "Sign up" e "Create account", role-selection con cliente+esercente, form registrazione/negozio presenti, consent con 6 toggle -> nessuna regressione.
Tutti gli account/utenti di test rimossi da CAT (3 account + 4 utenti auth, residui 0).

### DA DECIDERE / SEGNALATO A STEFANO
- AddShop e' bloccato anche per esercenti GIA' registrati che usano l'app iOS: e' una regressione funzionale voluta (il reviewer entra con demo@demo.it, che e' esercente: se il form fosse raggiungibile lo leggerebbe come registrazione business). Da confermare.
- Il login social crea comunque l'utente su GoTrue prima del callback: resta un utente auth orfano senza account Puntify. Eventuale pulizia periodica lato server.

## Aggiustamento finale (decisioni Stefano) — commit d511550
- Stefano: nelle Review Notes indirizzera' Apple a provare come CLIENTE (non esercente); quindi "se un esercente entra deve poter fare tutto".
- RIAPERTO /merchant/add-shop nell'app iOS (rimosso gating + guard submit + inject inutilizzato): aggiungere un PV e' gestione di un account business esistente, non registrazione. Resta BLOCCATO cio' che Apple contesta: /merchant/registration e l'opzione ruolo esercente in role-selection.
- "Lasciamo come hai fatto" interpretato come risposta alla domanda sul popup -> NIENTE popup: resta l'avviso testuale in cima al login + la riga non cliccabile "Registrati su puntify.it" (modello Netflix/Spotify; un pulsante verso il sito sarebbe anti-steering 3.1.1). Segnalato a Stefano nel report in caso di fraintendimento.
- VERIFICA post-modifica (playwright UA-iOS): add-shop -> form presente, nessun blocco; merchant/registration -> ancora bloccato; consent -> 1 solo toggle (privacy); esercente entra su /merchant-shops. Account di test ripuliti.
