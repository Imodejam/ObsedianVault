# 2026-07-20

## Puntify - Conto tavolo (Opzione B): ordini separati aggregati + pay-table

Stefano ha scelto B (vs append come portate): ordini SEPARATI per tavolo, aggregati
in un unico conto in Cassa e saldati insieme. Ogni giro resta un record distinto
(comodo sugli schermi di sala).

### Fatto
1. REVERT append-come-portata in MenuController.cs:
   - SubmitOrder pubblico (~1401) e path POS (~1907): rimossi i blocchi
     TryAppendCourseToOpenTableOrderAsync -> ogni invio crea SEMPRE un nuovo ordine.
   - Rimossi helper TryAppendCourseToOpenTableOrderAsync + StationNeedsReopen (non usati).
   - display_course* / raggruppamento display: lasciati (innocui, ogni ordine = card).
2. Endpoint POST /api/menu/orders/pay-table (record PayTableRequest):
   { shopId + tableOperatorId }  OPPURE  { orderIds[] }, + payments + invoiceRequested.
   - Salda TUTTI i giri aperti del tavolo (paid=true, status+stazioni=delivered).
   - Idempotente: esclude paid/cancelled; 2o colpo -> 409 nothing_to_pay.
   - Fedelta' UNA volta sul totale complessivo (CreditTableLoyaltyAsync), guardia
     loyalty_credited su tutti i giri; 1 sola transaction (desc = codici dei giri).
   - /{orderId}/pay singolo INVARIATO per asporto/vendita diretta.
3. Cassa (Puntify.App/Pages/Merchant/Dashboard.razor):
   - Campo _cassaTableOrders (giri gia' inviati del tavolo, read-only aggregati).
   - OnTableClick -> LoadTableConto: carica tutti gli ordini aperti + draft vuoto.
   - CassaGrandTotal = giri inviati + draft in corso; CassaPayableTotal = solo giri
     inviati (il draft va prima "Inviato" -> crea un ordine separato).
   - CassaCanPay/OpenPayment abilitati anche senza _cassaOrderId se c'e' conto tavolo.
   - PayPrimaryAsync: se conto tavolo -> MenuOrders.PayTableAsync(orderIds); altrimenti
     flusso storico /pay singolo. VAT breakdown include i giri inviati.
   - MenuOrdersApiService.PayTableAsync aggiunto.
   - 3 chiavi resx (cassa_order/cassa_sent_round/cassa_new_round) in 10 lingue.

### Verifica reale (puntify_cat, Pepto ad69804f, table_resource_id)
- 2 ordini dine_in separati (#A01/#A02) sullo stesso tavolo -> 2 record distinti (no append).
- pay-table (orderIds, 5.00) -> entrambi paid/delivered; fedelta' 1 sola tx (5pt/5.00,
  account pppp@gmail.com); 2o pay-table -> 409; loyalty tx resta 1.
- asporto: /pay singolo -> 200 paid/delivered (invariato).
- Build server+app 0 err; riavviati uno alla volta (server poi app); 8001/8002 = 200.
- Dati di test rimossi (5 ordini + 1 tx). Residui pre-esistenti del vecchio agent su
  tavoli "2"/"4" NON toccati.

### Note
- Server gira sotto `dotnet watch`: restart via systemctl impiega ~25s a ritornare 200.
- CAT non committato (regola: solo collaudo).

## Puntify — 3 richieste UI Stefano (FATTO)
- A) Dashboard.razor Cassa: route @page .../dashboard/{Tab}/{OrderRef} (deep-link conto/tavolo, sopravvive refresh); badge od-badge dentro Cassa per singolo giro; tasto "Invia in cucina" (se draft) vs "Incassa" (se saldabile). per-item delivered = FOLLOW-UP (serve nuovo modello per-piatto).
- B) "Tav. N" via display_table_prefix + helper ResourceKinds.IsTable (solo tavoli) su Dashboard/TablesPlanimetry; sedie colorate: seduti = max Covers dei giri aperti (menu_public_orders non pagati), rosso .is-taken (pagina editor senza accent shop). TablesHome/BookingAgenda: nessuna modifica necessaria.
- C) MerchantMenuPreview.razor: sheet tracking persistente (non sparisce a ready) con codice+voci+badge stato + blocco "Stato per reparto" Cucina/Bar; esteso PublicOrderStatusResponse (kitchen_status/bar_status), verificato live. Localizzazione via UiStrings inline (non resx), 4 chiavi in 20 lingue.
- Chiavi resx App (10 lingue): cassa_collect, cassa_send_kitchen, cassa_send_hint_empty.
- Build 0 err server/app/vetrina; restart uno alla volta; :8001/:8002/:8003 up. WASM: Stefano hard-refresh Ctrl+F5.

## [2026-07-20] Selettore Tipo sezione + regressione settimanale
- MenuEditor.razor: aggiunto <select> Kind (Cucina/Bar) accanto a nome/descrizione sezione,
  con FieldInfo "i" + tip; @bind:after -> SaveSection -> UpdateSection (persiste s.Kind).
  Fix feedback 6863: prima il tipo sezione non era configurabile da UI (solo il tipo del piatto).
  2 resx nuove (menueditor_section_kind, tip_section_kind) x10 lingue. Build 0 err, app:8002=200.
- Regressione settimanale (subagent QA, Playwright headless): 9 PASS / 0 FAIL.
  Vetrina home/negozi/pepto(accento verde ok)/menu/blog, API health+order-status, servizi systemd, app WASM.
  Nessun regresso, nessun dato di test creato. Screenshot in scratchpad.

## [2026-07-20] Catalogo: tipologia + rename Menu->Catalogo (feedback 6866)
- Modello: ShopMenu.CatalogType (default restaurant) + costanti CatalogTypes (Restaurant/Services/Retail/Generic,.All,.IsFood).
- Migration 2026-07-20_catalog_type.sql su CAT: colonna catalog_type + CHECK + NOTIFY pgrst.
- Server: Create/Update/duplicate menu persistono CatalogType. Build+restart server OK.
- App (subagent A): selettore tipologia a card in MenuEditor "dati"; tipo-sezione Cibo/Bevande SOLO restaurant
  in box dedicato (CSS booking.css); DishEditor titolo "Modifica prodotto" + param CatalogType, sezioni food
  (kind/ingredienti/allergeni/dieta/caratteristiche) SOLO restaurant, formati nascosti su generic;
  rename label Menu->Catalogo (menulist_title, MerchantHome tile) no rotte/no bottomnav. resx x10.
- Vetrina (subagent B): rename Menu->Catalogo su etichette feature (Nd_MenuBtn, footer/mega-menu, Menu.razor,
  Home/Prezzi/FAQ), resx 11 locale (+it.resx override), nessuna rotta/URL/hreflang toccata.
- Health: 8001/8002=200, 8003=302(up). Build 0 err su tutti.
- SKIP segnalati a Stefano: (1) MerchantMenuPreview (pagina menu pubblica shop) resta terminologia ristorante
  -> valutare label per catalog_type; (2) "menu cartaceo" tenuto letterale in alcune lingue.

## [2026-07-20] Follow-up 6870/6872: pagina pubblica per tipologia + riga compatta + cartaceo
- Vetrina MerchantMenuPreview (subagent): TCtx reso catalog-aware via CatalogOverrides per-lingua;
  restaurant -> wording "Menu" invariato (short-circuit su IsFood), non-restaurant -> "Catalogo" + nomi neutri
  (prodotti/articoli) su 9 chiavi (empty.*, home.explore_*, home.featured_title, home.dishes_count, cart.browse),
  20 lingue. Sostituito il vecchio ServiceOverridesIt (IT-only "servizi") -> ora booking shop non-restaurant = "Catalogo".
- App MenuEditor: "Tipo sezione" ora su UNA riga (me-section-kind-row flex space-between, select 160px), CSS booking.css.
- Vetrina resx: ripristinato "menu cartaceo" IT (Menu_Feat7_Desc, Menu_Faq1_A) per la ristorazione.
- Health 8001/8002/8003 up, build 0 err. Check headless pepto = restaurant wording intatto (no "catalogo").

## [2026-07-20] Fix riga "Tipo sezione" (6876/6878)
- Stefano "non si e' aggiornata bene": NON era cache (build fresca, CSS versionato via __buildVersion, SW off su CAT).
  Era la resa: me-section-kind-row con justify-content:space-between -> label e select ai due estremi, vuoto in mezzo.
- Fix: justify-content flex-start, label+select adiacenti a sinistra, select 180px; su <=480px torna space-between.
- Nota processo: il ?v= dei CSS App usa __buildVersion (build-info.js generato da MSBuild) -> per far arrivare
  una modifica SOLO-CSS al browser bisogna RICOMPILARE (bump build-info), non basta editare il file.

## [2026-07-20] Redesign meta-sezione (6880): descrizione+tipo affiancate, input bianchi, margine
- Desktop: me-section-meta--2col flex -> Descrizione categoria (flex:1) + Tipo sezione (240px) affiancate; <=720px impilate.
- Grigio: .me-section (#FAFAFA) + input (--cfg-input-bg #FAFAFA) si confondevano -> input sezione forzati a #fff.
- Margine: me-section-meta margin-bottom 14px (prima la lista piatti era attaccata alla tipologia).
- Build bump + restart app (8002=200). CSS servito verificato (2col + bianco).

## [2026-07-20] Description sezione: bianco + altezza ridotta + auto-grow (6882)
- Bianco non applicava: .me-section-desc-input (0,1,0) pareggiava .cfg-textarea di config.css (caricato dopo booking.css) -> vinceva config.
  Fix: selettori .me-section-meta .me-section-desc-input (0,2,0) per bg #fff + hover/focus.
- Altezza: min-height 96px->40px, rows 2->1; field-sizing:content (gia usato in .cfg-textarea--grow) per crescere col testo (no scroll).
- Build bump + restart (8002=200).

## [2026-07-20] Description altezza = select (6884, Stefano infastidito "sii preciso")
- Select Tipo sezione = 44px (height:44,line-height 1.4,padding 0 14px). Allineata description: min-height 44px,
  padding 11px 14px, line-height 1.4, font 14px -> single-line identica al select; field-sizing cresce se multiriga.
- Build bump + restart (8002=200). Lezione: prendere metriche esatte del riferimento, non stimare.

## [2026-07-20] Tasto catalogo pagina negozio dinamico (6886)
- NegozioDetail: tasto etichetta per catalog_type + menu_orders_enabled:
  ristorazione+ordiniON="Ordina Ora", ristorazione+ordiniOFF="Menù", altri="Catalogo".
- Nuovo SupabaseStatsService.GetActiveCatalogTypeAsync (preferisce catalogo restaurant se piu' menu attivi).
- Shop.MenuOrdersEnabled = ordini attivi; GetShopBySlug non restringe colonne -> caricato.
- 2 resx nuove (Nd_OrderBtn, Nd_MenuLabel) x11 lingue (default+it override inclusi). Nd_MenuBtn=Catalogo riusata.
- Test reale headless su pepto TUTTI 3 rami OK, poi ripristinato (restaurant, ordini ON). 8001/8002/8003 up.
- Nota: toccato Punto.Shared -> possibile rebuild watcher multipli; verificato nessuna corruzione (tutti 200/302).

## [2026-07-20] Display "Pronti" ordine + task Firebase (6889/6890/6892/6893)
- DrinksDisplay + KitchenDisplay: colonna ready ora OrderByDescending(CreatedAt) (ultimi in cima), altre FIFO. Build+restart app.
- NUOVO task in corso: mail+notifica web (Firebase) al cliente quando cibo va "in preparazione".
  Stefano segnala che le notifiche Firebase non arrivano mai -> indagine infrastruttura push prima di implementare.

## [2026-07-20] Notifiche ordine "in preparazione" + diagnosi Firebase (6892/6893)
- Email "in preparazione" al cliente: AGGIUNTA in NotifyOrderStatusChangeAsync (prima solo su "ready").
  PreparingEmailStrings 6 lingue. VERIFICATA reale: log Resend "Email inviata ... Il tuo ordine #A11".
- Diagnosi push Firebase (perche' non arriva mai): server Firebase REALE (non Mock), push su preparing gia' codificato,
  MA 0/11 ordini con push_token -> il cliente non concede/registra mai il permesso notifiche (RegisterOrderPushAsync
  fire-and-forget con catch silenzioso; codaPush.getToken torna null senza permesso). Email OK 7/11.
- Fix delegato a subagent Vetrina: opt-in esplicito "Avvisami" sulla schermata conferma ordine (click->permesso->salva token),
  hardening RegisterOrderPushAsync (no catch muto). Push web richiede consenso browser -> test finale lato Stefano.

## Puntify Vetrina — web push ordini menu (fix cattura token)
Problema: 0/11 ordini menu avevano push_token (client registrava fire-and-forget con catch silenzioso, prompt permessi mai concesso).
Fatto in Pages/MerchantMenuPreview.razor (CAT):
- Nuovo opt-in sulla schermata di conferma: bottone "push.enable" (gesto utente -> prompt permessi browser), stati _pushState (prompt/granted/denied/error/unsupported) + _pushBusy.
- InitOrderPushAsync: se permesso gia' granted -> auto-register silenzioso; denied -> hint bloccato; default -> mostra bottone; unsupported -> niente.
- EnableOrderPushAsync: handler bottone; RegisterOrderPushAsync ora ritorna bool, logga console.warn su errori (niente catch silenzioso). Payload/endpoint invariati: { pushToken } -> /api/public/menu/order/{orderId}/push.
- Traduzioni push.enable/active/blocked/error in tutte le 20 lingue.
- Reset di _pushState in SubmitOrder/OpenSavedOrder/ResetAfterOrder.
Build 0 errori, restart puntify-vetrina OK (curl 302).

### Update (Stefano): prompt automatico dentro il gesto ordine
- Il prompt permessi ora compare DA SOLO nel tap "ordina": SubmitOrder chiama `await AutoRegisterOrderPushAsync()` (getToken dentro il gesto utente -> "Consenti/Allow" appare senza bottone), poi registra il token.
- Bottone "🔔 Avvisami" degradato a FALLBACK: mostrato solo se permesso resta 'default' (prompt chiuso) o auto-register fallita; 'granted' = registrazione silenziosa (nessun bottone); 'denied' = hint "notifiche bloccate".
- Net: dopo un solo "Consenti", ricevuto/preparazione/pronto arrivano da soli. Endpoint/payload invariati. console.warn su errori, nessun catch silenzioso.

## [2026-07-20] Push fix MIME + opt-in automatico + email preparing + Social Nemi
- BUG PUSH ROOT CAUSE (con prova): su Vetrina /firebase-messaging-sw.js era servito come text/html (fallback pagina),
  il browser rifiuta di registrare il SW -> nessun token -> "Non e' stato possibile attivare gli avvisi".
  Fix: MapGet esplicito in Program.cs con content-type text/javascript (come manifest/robots). Verificato: ora text/javascript + body JS.
  Sblocca anche il push della coda. App (8002) lo serviva gia' giusto.
- Opt-in push automatico (subagent): richiesta permesso nel gesto invio ordine + bottone ripiego, 20 lingue. Vetrina up.
- Email "in preparazione" al cliente aggiunta (verificata Resend). Firebase server REALE (non Mock).
- Social Skin: bozza domani 21/07 cambiata da "cassa" (Stefano: non pubblicizzare cassa) a "Nemi" (telefono che squilla).
  Inviata a Stefano per approvazione, NON pubblicata. topic-history NON ancora aggiornato (solo dopo publish).
- NOTA STEFANO (6906): app iOS/Android gia' create -> le notifiche dovranno arrivare anche alle app native.
  FCM server-side e' lo stesso (SendToTokenAsync funziona con qualsiasi token FCM web/nativo); serve registrare il
  token FCM nativo dal device + salvarlo legato a utente/negozio + instradare (ordini/prenotazioni/coda). DA FARE quando si collegano le app.

## [2026-07-20] Conferma ordine: titolo dinamico + icona push stile vetrina (6912)
- MerchantMenuPreview: titolo H2 conferma ora riflette lo stato live (ConfirmTitle(): Ordine ricevuto/in preparazione/consegnato),
  non piu' fisso "Ordine inviato!"; rimossa la pill di stato ridondante sotto. 3 chiavi title_* x20 lingue.
- Icona "Avvisi attivi": emoji -> SVG campanella Feather (stroke currentColor) come le icone di NegozioDetail; rimosso il prefisso ✅ da push.active in 20 lingue.
- Build 0 err, vetrina up (302). Subagent Vetrina.

## [2026-07-20] Sottotitolo conferma neutro (6915)
- confirm.waiting non inizia piu' con "Ordine ricevuto." (contraddiceva il titolo dinamico "Ordine in preparazione");
  ora solo info mail di tracciamento, in tutte le 20 lingue. Build+restart vetrina (302).

## [2026-07-20] Lock tavolo con ordine aperto (6918)
- Nuovo endpoint server GET /api/public/menu/{slug}/tables/{tableId}/open -> {open:bool} (ordine non pagato non annullato). Build+restart server, testato True/False (con cleanup).
- GOTCHA COLONNA: menu_public_orders ha DUE colonne tavolo: table_operator_id (LEGACY, NULL) e table_resource_id (REALE,
  popolata dagli insert via [Column("table_resource_id")] su MenuPublicOrder.TableOperatorId). pay-table/transfer usano table_resource_id.
  Le query raw PostgREST DEVONO usare table_resource_id (io avevo usato table_operator_id -> open sempre false, corretto).
- Client (subagent Vetrina in corso): _tableLocked via RefreshTableLockAsync su set tavolo (token/picker); se locked blocca cambio tavolo/modalita + nota table.locked_note 20 lingue.
