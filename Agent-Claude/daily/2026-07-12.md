
## Routine 9:00 — stato traduzioni
- Vetrina: 10 lingue complete (9 file tradotti ~6246-6249 chiavi + it/base neutral). Nessun gap.
- Menu realtime: bloccato (credito Anthropic esaurito), decisione pendente A (ricarico) / B (chiave esercente). Nessun errore in log 24h = non esercitato su collaudo.
- Report inviato a Stefano (msg 5843).

## Piracity incident (immagini + ricerca)
- IMMAGINI: DB cover_url usa schema vecchio /assets/auto/maps/piracity-<id>.webp; il codice (commit c3fbae6) serve /assets/webp/maps/<id>.webp. I file auto/ (generati a runtime, mai committati) persi → 200 HTML fallback = rotte. Ritrovati in piracity-web/public/assets/auto/ (maps 38 + cities 140) e copiati in piracity/frontend/public/assets/auto/. 104/105 published ok, 1 senza cover. RISOLTO (Ctrl+F5).
- DEBITO: cover non versionate → riperse al prossimo deploy pulito. Da committare o rigenerare stabilmente.
- RICERCA: SearchBar in MarketplacePage.tsx non usata (stato search mai applicato) + backend /maps senza param search. Fix delegato a subagent (backend .or(title/slug ilike) + frontend debounce). In corso.
- RICERCA RISOLTA: /maps?search= (title/slug ilike) + debounce frontend. Verificato. Backend tsx watch auto-reload. Entrambi i problemi Piracity chiusi (collaudo). Aperto: decisione versionamento/rigenerazione cover.
- NUOVO: città più vicine per geolocation su app /marketplace (chip città ordinate per distanza, tap=filtro cityId) e vetrina /it/towns (nearest-first, fallback alfabetico). 105 città con lat/lng. 2 subagent in corso.
- FATTO: città più vicine su app /marketplace + vetrina /it/towns. tsc pulito, servizi 200, HMR. Collaudo (PROD no). Entrambi i subagent chiusi.
- FIX marketplace riordino città: high-accuracy falliva su desktop → low-accuracy dedicata (setPosition diretto nello store). tsc ok, HMR. Ricordare a Stefano: se negato in passato, riattivare posizione nel browser.
- NUOVO: hero Recensione = struttura NegozioDetail. Estratto Components/ShopProfileHero.razor (Tailwind globale via App.razor), refactor NegozioDetail (Actions slot per i bottoni) + Recensione fetch Shop via SupabaseStatsService. Verifica pepto-roma invariata. Subagent in corso.
- FATTO hero recensione identico a negozio: componente condiviso ShopProfileHero + helper orari. Verificato su entrambe (pepto invariata). Collaudo, PROD no.
- NUOVO: restyle form recensione Apple/Stripe (Recensione.razor + rv-* in menu-public.css, bump ?v=). Delegato a subagent.
- FATTO restyle form recensione Apple/Stripe (Recensione.razor + rv-* menu-public.css, bump ?v=20260712a). Verificato markup+build; giudizio estetico a Stefano. Nota: screenshot headless inaffidabile (Blazor Server skeleton).
- CAMBIO DIREZIONE (Stefano: restyle precedente 'antico'): flusso a domanda singola Typeform/Apple, logo top, stelle grandi, auto-advance; hero cover rimosso dalla recensione. Delegato a subagent.
- FATTO flusso Typeform recensione. Verificato con Playwright su token reale (auto-advance funziona), screenshot step1/final inviati a Stefano. Collaudo, PROD no.
