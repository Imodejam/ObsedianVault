
### Fix icona/manifest App (HH)
- Bug: icone PWA 404 su app-cat (manifest con path assoluti /app/... da StaticWebAssetBasePath, ma app servita a root sul subdominio).
- Fix: manifest.json a path relativi (icons/screenshots/shortcuts/start_url/scope/id) → robusto a root e sotto /app/. Commit d9bf2de.
- Pagina admin esercente in caricamento: nessun errore server/build, giudicato transitorio da rebuild live (dotnet watch durante traduzioni). Se persiste ad app stabile → aggiungere timeout+errore.
- In corso: Fase 3 traduzioni app (area CLIENTE, subagent).
