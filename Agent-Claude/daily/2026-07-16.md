
## [2026-07-16 ~05:25] Pagina Dispositivi: 3 fix (6327/6329/6331)
- 6327 (PROD): anteprima monitor (iframe src=m.Url, sandbox allow-scripts allow-same-origin) su iOS dirotta l'app allo schermo "prendi ticket". Causa: pagine coda nell'iframe navigano il top (CodaNegozioDisplay NavigateTo /coda/totem forceLoad) + footgun sandbox. Fix: iframe con embed=1 + guardie embed sulle pagine coda (no navigazione top in embed) + hardening sandbox.
- 6329: separare monitor Cucina (kitchen/drinks/customer) da Code (totem/board/queue_display) in gruppi.
- 6331: nascondere gruppo Code se !HasQueue (&64), gruppo Cucina se !HasScreens (&32). Flag in ShopExtensions.
- Subagent ae455063 in corso (Screens.razor + coda pages + AppResource).

## [~05:40] Pagina Dispositivi 3 fix — FATTO (commit c803df5)
- 1) Hijack iOS: iframe con EmbedUrl(?embed=1) + CodaNegozioDisplay guardie _isEmbed (SwitchLang early-return + auto-nav gated) + sandbox senza allow-top-navigation. 2) Gruppi Code/Cucina/Altri (RenderFragment MonitorCard). 3) Hiding: !HasQueue nasconde Code, !HasScreens nasconde Cucina. 3 chiavi ×10 lingue. Build ok, app+vetrina riavviate.
- Bonus: rimossa dal repo 20260714_operators_manual_blocks.sql (obsoleta, dava errore a Stefano).
- Caveat comunicato: se il salto totem persiste su iOS = interception nativa dell'URL → fallback anteprima statica.
- git: c803df5 pushato.

## [~06:00] Auto-reload app dopo deploy (no piu' Ctrl+F5) — FATTO
- Richiesta Stefano (6334/6335): l'app gira in browser + WebView iOS/Android, il cliente non sa fare Ctrl+F5 -> serve refresh automatico dopo ogni pubblicazione.
- Soluzione: nuovo `Puntify.App/wwwroot/js/version-watch.js`, collegato in index.html dopo build-info.js. Poll di build-info.js (?_=ts, no-store) ogni 60s + visibilitychange/focus/pageshow; cambio di window.__buildVersion => pulizia SW (preserva firebase-messaging) + Cache Storage + location.reload().
- Guardie: rimanda se utente sta scrivendo (input/textarea/select/dialog) per non perdere dati; anti-loop -> dopo 2 reload falliti banner "Aggiorna" cache-busting (?_v=); escluso su localhost (hot-reload dotnet watch).
- Header verificati (curl :8002): blazor.boot.json gia' no-cache (DLL fresche al reload); index.html/build-info.js solo ETag (cache euristica) = causa storica dell'hard-refresh.
- Live su CAT (solo static JS/HTML, nessuna ricompilazione). Bootstrap: le pagine gia' aperte ora richiedono ancora 1 refresh manuale; da qui in poi auto.
- Non committato (in attesa di "committa" da Stefano). Memoria reference_puntify_app_deploy aggiornata.

## [~08:05] Traduzione pagine supporto in 9 lingue — IN CORSO
- Richiesta Stefano (6337): tradurre le pagine di supporto nelle varie lingue.
- 25 articoli x 9 lingue (en,es,fr,pt,zh,ar,hi,bn,uk) via 9 subagent paralleli -> JSON validati (25/25 ognuno).
- Aggregatore Python -> migration idempotente 2026-07-16_support_articles_i18n.sql (225 righe, PURO ASCII con E'\uXXXX', ON CONFLICT slug+language). Applicata su CAT: ora 10 lingue x 25 = 250 righe. Byte-check ar/bn/hi/uk/zh OK.
- Fix codice: GetSupportArticleBySlug(slug, language) con fallback IT (slug uguali tra lingue) + SupportoArticle.razor passa Lang. Pagine articolo /es /ar verificate tradotte.
- Scoperto: UI statica pagina supporto (H1/hero/form) usa chiavi resx Support_*/Meta_Supporto_* presenti solo nel resx IT default -> mancano nelle 9 lingue. 38 chiavi in traduzione via subagent; poi inject_resx.py nei SharedResource.<lang>.resx.
- Risposto anche a Stefano: OAuth Facebook prod URL https://api.puntify.it/api/oauth/callback/facebook e' corretta (api.puntify.it = backend .NET prod; path = rotta SocialOAuthCallbackController).

## [~08:20] Traduzione supporto — COMPLETATA
- UI resx: 38 chiavi Support_*/Meta_Supporto_*; mancavano 15 per lingua (le 23 form-key esistevano). Inserimento MINIMALE (inject_resx_minimal.py) solo delle mancanti prima di </root> -> diff pulito 45 righe/file, 9 lingue. (Primo tentativo ET-based riscriveva tutto il file/rinominava ns -> scartato, ripristino backup.)
- Verifica render headless: /en /ar /uk supporto -> H1+title+meta tradotti correttamente. Articoli /es /ar gia' ok.
- Deliverable prod: docs/DB Migrations/2026-07-16_support_articles_i18n.sql (225 righe, idempotente, ASCII-safe). Codice+resx col deploy Vetrina.
- NON committato (Stefano aveva detto "non ancora"); chiesto se committare il blocco.

## [~08:15] Email servizi: icone home al posto delle emoji — FATTO
- Richiesta Stefano (6342): nei template email che elencano i servizi usare le icone dei servizi della home esercente (non emoji).
- Trovato: MerchantFeatureService.Catalog (8 feature con emoji) -> renderizzate in EmailTemplates.FeatureCardCell riga 529 come {f.Icon}. Unica email che elenca servizi (FeatureDiscovery).
- Home usa icone .webp in wwwroot/images/icons (Premi/Calendar/Menu/Ordini/Tavoli/EliminaCode/Nemi/Marketing).
- Convertite 8 webp->png 144px (Pillow) in wwwroot/images/icons/email/{key}.png (loyalty,appointments,menu,takeaway,tables,queue,nemi,social). Servite dall'app (200).
- FeatureCardCell: emoji div -> <img src="{app}/images/icons/email/{f.Key}.png" 52x52> email-safe (width/height attr, inline style, bicubic).
- Verifica: anteprima headless -> tutte le card mostrano l'icona branded della home. Server ricompilato ok.
- Deliverable deploy: 8 PNG (wwwroot app) + EmailTemplates.cs. Nessuna migration.

## [~08:40] Prod menu 500 — RISOLTO (created_at corrotto)
- Diagnosi (foto Stefano): /api/menu 500 perche' GET su shop_services falliva. 3 righe shop_services (e ~50+ su 10 tabelle) con created_at/updated_at = 0001-01-01T00:49:56+00:49:56 (DateTime.MinValue con offset LMT Roma +00:49:56, coi SECONDI -> non ISO-8601). System.Text.Json .NET lancia -> intera GET in 500.
- Fix 1 (codice, durevole): Puntify.Server/Data/TolerantDateTimeConverters.cs (TolerantDateTime + Nullable) usati in SupabaseClient GetAsync/RPC (_readJsonOptions). Normalizza offset sub-minuto, fallback non-fatale. Bug intermedio: RoundtripKind|AdjustToUniversal illegale -> corretto in AssumeUniversal|AdjustToUniversal. Testato standalone con la stringa esatta: OK. Vale per OGNI tabella.
- Fix 2 (dati): migration idempotente 2026-07-16_fix_corrupt_created_at.sql (DO-block su tutto schema puntify, created_at+updated_at < 2000 -> updated_at valido o now()). Applicata su CAT -> 0 residui.
- Prod: eseguire la migration (fix immediato) e/o redeploy server (fix codice). Consiglio: migration ORA + codice al prossimo deploy.
- SW/CloudFront gif in console = secondario (immagine legacy cross-origin + vecchio SW prod), si pulisce col deploy app. Non causa il 500.
- Fonte corruzione = insert che salva DateTime.MinValue; reader tollerante lo assorbe. Prevenzione insert (default DB + non inviare MinValue) = follow-up proposto.

## [~08:55] Prod /consent 42P01 consent_history — RISOLTO (bug latente CAT+prod)
- Errore: salvataggio profilo /consent -> 42P01 relation "consent_history" does not exist.
- Causa: trigger trg_consent_changes su account_profiles chiama public.log_consent_changes (SET search_path=public) che inseriva in consent_history NON qualificato -> risolveva public.consent_history (inesistente); la tabella vera e' puntify.consent_history. In prod la tabella mancava del tutto. Rotto anche su CAT (testato rollback -> 42P01), solo mai emerso.
- Fix (solo DB, idempotente): 2026-07-16_consent_history_fix.sql = CREATE TABLE IF NOT EXISTS puntify.consent_history + CREATE OR REPLACE della funzione con INSERT qualificati puntify.consent_history + ricrea trigger + NOTIFY pgrst reload.
- Applicata su CAT + ri-testata (rollback): salvataggio consensi OK (trigger scrive in puntify.consent_history).
- Prod: eseguire 2026-07-16_consent_history_fix.sql (nessun deploy codice); se serve reload/restart PostgREST prod.

## [~09:00] /consent toast esito — FATTO
- Richiesta Stefano (6350): al salvataggio in /consent mostrare il toast standard con l'esito.
- Consent.razor: sostituito l'alert inline cfg-alert-error con il toast standard cfg-toast--floating (varianti --error e --success, come ShopEdit). Successo -> toast verde L["common_saved"]="Salvato" per ~900ms poi redirect; errore -> toast rosso. Riuso common_saved (gia' in 10 lingue), niente nuove chiavi.
- WASM (Puntify.App): live su CAT dopo rebuild (0 errori); in prod col deploy app.

## [~09:10] Pannello Nemi: 3 richieste (6352/6353/6354) — FATTO
- 6352 Contesto pagina: NemiSendAsync/endpoint/SendAsync ora accettano pageContext; NemiChatService lo inietta nel system prompt (## CONTESTO UI CORRENTE). Fab CurrentPageLabel() mappa la route merchant a nome leggibile (Panoramica, Menu, Prenotazioni, Cassa, Dispositivi, ...). Telegram passa null. Interfaccia+impl+controller+2 caller aggiornati.
- 6353 ESC: overlay aiab-overlay con tabindex=-1 + @onkeydown=OnPanelKeyDown -> _open=false su Escape (input gia' focussato a OnOpen -> keydown bolla). Pattern Blazor nativo (no global handler condiviso).
- 6354 Icona nuova conversazione: da griglia incomprensibile a square-pen (foglio+matita, compose). Verificato render a confronto.
- App+Server compilano (0 CS errori, 200). Live CAT dopo rebuild; prod col deploy.

## [~09:12] Pagina coda: label aperta/chiusa colorata (6355) — FATTO
- QueueList.razor riga 250: wrap della label in <span> con color var(--cfg-success) se IsOpen, var(--cfg-danger) se chiusa, font-weight 600. App compila (200, 0 errori). Live CAT dopo rebuild.

## [~09:18] Notifica Telegram creazione coda (6359) — FATTO
- Come per i premi: app fires /api/admin/events/queue-created alla creazione coda.
- Server: QueueCreatedRequest (AdminEventModels) + AdminMonitorHelper.NotifyQueueCreatedAsync (msg HTML: Coda/Negozio/Modalita'/Negoziante) + endpoint AdminEventsController.QueueCreated.
- App: AdminEventService.NotifyQueueCreatedAsync + QueueEdit.Save() ramo create -> FireQueueCreatedAsync (shop.BusinessName + CurrentUser.Email). Solo su create, non edit.
- Stesso canale/bot dei premi (_telegram.SendMessageAsync). Compila server+app 0 errori.

## [~09:20] Totem coda: layout tasti centrati/responsive (6360) — FATTO
- CodaNegozioDisplay.razor .nt-grid: da grid 2col fissa a flex-wrap + justify-content:center + align-content:center. .nt-pill flex 1 1 clamp(300,38vw,480) max 560. Mobile: colonna (lista) centrata.
- Risultato (mock verificato): 1 tasto centrato; 3 tasti = 2 centrati + orfano centrato sotto; molti = matrice; stretto = lista.

## [~09:25] Totem: tasti uniformi (6365) + diagnosi suono/Firebase (6361)
- 6365: .nt-pill flex 0 1 clamp(300,40vw,500) (grow 0 = larghezza uguale) + min-height clamp(150,24vh,260) = altezza uniforme. Verificato su pagina CAT reale (caffe-santeustachio): 3 tasti identici, orfano centrato.
- 6361 diagnosi (riportata, in attesa OK): suono biglietto funziona solo foreground+audio sbloccato (limite browser, non bug). Push "tocca a te" (NotifyQueueTurnAsync) parte solo per clienti remoti+loggati+con app FCM; la pagina biglietto WEB non registra web-push -> nessuna notifica. Firebase server acceso solo se esiste il file credenziale (prod da verificare). Proposto: aggiungere WEB PUSH alla pagina biglietto (SDK web+SW Vetrina + invio push ai token biglietto). Attendo conferma.
