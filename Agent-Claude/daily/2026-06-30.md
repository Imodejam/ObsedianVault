
## Review log errori live (richiesta Stefano 4636) — 2026-06-30
Esaminati error-*.log (oggi+ieri). Trovati:
1. admin_activity_log insert FALLITO 23× (PGRST204 "Could not find 'Action' column"): il model AdminActivityLog aveva [Column] ma NON [JsonPropertyName] → InsertAsync (System.Text.Json, no naming policy) serializzava PascalCase. → FIX: aggiunto [JsonPropertyName] a tutte le proprietà. Audit log admin ora funziona. (commit)
2. social_reviews crash deserializzazione 6× (topics jsonb array ma model string): → FIX: FlexibleJsonStringConverter (legge array o stringa → string; scrive JSON nativo). (commit)
3. notification_queue/email_queue errori 60× (ModuleHandle token): artefatto dotnet-watch hot-reload, 0 occorrenze oggi dopo restart → NON bug, prod (senza watch) non affetto.
Nessuna modifica DB/esterna. Fix solo codice → vanno in prod col deploy.
