# 2026-05-23

## Sessione: Booking multi-mode + Cruscotto controlli (Fase 1)

### Fatto
Estensione del sistema prenotazione Puntify per supportare 3 modalità coesistenti per PV (Appuntamento/Tavolo/Asporto) + cruscotto controlli moduli.

**DB (migrations applicate da Stefano su Supabase self-hosted CAT)**:
- `20260601_booking_multimode.sql`: aggiunge `shops.booking_modes` (bitfield), estende `shop_operators` con capacity/zone/durata-override, aggiunge `bookings.booking_kind` + `preparation_status`, estende `shop_services` con `service_kind`/`slot_capacity`, aggiunge `booking_settings.table_default_duration_minutes` (90) + `table_turnover_buffer_minutes` (15). Funzione SQL `fn_find_best_table` per allocazione automatica del tavolo più piccolo che fitta.
- `20260601_shop_enabled_features.sql`: aggiunge `shops.enabled_features` bitfield (loyalty/social_studio/nemi/notifications), default 15 (tutti ON per PV esistenti).

**Server (.NET 8)**:
- `Puntify.Server/Controllers/BookingModesController.cs`: GET/PATCH booking-modes + apply-defaults da categoryid
- `Puntify.Server/Controllers/ShopFeaturesController.cs`: GET/PATCH enabled-features
- `Puntify.Server/Controllers/TableBookingController.cs` + `PublicTableBookingController`: availability + reserve via `fn_find_best_table`
- `Puntify.Server/Controllers/TakeawayBookingController.cs` + `PublicTakeawayBookingController`: availability + order (conteggio capacity per slot)
- `Puntify.Server/Controllers/TakeawayBoardController.cs`: bacheca kanban + transizioni status (push al cliente quando 'ready')
- `Puntify.Server/Services/Booking/BookingModesDefaults.cs`: mapping categoryid→booking_modes default
- `Puntify.Server/Services/Notifications/NotificationHelper.cs`: nuovo `NotifyOrderReadyAsync`
- `Puntify.Server/Data/SupabaseClient.cs`: estesi con `InvokeRpcAsync<T>` e `CountAsync`

**Modelli (Punto.Shared)**:
- `BookingModels.cs`: BookingService + ServiceKind/SlotCapacity, BookingOperator + Capacity/CapacityMin/Zone/IsCombinable/DefaultDurationMinutes/TurnoverBufferMinutes, BookingEntry + BookingKind/PreparationStatus, BookingSettings + TableDefault/TurnoverBuffer. Costanti BookingKinds/PreparationStatuses/OperatorTypes/ServiceKinds + enum BookingModesFlags/ShopFeaturesFlags
- `Shop.cs`: + BookingModes/EnabledFeatures + helper Has* + BookingModesCount
- `BookingDtos.cs`: MerchantPublicDto + Id/BookingModes/Has*, ServicePublicDto + ServiceKind/SlotCapacity/IsActive

**App merchant (Blazor WASM)**:
- `Pages/Merchant/MerchantServices.razor`: refactor in 2 tab (Servizi prenotabili / Funzionalità Puntify)
- `Pages/Merchant/PuntifyFeaturesTab.razor` (nuovo): cruscotto controlli con 3+4 toggle
- `Pages/Merchant/BookingHub.razor`: tab condizionali "Tavoli", "Finestre asporto", "Bacheca ordini" basate su booking_modes
- `Pages/Merchant/Booking/TablesManager.razor` (nuovo): CRUD tavoli con capacity/zone/override
- `Pages/Merchant/Booking/TakeawayWindows.razor` (nuovo): CRUD finestre asporto con slot_capacity
- `Pages/Merchant/Booking/TakeawayBoard.razor` (nuovo): kanban 4 colonne con refresh 30s
- `Services/ShopFeaturesApiService.cs` (nuovo), `Services/TakeawayApiService.cs` (nuovo)

**Vetrina (Blazor SSR)**:
- `Components/Booking/PublicBookingFlow.razor`: aggiunto branch ModeStep + QuickTable + QuickTakeaway prima del flow appuntamenti tradizionale
- `Components/Booking/ModeStep.razor` (nuovo): selettore 3 card
- `Components/Booking/QuickTableBooking.razor` (nuovo): form compatto (persone/data/nome/...) → POST table/reserve
- `Components/Booking/QuickTakeawayBooking.razor` (nuovo): form con selettore finestra + slot ritiro → POST takeaway/order
- `Services/BookingPublicService.cs`: nuovi metodi ReserveTableAsync/OrderTakeawayAsync
- `wwwroot/css/booking.css`: + CSS book-mode-card, book-form, book-success

### Verifica end-to-end
- Migration OK su DB (verifica via REST API columns)
- GET /api/shops/{id}/booking-modes → 200 con suggested_from_category corretto
- GET /api/shops/{id}/enabled-features → 200 con flag default 15
- RPC fn_find_best_table → risponde (null per shop senza tavoli)

### Aperto
- **Wiki**: `/home/progetti/obsidian-vault/wiki/projects/puntify.md` è root-owned, non sono riuscito ad appenderci la sezione Booking multi-mode. Stefano può applicare il diff manualmente o il sync script lo gestisce automaticamente.
- **Fase 3 (AI)**: piano in `/home/claudebot/.claude/plans/voglio-estendere-le-prenotazioni-enchanted-sunset.md`. Pendiente conferma su provider image gen (OpenAI gpt-image-1 vs Replicate) prima di iniziare.
- Bucket `dishimages-cat` su MinIO ancora da creare (la chiave puntify-api non ha CreateBucket; serve admin key/console — Stefano segnala che paperclip NON è lo storage, è altro).
- Stampa scontrini cucina (out of scope iniziale).

## Fase 2: Menu digitale (stessa sessione)

### Fatto
- Migration `20260602_menu_schema.sql`: shop_menus + shop_menu_sections + shop_menu_dishes + dish_options + option_values + dish_pairings + booking_order_items. Trigger `fn_check_menu_overlap` (BEFORE INSERT/UPDATE) che blocca due menu attivi sovrapposti su (weekday, time, date range). Indici. RLS policies merchant-write/public-read.
- Modelli C# `Punto.Shared/Models/Menu/MenuModels.cs`: ShopMenu/Section/Dish + DishOption/Value + Pairing + BookingOrderItem. Costanti Allergens (EU14) + DietaryTags + PhotoSources + PairingSources + SelectionModes + WeekdayMask helpers.
- DTOs: ServicePublicDto + ServiceKind/SlotCapacity; MerchantPublicDto + Id/BookingModes.
- Server-side:
  - `Controllers/MenuController.cs` CRUD completo menu/sezioni/dishes/options/values/pairings + Publish/Unpublish/Duplicate (copia con sezioni+dish, parte come bozza).
  - `Controllers/PublicMenuController.cs` GET /api/public/menu/{slug}?ts&lang con risoluzione menu attivo per timestamp.
  - `Services/Menu/MenuValidator.cs`: replica logica trigger SQL lato app per restituire conflict_menu_name user-friendly.
  - `Services/Menu/PairingAnalyticsBackgroundService.cs`: job ogni 24h, lookback 90gg, calcola co-occorrenze dish_a-dish_b per shop, normalizza score, upsert in shop_menu_dish_pairings con source='auto_analytics' (non sovrascrive pairings manuali/AI).
  - `Data/SupabaseClient.cs`: estesi con `InvokeRpcAsync<T>` (RPC con return value) e `CountAsync` (count via Content-Range header).
- App merchant:
  - `Services/MenuApiService.cs`.
  - `Pages/Merchant/Menu/MenuList.razor` (lista + badge "Attivo ora" + Duplica/Modifica/Pubblica/Pausa + crea nuovo).
  - `Pages/Merchant/Menu/MenuEditor.razor` (dati menu con weekday picker LMMGVSD + time range + date range; sezioni e piatti inline).
  - `Pages/Merchant/Menu/DishEditor.razor` modale (nome, descrizione, prezzo, foto URL, ingredienti, allergeni EU14, dietary tags, disponibilità).
  - Icona "Menu" in MerchantHome (accanto a Servizi).
  - CSS booking.css ampliato con me-*, cfg-modal-*.
- Vetrina:
  - `Components/Menu/MenuView.razor` con sticky nav sezioni, card piatto (foto, prezzo, badge allergeni/diete), risoluzione translations JSONB per lingua.

### Aperto Fase 2
- Bucket `dishimages-cat` non creato (manca admin key per CreateBucket via S3 API). Per ora le foto si possono solo passare via URL nel DishEditor.
- Integrazione MenuView dentro QuickTakeawayBooking (carrello con +/-) non fatta — l'asporto di Fase 1 accetta ancora note libere. Da fare in F2.1 se serve.
- Drag&drop sezioni/piatti non implementato — per ora solo sort_order via API.
- Traduzioni multi-lingua: schema pronto (translations JSONB) ma UI editor per inserire traduzioni nel DishEditor non c'è — solo lettura nel MenuView lato cliente. Da fare.

### File principali Fase 2
- `/home/progetti/puntify/docs/DB Migrations/20260602_menu_schema.sql` (applicata da Stefano)
- `/home/progetti/puntify/Punto.Shared/Models/Menu/MenuModels.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Controllers/MenuController.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Menu/MenuValidator.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Menu/PairingAnalyticsBackgroundService.cs` (nuovo)
- `/home/progetti/puntify/Puntify.App/Pages/Merchant/Menu/{MenuList,MenuEditor,DishEditor}.razor` (nuovi)
- `/home/progetti/puntify/Puntify.Vetrina/Components/Menu/MenuView.razor` (nuovo)
- `/home/progetti/puntify/Puntify.App/Services/MenuApiService.cs` (nuovo)

## Fase 3: AI assist menu (stessa sessione)

### Fatto
- **OCR menu cartaceo**: `Puntify.Server/Services/Ocr/IMenuOcrService.cs` + `ClaudeMenuOcrService.cs` (Claude Sonnet 4.6 default, override via `Anthropic:OcrMenuModel`). Prompt strutturato chiede JSON con sezioni e piatti, allergeni inferiti da ingredienti (EU14), prezzi solo se chiaramente leggibili. Endpoint `POST /api/menu/ocr` ritorna draft NON persistito.
- **UI merchant OCR**: bottone "📷 Scatta menu cartaceo" su `MenuList.razor` → file picker → upload → modal anteprima sezioni/piatti → "Crea menu" salva tutto come bozza e apre l'editor. Photo_source dei dish creati = 'ocr'.
- **Nemi tools menu/asporto**: 4 nuovi tool registrati in `NemiToolRegistry`:
  - `list_menus` → elenco menu del PV
  - `add_dish_to_menu` (menu_name+section_name+dish_name+price+description+allergens+ingredients) — crea sezione se manca
  - `update_dish` (dish_name+new_name?/new_price?/new_description?/set_available?) — match case-insensitive con fallback contains
  - `list_pending_orders` — ordini takeaway in received/in_preparation/ready

### Aperto Fase 3
- **Image gen foto piatti**: provider non scelto da Stefano (OpenAI gpt-image-1 ~$0.04/img vs Replicate Flux ~$0.003). Quando decide, aggiungo `IImageGenerator` + `OpenAIImageGenerator.cs`/`ReplicateImageGenerator.cs` + endpoint `POST /api/menu/dishes/{id}/generate-photo` + Nemi tool `generate_dish_photo`.
- **Bucket dishimages-cat** non ancora creato su MinIO (chiave puntify-api non ha CreateBucket via S3 API; serve admin console o key admin — paperclip NON è lo storage).

### File principali Fase 3
- `/home/progetti/puntify/Puntify.Server/Services/Ocr/IMenuOcrService.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Ocr/ClaudeMenuOcrService.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Controllers/MenuController.cs` (esteso con endpoint /ocr)
- `/home/progetti/puntify/Puntify.Server/Services/Nemi/Tools/ListMenusTool.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Nemi/Tools/AddDishTool.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Nemi/Tools/UpdateDishTool.cs` (nuovo)
- `/home/progetti/puntify/Puntify.Server/Services/Nemi/Tools/ListPendingOrdersTool.cs` (nuovo)
- `/home/progetti/puntify/Puntify.App/Pages/Merchant/Menu/MenuList.razor` (esteso con OCR upload + preview modal)
- `/home/progetti/puntify/Puntify.App/Services/MenuApiService.cs` (esteso con `OcrMenuAsync`)

### Note tecniche
- Schema `puntify` (non public).
- Tavolo = `shop_operators.type='table'` con `capacity` valorizzato (constraint DB).
- Finestra asporto = `shop_services.service_kind='takeaway_window'` con `slot_capacity`.
- Allocazione tavoli ordina per capacity ASC + anti-spreco via capacity_min.
- Push "ordine pronto" parte solo se booking ha customer_id (cliente registrato); guest sono silenti.
