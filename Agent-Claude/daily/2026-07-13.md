- NUOVO slug città: delegato (SlugHelper + JS city + uniqueness in AddShop). Rating shop: vetrina già media shop → chiesto a Stefano quale pagina mostra il voto Puntify.

## Rating Google shop + slug città
- RATING: causa trovata = SoftwareApplication aggregateRating 4.9/185 in App.razor head globale → Google lo attribuiva ai negozi. Spostato su Home. Pagine negozio ora solo LocalBusiness proprio. Verificato. Serve re-crawl.
- SLUG: implementato {nome}-{città} (city da Google Places), uniqueness. 5 slug null (vecchi) → chiesto a Stefano se backfillare.

## Slug backfill + NegozioDetail contatti/maps + logo Google
- Backfill 5 slug null fatto (SQL su ops-postgres, no reload PostgREST). 0 null.
- NegozioDetail: delegato indirizzi→Maps, tasto Chiama (primo), Contatti+email, + logo/image nei dati strutturati (rich result). Favicon per-dominio: spiegato a Stefano non è per-negozio.
- FATTO NegozioDetail: maps links + Chiama + Contatti/email + logo JSON-LD. Verificato. Serve re-crawl Google per rating+logo. Collaudo, PROD no.
- FATTO: pannello 'I tuoi ordini' ancorato bordo destro sopra footer (menu page). Compare solo con ordini salvati.
- Q&A: pannello Da incassare = Dashboard→Nuovo ordine (Cassa). Solo con ordini awaiting_payment. Offerto badge visibile su Dashboard.
- FIX UX asporto (Stefano: non intuitivo): ordini da pagare visibili in Dashboard senza aprire Nuovo ordine; post-pagato → In cucina. Rimosso pannello POS. Delegato.
- FATTO: ordini da pagare in Dashboard (no Nuovo ordine), Segna pagato→In cucina, POS panel rimosso. Verificato. Serve ordine di prova o inject test.

## POS rotta propria + dettaglio ordine
- POS: MerchantPos ora pagina /merchant/{id}/pos, back torna a dashboard. Fatto+build ok.
- Dettaglio ordine su click (tab Ordini): delegato (modale con tutti i campi + azione avanza, stopPropagation su bottone).
- Routine 9:00: Vetrina ok (10 lingue). Menu realtime bloccato (credito). Report a Stefano. + segnalato bug duplicati #A01 (in analisi).
- Bug #A01 duplicati: codici giornalieri riusati + localStorage vecchio. Proposto fix (atomico + lista cliente data/nascondi vecchi). Attendo scelta Stefano (giornalieri vs unici).
- FATTO dettaglio ordine su click (Dashboard tab Ordini). Verificato+build. Collaudo.

## Numero ordine atomico + QA + accordion + coperti
- Ordine numero atomico (RPC SECURITY DEFINER) FATTO+verificato. Numeri corti.
- Regressione QA settimanale PASS. Bug minore: /it/nemi video hero 404.
- Accordion Cassa più alti FATTO.
- Coperti al tavolo (cover_charge menu default 2 + covers order + totale) delegato; flusso: tavolo auto-avanza, tavolo/coperti editabili sempre.

## Coperti al tavolo (cover charge) — COMPLETATO
- DB: `docs/DB Migrations/2026-07-13_cover_charge.sql` applicata su puntify_cat; `shops.cover_charge numeric(8,2) not null default 2.00`, `menu_public_orders.covers int null`; PostgREST (postgrest-puntify-cat) riavviato.
- Modelli: `Shop.CoverCharge` (default 2m), `MenuPublicOrder.Covers` (int?).
- MenuEditor.razor: campo "Coperto (€ a persona)" (number step 0.50 min 0) bound a `_shop.CoverCharge`, salvato via `Supabase.UpdateShop` in SaveMenu.
- MerchantPos.razor: carica shop.CoverCharge; select tavolo → auto-avanza ai piatti (rimosso bottone Avanti dine_in); barra riepilogo `Tavolo · Coperti` con +/- (min 1, max 99) e cambio tavolo, visibile su step piatti e riepilogo; riga "Coperto (n × €)" nel totale; payload `covers`.
- Server MenuController: `MerchantCreateOrderRequest.Covers`; per dine_in clamp(1..99) + `total += covers × shop.cover_charge` (valore SEMPRE dal DB); set `order.Covers`. SubmitOrder pubblico invariato.
- Resx: menueditor_cover_charge, pos_covers, pos_cover_charge in tutte le 10 lingue.
- Build Shared/Server/App OK; puntify-server riavviato (200). NON committato.
- Coperti al tavolo FATTO+verificato (config menu, barra editabile, auto-advance, totale server). Collaudo.
- Cassa: draft localStorage + step routed (riepilogo path proprio) + coperti nel riepilogo. Delegato (resume).

## POS follow-up (draft localStorage + routed steps + covers line) — COMPLETATO
- MerchantPos.razor: seconda rotta `@page "/merchant/{ShopId}/pos/{Step}"` + `[Parameter] string? Step`. Mapping tavolo/menu/riepilogo↔_step; transizioni (SelectMode/SelectTable/GoToMenu/GoToConfirm/GoBack/ChangeTable) navigano via NavigateToRoute; OnParametersSetAsync→SyncStepFromRoute con guardie (riepilogo con carrello vuoto→base; menu senza tavolo/indirizzo→step1). Step 4 risultato resta interno (snapshot _resultTotal/_resultModeLabel).
- Draft localStorage: key `puntify_pos_draft_{ShopId}`, System.Text.Json via IJSRuntime (setItem/getItem/removeItem). Save su ogni mutazione (mode/table/covers/cart/address/note/step, @bind:after su input); restore in OnInitializedAsync prima del render (+ redirect una-tantum allo step salvato entrando da /pos); clear su ordine creato e su "Ricomincia" (StartOverAsync/ResetState).
- Riepilogo: riga "Coperti: N" sotto il tavolo (solo dine_in, key pos_covers).
- Nuova key resx pos_start_over in 10 lingue. Build Puntify.App OK (dotnet watch, no restart).
- FATTO Cassa: draft localStorage + step routed + coperti riepilogo. Verificato+build. Collaudo.
- Fix /account: path CSS + foto legacy cloud morta azzerata + onerror. 1 shop logo/cover stesso host: chiesto a Stefano.
- FATTO Dashboard: tab bar a sinistra + Nuovo ordine nel footer fisso. Build ok. Collaudo.
- Audit Dashboard vs linee guida: conforme (tab-in-url, footer, tab sinistra, responsive). Fix toolbar vuota condizionale. Build ok.
- Ordini board vuota = cutoff 2h (by design) su ordini vecchi delivered. Proposto: cutoff solo per chiusi, aperti sempre visibili. Attendo conferma.
- Dashboard: tab Ordini solo aperti (no cutoff) + tab Archivio chiusi (7gg). Delegato.
- FATTO Dashboard Ordini/Archivio: aperti sempre visibili + tab Archivio chiusi 7gg. Verificato+build. Collaudo.
- Dashboard: search in header top-right + Ordini/Archivio full-width. Build ok.
- Dashboard full-width tutte le tab. Sezione già 'Orders'/'Ordini' (label+title); chiesto se rinominare URL /dashboard→/orders.
- Ricerca Stripe-style (icona+clear+focus ring) nell'header Dashboard. Offerto shortcut / e ⌘K palette.
- Stato ordine non tracciato (no history). Proposto logging eventi per stats future. Attendo ok.
- NUOVA feature grossa: tab Cassa POS (2 pannelli, griglia prodotti, conto, apertura da ordine/tavolo, Azioni popup). Proposto fasi, chiesto conferma scope. Attendo ok.
- Cassa POS (dettaglio 3ª foto): conto con PORTATE (Portata 1/2), note per piatto, imposte+totale, Invia (cucina, conteggi reparto), Pagamento. Fase 1 include portate+note+invia+coperti; Fase 2 pagamento/fattura/suddividi/trasferisci/annulla/listino. Attendo conferma scope per partire.
- Cassa POS Fase 1 delegata (build corposo, background). Foundation: 2 pannelli, portate, apertura da ordine/tavolo, Invia, Coperti; backend Course+update endpoint. Fase 2: pagamento/fattura/suddividi/trasferisci/annulla.
- Cassa: Coperti = modal tastierino 'Numero di ospiti' (display + €/Ospite + keypad + Conferma/Abbandona), non stepper — rifinitura inviata al subagent. Trasferisci/Unisci = cambia/unisci tavoli (Fase 2).
- Salvate tutte le schermate Cassa (coperti keypad, pagamento metodi+tastierino+resto+fattura+quota ospite, trasferisci/unisci). Fase 1 in build; Fase 2 pianificata sul disegno di Stefano.
- Catturato intero flusso pagamento Cassa (split metodi, rimanente/resto, valida, fattura, post-pagamento stampa/invia ricevuta) per Fase 2. Fase 1 in build.
- CASSA Fase 1 FATTA+verificata (build ok). Provabile su collaudo. Fase 2 (pagamento+azioni) sul disegno di Stefano dopo suo feedback.
- Fase 2 build completo (no test intermedi per scelta Stefano). 2a pagamento in corso; poi 2b azioni. Review alla fine.
- Fase 2a pagamento FATTA+verificata. 2b azioni avanzate (5) delegata; Trasferisci piatto+Suddividi differite. Review finale con Stefano.
- Cassa Fase 2 COMPLETA (pagamento + azioni) verificata+build. Restano Trasferisci piatto+Suddividi. Presentata a Stefano per review.
- Risorse: mappa full-screen+auto-fit, filtro sala overlay top-right, search no-rosso-focus + flush dx. Build ok.
- Fix header search a destra: header wide flex + margin-left auto (grid 1fr non espandeva). Build ok.
- Cassa: Listino=selettore menu, click-portata seleziona, ESC azioni, rimozione portata (elimina/sposta). Proposto restyle Stripe come passata dedicata.
- Nuovo ordine wizard tipologia + info per tipo + accredito punti fedeltà post-pagamento. Delegato. Con latitudine (Stefano).
- Cassa header: mostrare 'Tav. N' (prefisso cassa_table_prefix localizzato 10 lingue) invece del solo numero. Aggiunto al subagent wizard (stesso file).
