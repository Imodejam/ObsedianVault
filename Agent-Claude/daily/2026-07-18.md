
## Nemi App — stato waiting_user visibile (audit #1+#14)
- ShopAiController active-task: aggiunto campo additivo WaitingUser (status waiting_user, finestra 12h su updated_at)
- AssistantAi.razor + AiAssistantFab.razor: banner ambra 'Nemi e' in pausa' al posto dello spinner; polling 3s→15s in pausa; resume polling all'apertura pagina/pannello (multi-tab); fix reload forzato del Fab durante pausa
- resx: nemi_waiting_user_banner in default+en/de/es/fr/nl/pl/ro/ru/uk
- Build App+Server 0 errori. Nessun commit (richiesto).

## Nemi menu-publish reminder (opzione B) — 2026-07-18
- NemiChatService: tracking scritture menu RIUSCITE (IsMenuWriteTool+IsSuccessfulWriteOutput) in ExecuteSimple e ExecuteRemainingSteps; promemoria appeso solo a fine turno/completamento, mai su pausa/fail; dedupe se la reply parla gia di salvare/pubblicare il menu.
- NemiToolGuards: MenuPublishNote (campo note nei JSON success degli 8 tool menu-write), set esplicito nomi tool.
- Prompt BuildContext: regola PUBBLICAZIONE MENU.
- Scelta: niente check published_data (query extra + menu non identificabile dal wrapper) -> promemoria sempre dopo scrittura riuscita.
- Test: harness reflection 33/33 (round semplice + multi-step con pausa/fail/already_exists/riparazione). Build 0 err, puntify-server attivo, /api/menu 401.

## Elimina-code: fix priorita' peso + conteggio confermati operatore — 2026-07-18
- FLAW A (critico): RPC puntify.queue_call_next_across ordinava per created_at ASC (FIFO puro), ignorando queues.weight. Nuova migration docs/DB Migrations/20260718_queue_weight_priority.sql: CREATE OR REPLACE, cambiato SOLO l'ORDER BY -> (minuti attesa)*COALESCE(weight,1) DESC, created_at ASC. Tutto il resto identico (firma, FOR UPDATE SKIP LOCKED, filtri source/checked_in, counter, return). Proporzionale: no starvation (attesa cresce illimitata), pesi uguali => FIFO.
- Applicata su CAT (puntify_cat) + testata in transazione con ROLLBACK: high-weight recente batte low-weight; vecchio low-weight (60min) batte fresh high-weight (proportional fairness); pesi=1 => FIFO esatto; coda vuota => 0 righe; remoto non-checkin escluso. Nessun dato lasciato (verificato leaked=0, pesi ripristinati).
- FLAW B (alto): OperatorController.Context query "waiting" mancava &confirmed=eq.true -> conteggio/prossimo numero includeva pending remoti non chiamabili. Aggiunto filtro (coerente con QueueController.Tickets riga 266 e con RPC queue_call_next che gia' filtra confirmed). RPC queue_call_next_across NON filtrava confirmed ma usa la guardia source/checked_in (fuori scope, non toccata).
- Build server 0 errori, restart puntify-server, /api/menu 401. Nessun commit.
