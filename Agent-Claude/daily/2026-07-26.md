# 2026-07-26

## Task Puntify.App iOS (collaudo/CAT)

### A — Accedi con Apple (solo iOS)
- SupabaseService.cs: nuovo GetAppleSignInUrl(bool nativeApp) (pattern identico a GetGoogleSignInUrl, redirect it.puntify.app://auth-callback in nativo / {base}/auth/callback in web); rimosso vecchio SignInWithApple() incompleto.
- native-auth.js: aggiunto window.puntifyPlatform.isIOS().
- Login.razor: pulsante btn-apple (SVG Apple + login_apple) mostrato solo se _isIos (rilevato in OnAfterRenderAsync); handler SignInWithApple mirror di SignInWithGoogle (nativo -> puntifyNativeAuth.start("apple",url), web -> redirectTo).
- resx: chiave login_apple in tutte 10 le lingue ("Continua con Apple"...).
- Verifica headless: iOS UA -> bottone Apple visibile; desktop UA -> assente. Screenshot nello scratchpad.

### B — Blocco acquisti digitali in app nativa iOS (compliance Apple)
- Gating su puntifyNativeAuth.isNative() (true solo in WKWebView). Se nativo -> modale cfg-modal "acquista/gestisci da browser" (native_purchase_blocked_*), niente checkout.
- Punti bloccati: MerchantBilling (update payment method), MarketingCredits (tile pacchetti), AssistantAi (OpenBuy + OnPay minuti Nemi Voce).
- NON bloccati: cassa/pagamenti clienti (beni/servizi fisici), Safari/PWA.
- resx: native_purchase_blocked_title/subscription/credits + common_ok in 10 lingue.

### Stato / da confermare con Stefano
- GoTrue CAT: provider Apple non configurato (solo Google) -> il bottone c'e' ma il flow dara' errore finche' non si aggiungono GOTRUE_EXTERNAL_APPLE_* + credenziali Apple Developer. NON e' un blocco di sviluppo.
- Billing/Credits sono ancora mock (Stripe FASE 2): il blocco iOS e' pronto ma i checkout reali non esistono ancora.
