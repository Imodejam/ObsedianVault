# 2026-07-26

## Task Puntify.App iOS (collaudo/CAT)

### A — Accedi con Apple (solo iOS)
- SupabaseService.cs: nuovo GetAppleSignInUrl(bool nativeApp) (pattern identico a GetGoogleSignInUrl, redirect it.puntify.app://auth-callback in nativo / {base}/auth/callback in web); rimosso vecchio SignInWithApple() incompleto.
- native-auth.js: aggiunto window.puntifyPlatform.isIOS().
- Login.razor: pulsante btn-apple (SVG Apple + login_apple) mostrato solo se _isIos (rilevato in OnAfterRenderAsync); handler SignInWithApple mirror di SignInWithGoogle (nativo -> puntifyNativeAuth.start("apple",url), web -> redirectTo).
- resx: chiave login_apple in tutte 10 le lingue ("Continua con Apple"...).
- Verifica headless: iOS UA -> bottone Apple visibile; desktop UA -> assente. Screenshot nello scratchpad.

### B — Blocco acquisti digitali in app nativa iOS (compliance Apple)
- Gating su puntifyNativeAuth.isNative() (true solo in WKWebView). Se nativo -> modale cfg-modal "acquista/gestisci da browser" (native_purchase_blocked_*), niente checkout.
- Punti bloccati: MerchantBilling (update payment method), MarketingCredits (tile pacchetti), AssistantAi (OpenBuy + OnPay minuti Nemi Voce).
- NON bloccati: cassa/pagamenti clienti (beni/servizi fisici), Safari/PWA.
- resx: native_purchase_blocked_title/subscription/credits + common_ok in 10 lingue.

### Stato / da confermare con Stefano
- GoTrue CAT: provider Apple non configurato (solo Google) -> il bottone c'e' ma il flow dara' errore finche' non si aggiungono GOTRUE_EXTERNAL_APPLE_* + credenziali Apple Developer. NON e' un blocco di sviluppo.
- Billing/Credits sono ancora mock (Stripe FASE 2): il blocco iOS e' pronto ma i checkout reali non esistono ancora.

## Review pre-commit Puntify (73 file)
- Segreti: NESSUNO nel diff/untracked; appsettings con chiavi restano ignorati; tracciati solo .template con placeholder. Niente artefatti/bin/obj/log tra i file da committare.
- BLOCCANTI funzionali: (1) MenuController.UpdateItems non scrive course_status -> portate nuove in limbo (KDS le vede held, fire-course 409 course_not_held); (2) portate >=2 messe held anche su takeaway/delivery ma fire-course rifiuta non dine_in -> item persi; (3) Vetrina MerchantMenuPreview CartTotal somma netto e il flag prices_vat_inclusive non e' esposto -> totale cliente != totale ordine con catalogo IVA-esclusa.
- Sicurezza: PublicTableBookingController senza EnableRateLimiting + day-availability senza cap su Slots = amplificazione N-RPC anonima. Nessuna SQLi/XSS/open redirect/IDOR nel diff (fire-course usa RequireOwnerAsync; isolamento tenant sui dish verificato).
- Migration: UPDATE product_catalog SET tags='{}' senza WHERE (distruttivo a ogni run); 'te' non-ASCII dentro una regex eseguita; le 4 migration hanno NOTIFY pgrst.
- Deploy: applicare migration PRIMA del codice (course_status/prices_vat_inclusive finiscono in insert/select: PGRST204 e 500 su creazione ordini).
- Non ho committato ne modificato codice.

## Fix 3 bug di review menu/ordini (CAT, nessun commit)
- File: Punto.Shared/Models/Menu/MenuPublicOrder.cs, Puntify.Server/Controllers/MenuController.cs,
  Puntify.Vetrina/Pages/MerchantMenuPreview.razor, Puntify.App/Pages/Merchant/Dashboard.razor.
- Scelta BUG A: NON servire prezzi lordi dal server (il netto resta la fonte per lo scorporo IVA/fiscale):
  la Vetrina costruisce un MenuPublicOrderItem e chiama GrossAmount, lo STESSO metodo del server ->
  impossibile divergere. prices_vat_inclusive/vat_rate/default_vat_rate erano gia' esposti dall'endpoint
  pubblico (ShopMenu serializzato interamente): il bug era solo il calcolo client.
- Prezzi mostrati in Vetrina ora sono sempre FINALI (IVA inclusa): card/dettaglio/formati/CTA.
- Nota per Stefano: con arrotondamento PER RIGA, 2 pezzi da 12,15@10% fanno 26,73 (non 2x13,37=26,74):
  la riga e' l'importo autoritativo (client == server == DB); il carrello mostra il totale riga, non l'unitario.
- CassaLine.Gross (App) allineato allo stesso arrotondamento per non divergere dal totale registrato.
- Test reali: 4 ordini pubblici + 3 ordini POS su puntify_cat, poi cancellati; menu/prezzi/snapshot ripristinati
  identici (published_data 40810 char restore verificato).
- Puntify.Tests: 33 fail preesistenti, TUTTI in SocialStudioTests (403 Forbidden, auth non configurata) - non correlati.
  Puntify.Receipts.Tests: 84/84 verdi (coprono GrossAmount via BillReceiptMapper).
