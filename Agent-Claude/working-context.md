# Working context

## Sessione: traduzioni Vetrina 10 lingue + check giornaliero
Data: 2026-07-05

### COMPLETATO: ri-traduzione settori (ondata 2) fr/en/zh — 10 lingue complete
Contesto: le 78 case study settori erano state ricalcolate col modello Nemi Voce.
I _Case_ keys in es/pt/ar/bn/hi/uk erano già rifatti; fr/en/zh avevano valori STALE
(versione vecchia, non allineata al ricalcolo).

Stato:
- fr: MERGE FATTO (2808 case keys sostituiti in SharedResource.fr.resx)
- en: MERGE FATTO (via build_en.py, 654 uniq → 2808 keys) 
- zh: MERGE FATTO. Build OK, service riavviato, verifica live OK. Commit 2feaa78.
- Script merge: scratchpad/trad/merge_resx.py (caseKey→it→id→uniq_lang), idempotente

### Prossimi passi
1. Attendere zh subagent → python3 merge_resx.py zh
2. stop/build/start puntify-vetrina.service (neutro embedded serve restart processo)
3. Verificare 1 settore in fr/en/zh via browser
4. Commit "traduzioni settori ondata 2 (fr/en/zh) allineate a Nemi Voce" (NO attribuzione Claude)
5. Report Telegram a Stefano: sito completo nelle 10 lingue
6. Deploy PROD: SOLO dopo OK esplicito Stefano

### Check errori 8:00 (FATTO)
0 bug nuovi (tutti [CLIENT/vetrina] circuito, amplificati dai restart). JIRA PNT aperti=0.
Report Telegram inviati (id 5241 errori, 5242 jira).

## 2026-07-05 batch richieste — COMPLETO
- home 'italiane' rimosso, claim mercato IT solo-IT, categorie 65 localizzate, pagine coda localizzate, legali già IT/EN.
- Commits: 354971a, 24fd22e, bca4cc1, fff10f7 (+ 2feaa78 settori). NON pushati prod (attende OK).

## 2026-07-05 Nemi femminile + due anime (in corso)
- Nemi = femminile (una Lei, assistente digitale). Memoria: feedback_nemi_female.
- IT corretto: assistente telefonica, Nemi sempre pronta, Nemi è la tua assistente, +extra (Un'assistente completa, falla, lei esegue, risponde da sola, raccoglie lei). Commit 2b049e3 (batch1) + extra da committare con lingue.
- Altre lingue: 2 subagent genere in corso (17 keys + 7 keys) → applicare da nemi_vals_fixed.json / nemi_vals2_fixed.json.
- TODO "due anime": pagina Nemi framare esplicito Bot (gestione chat/Telegram, incluso) vs Voce (telefonate, a consumo). FAQ Nemi_Voce_Faq_Digital già ok. Reframe Nemi_Areas_Title/Desc + HeroDesc, poi tradurre 10 lingue.

## 2026-07-05 Nemi COMPLETO
- Femminile: a87cfd0. Due anime: 3590c77. Verificato it/en/es/fr/ar/uk.
- Da valutare (segnalati a Stefano): (1) stringhe Nemi PT/ES con testo spagnolo errato (ritradurre), (2) JSON-LD pagina Nemi hardcoded IT (localizzare).
- Commit oggi non pushati prod: 354971a,24fd22e,bca4cc1,fff10f7,2feaa78,2b049e3,a87cfd0,3590c77.

## Routine ricorrenti attive (cron di sessione)
- Giornaliera 8:00 IT: controllo errori (cron 9ec6a90b, 6:03 UTC)
- Giornaliera 9:00 IT: report traduzioni Vetrina (cron 917cdd01, 7:03 UTC)
- SETTIMANALE lun ~9:07 IT: TEST PLAN (cron b3792de9, 7:07 UTC) — agent QA aggiorna testbook (wiki/projects/puntify-testbook.md) + regressione Playwright su tutte le pagine Vetrina/App + servizi + report Telegram. Auto-riarmo incluso (cron sessione scade a 7gg).

## 2026-07-05 PUSH per deploy
- Pushati 36 commit su origin/master (fino a 261885e). Stefano fa il deploy prod.
- Note prod: badge Nemi Bot diventa 🔴 automatico (IsProduction); login Google nativa iOS inerte finché non c'è app nativa + scheme nell'allow-list GoTrue PROD (da fare con OK quando serve); app WASM prod = pipeline LoadAll, utenti potrebbero dover ricaricare (service worker).

## 2026-07-05 (sera) — stato richieste coda + shop
FATTO oggi (commit su master, pushati): FAQ Nemi(4091c27), biglietto+lastissued coda(213d4bb), totem numero inline 'Gastronomia 8'(30e31d3), BUG QR sempre-1 device-vuoto fix RPC+app(6fce46c, migration in docs/DB Migrations DA APPLICARE PROD), esercente gestisce coda(b77a6b7, deploy CAT), selettore lingua totem(9f846db).
IN CORSO: 5298 popup embed pagina cliente /shop → Fase 1 subagent a353eeb71f66800e3 (embed mode + framing + prefill dati + postMessage sulle pagine pubbliche Vetrina, build no-deploy). Poi Fase 2 lato app (ShopDetail popup).
DA VALUTARE con Stefano: campo lingua-default esercente (oggi si usa la lingua del link totem); applicare migration coda in PROD.

## 2026-07-05 (tardo) — coda remoto 5307 in corso
Commit/deploy fatti oggi (pushati master): footer board #B80000(af15163), popup embed Fase1 vetrina(4e37c42)+fix XFO(2a6184a), popup Fase2 app(3d307af, deploy CAT), biglietto squillo+attesa(d915694, server+vetrina IssuedAt).
IN CORSO: 5307 coda remoto FASE A (server+DB) subagent a0e7ab7fa48ab4bc4 — schema remote_limit+confirmed+confirm_token+confirm_expires_at, RPC remoto-pending, endpoint conferma via token email, dedup 1-per-email, limite per coda/giorno, email link, regola turno-perso. Applica migration SOLO CAT, no commit. DA RIVEDERE io (sicurezza: dedup/limite/confirm-token/one-per-email) prima di commit/deploy. Poi FASE B: UI pubblica "Mettiti in coda" (vetrina+app) + notifica in-app.
PENDING PROD: migration coda device-vuoto(2026-07-05_queue_take_ticket_empty_device_fix) + eventuale remote (dopo review). Validazione cacct embed (fase server). 

## 2026-07-05 (notte) — ripresa: diagnosi server + Fase B coda remoto
DIAGNOSI SOFFERENZA SERVER (Stefano chiede perché): 2 cose distinte.
1) Reboot 21:16→21:18 = aggiornamento kernel 6.8.0-124→6.8.0-134 (unattended/host), NON crash. No OOM, no panic.
2) Sofferenza vera: puntify-server dalle 14:50 alle 21:16 ha sparato 2140 errori EAGAIN "Resource temporarily unavailable (api-cat:443)" = saturazione socket/risorse su VPS 8GB carica (build .NET ripetute, VBCSCompiler, molti subagent traduzioni, N dotnet-watch, next, openclaw, concilium, piracity). NON leak codice (SupabaseClient usa IHttpClientFactory ok). NotificationQueueService polling continuo = primo a franare. Ora sano (load 0.2, 3.7GB liberi).
Proposto a Stefano hardening opzionale: backlog/limiti socket + retry/backoff su polling notifiche + swap/limiti memoria servizi.
FASE A coda remoto = CHIUSA: HEAD 155a13d, già pushato origin/master. Migration solo su collaudo, PROD in attesa OK esplicito.
FASE B coda remoto (UI) = FATTA (subagent, in working tree, NON committato/deployato, build OK Server+Vetrina+App). Include: sezione "Mettiti in coda da remoto" Vetrina(CodaTicket.razor)+pagina conferma link email(CodaBiglietto.razor), pill app ShopDetail→embed popup con account, campo remote_limit in QueueEdit, notifiche in-app (Sei in coda/Manca poco/Tocca a te via NotificationHelper+QueueController call-next), nuovo endpoint pubblico read-only shop/{id}/remote, 25 stringhe Vetrina+6 App in 10 lingue.
TRADUZIONE notifiche = FATTA (subagent): meccanismo = EmailStrings.T (stesso dell'email Fase A, NON resx server). Aggiunto ResolveLangByUserIdAsync in NotificationHelper (account?userid→language, fallback it). 7 chiavi × 15 lingue in EmailStrings.cs (it,en,es,fr,pt,zh,ar,hi,bn,de,pl,uk,ro,nl,ru). Build Server OK. Completate anche le chiavi email Fase A che erano solo in 7 lingue.
REVIEW pre-commit COMPLETA (2 subagent). NON COMMITTATO — attende decisioni Stefano sui fix.
FINDINGS security: [HIGH-1] email bombing (endpoint pubblico manda mail a email arbitraria senza auth; dedup 1-per-email aggirabile con gmail +alias/punti; no rate-limit per-email, solo 60/min per IP). [HIGH-2] customer_id (cacct query string) spoofabile → notifiche in-app harassment verso account vittima + ticket iniettato in account altrui; fix = derivare account da JWT, non fidarsi client (impatta approccio embed). [MED] race TOCTOU su remote_limit in confirm (non atomico); confirm_token in query string ?ct= (Referer leak, non azzerato a confirm); no unique constraint dedup + inflazione numerazione. Difese OK: RLS deny-by-default (confirm_token non leakabile anon), token UUIDv4 random, entry_mode server-side, no SQLi, postMessage origin-checked.
FINDINGS correttezza: [MAJOR] notifica "manca poco"/near inaffidabile — near cercato per match esatto number=called+near, ma i pending non confermati creano buchi permanenti nella numerazione → near spesso non scatta (è il senso della coda remota). Fix: trigger per POSIZIONE relativa + flag near_notified. [MINOR] off-by-one testo "mancano N"; URL conferma non localizzato (manca prefisso lingua → pagina IT); near perso per chi entra già sotto soglia. Build tutti OK, i18n completo (7×15 notif, 25 Vetrina, 6 App), flusso in-loco NON rotto (confirmed DEFAULT true), "tocca a te" affidabile.
PROD SAFE nel frattempo: feature inerte finché codice non deployato → nessun abuso possibile ora, c'è tempo per fixare bene.
INCIDENTE COLLAUDO 22:35: app-cat loading perenne — causa dotnet-watch hot-reload corrotto da edit live del subagent (TypeLoadException tipo anonimo→500). Recovery: restart puntify-server. Registrato in mistakes.md + auto-memoria reference_puntify_collaudo_hotreload. Stefano scelto A (lascia finire i fix, poi restart pulito). CURA proposta: dotnet watch --no-hot-reload o worktree isolato.
GOOGLE AUTH PROD (richiesta Stefano 22:43): vuole abilitare login Google in prod. Chiarito: PUNTIFY_URI_ALLOW_LIST non è il blocco (Google non richiede voce dedicata; app.puntify.it/** già copre; opz. /*→/**). Vera checklist (da config CAT funzionante): (1) provider google nel compose/.env prod (GOTRUE_EXTERNAL_GOOGLE_ENABLED/CLIENT_ID/SECRET/REDIRECT_URI=https://<api-prod>/auth/v1/callback); (2) CAUSA#1 = Google Cloud Console authorized redirect URI deve avere https://<api-prod>/auth/v1/callback (cat usa api-cat.puntify.it/auth/v1/callback); (3) ricreare gotrue prod. Chiesto a Stefano output grep DOMAIN_API_PUNTIFY/GOOGLE su prod per valori esatti. NB sed iOS di prima NON rimasto su prod (stringa pulita).
STEFANO OK (msg 5340): fixare tutto il set, strada A per HIGH-2, + commenti nel codice che spiegano ogni fix.
FIX FATTI (subagent ada3f0641b820ffee): tutti e 5 implementati, build 3 progetti 0 errori, NON committato. Migration nuova `2026-07-05_queue_remote_review_fixes.sql` applicata+verificata su collaudo (funzioni queue_confirm_ticket/queue_normalize_email/queue_take_ticket_remote + colonne contact_email_norm/near_notified). Dettagli: FIX1 dedup email normalizzata(gmail alias/punti)+cap 3/h 3/gg tabella persistente queue_remote_email_sends+rate-limiter per-email+advisory lock; FIX2 endpoint AUTENTICATO /api/queue/{qrToken}/ticket/remote (customer_id da JWT), pubblico non accetta più customer_id, app chiama endpoint auth (no cacct); FIX3 near per-posizione+flag near_notified; FIX4 RPC atomica queue_confirm_ticket (FOR UPDATE+advisory lock, GRANT service_role); FIX5 token nel path+azzerato a confirm+Referrer-Policy no-referrer+prefisso lingua URL. i18n: Coda_RemoteErrRate (10 Vetrina) + 9 chiavi shop_queue_* (10 App).
COLLAUDO STABILIZZATO: restart pulito sequenziale, 3 servizi su (server/vetrina 302, app 200, app-cat pub 200).
VERIFICA adversariale FATTA (a187dfa6dffbccb9e): FIX 1/3/4/5 CHIUSI; FIX2 PARZIALE. Build ok, i18n ok, normalizzazione email verificata live, nessuna regressione in-loco.
BUCO RESIDUO [ALTA, pre-esistente/sistemico]: le RPC coda sono SECURITY DEFINER + eseguibili da anon/authenticated; Caddy espone PostgREST su api-cat.puntify.it/rest/v1/* → /rest/v1/rpc/<fn> raggiungibile dall'esterno con anon key pubblica → si può bypassare il server e spoofare customer_id (riapre FIX2), o chiamare queue_call_next direttamente (grief). Server usa key service_role (verificato claim JWT) → REVOKE FROM anon/authenticated SICURO (app usa direttamente solo search_account_by_*/get_account_photo_by_id, NON le queue). FIX da applicare: REVOKE EXECUTE ON queue_take_ticket_remote/queue_confirm_ticket/queue_take_ticket/queue_call_next/queue_normalize_email FROM PUBLIC,anon,authenticated (GRANT service_role resta). VALE ANCHE PER PROD (stesso schema). Osservazione extra: search_account_by_email eseguibile da anon = enumerazione account (fuori scope, segnalare).
DECISIONI Stefano ricevute: (1) email conferma app = lascia com'è; (2) near → consolidare su near_notified_at (timestamptz) esistente, rimuovere boolean near_notified.
DA APPLICARE prima del commit (una passata): [a] REVOKE grant RPC; [b] consolidazione near su near_notified_at. Poi build + commit (NO deploy prod).

INCIDENTE 2 app-cat (23:02): loading perenne DIVERSO dal primo — errore client "Blazor culture change not supported / BlazorWebAssemblyLoadAllGlobalizationData". Causa: Program.cs app imposta cultura da localStorage puntify_culture tra 10 lingue; in Debug/collaudo ICU completo non caricato (icudt.dat 404 dal DevServer), LoadAll è solo in Release (csproj ha commento esplicito: NON attivare in Debug). Lingue it/en/es/fr/de ok; pl/uk/ro/nl/ru crashano il boot. NON causato dai fix (subagent non ha toccato app/csproj). Sblocco utente: localStorage.removeItem('puntify_culture')+reload. Proposto a Stefano: A) graceful fallback a EN in Debug per lingue non-EFIGS (no crash, quelle 5 lingue in EN solo su collaudo); B) far servire ICU completo/Release su collaudo (più lavoro). Attendo scelta A/B.
DEPLOY PROD (futuro): 2 migration in ordine — queue_remote_pending poi queue_remote_review_fixes — + REVOKE grant (vale anche prod).

## 2026-07-05 (notte, ~23:50) — app culture fix RISOLTO + 2 nuove richieste Stefano
APP-CAT FIX (incidente 2): risolto in Puntify.App/Program.cs con guard `#if DEBUG` attorno al blocco che imposta la cultura da localStorage/browser → in Debug NON cambia cultura (no crash), in Release resta dinamico. csproj ripristinato (LoadAll in Debug NON funziona: icudt.dat 404 dal DevServer, confermato). Verificato con chromium headless (~/.cache/ms-playwright/chromium-1228): l'app boota, pagina Blazor renderizzata, 0 errori cultura. Modifiche in working tree, NON committate. (2 cause distinte di "loading perenne" stanotte: (1) hot-reload TypeLoadException server, (2) culture change Debug.)
NUOVA RICHIESTA A — EMAIL candidati Sales Ambassador/BDR: inviare da info@puntify.it la bozza "Puntify | Fissiamo un primo incontro" a 8 candidati (alessandromaffei1012@virgilio.it, adamsarhan779@gmail.com, brvillaggio@gmail.com, laccettig@virgilio.it, nabihajamal4@gmail.com, gianmarco.dolce2002@gmail.com, aysenazakkaya@yahoo.com, berkaycabuk@outlook.com) + Stefano tutti in Ccn. PRIMA una PROVA a Stefano, invio ai candidati SOLO dopo suo OK. Timing "9 di oggi" già passato → chiesto conferma domani 6/7 09:00. Chiesta coerenza nome ruolo (email dice Sales Ambassador, job descr titolo BDR). Canale invio: Resend (da verificare chiave/path — RESEND non in /opt/ops/.env, cercare). ESTERNO = gate approvazione.
EMAIL — decisioni Stefano: (1) invio 6/7 ore 09:00 Roma (=07:00 UTC, CEST+02:00); (2) nome ruolo = Business Development Representative (dalla job descr); (3) UNA email SEPARATA per candidato (To: candidato, BCC: Stefano imodejam@gmail.com su ciascuna), NON un unico BCC. Canale Resend (chiave in Puntify.Server/appsettings Resend:ApiKey, re_..., dominio puntify.it verificato). GOTCHA: api.resend.com dietro Cloudflare blocca User-Agent urllib (403 code 1010) → mettere User-Agent browser. PROVA a Stefano INVIATA ok (id fd26907a, oggetto [PROVA]). ATTENDO "ok invia" per schedulare le 8 (usare scheduled_at Resend = 2026-07-06T09:00:00+02:00). Candidati: alessandromaffei1012@virgilio.it, adamsarhan779@gmail.com, brvillaggio@gmail.com, laccettig@virgilio.it, nabihajamal4@gmail.com, gianmarco.dolce2002@gmail.com, aysenazakkaya@yahoo.com, berkaycabuk@outlook.com. Chiave salvata temp in scratchpad/.rk (600), eliminare dopo invio.
NOMI candidati (da xlsx "Screening candidati" inviato da Stefano): 1 Alessandro(Maffei) 2 Adam(Sarhan) 3 Giordanobruno(Villaggio) 4 Giuseppe(Laccetti) 5 Nabiha(Jamal) 6 Gianmarco(Dolce) 7 Aysenaz(Akkaya) 8 Berkay(Cabuk). Greeting "Buongiorno {Nome},". Prova personalizzata inviata a Stefano (id aa83c556) + ri-test su richiesta (id 9ac1e4ae). ATTENDO "ok invia" per schedulare 8 personalizzate 6/7 09:00 (oggi è già 6/7 dopo mezzanotte). Invio a TUTTI e 8 anche Basso-fit salvo diverso ordine.
NUOVA RICHIESTA B — pagina "Lavora con noi" FATTA (subagent a0668732693ad6531): Pages/LavoraConNoi.razor route /lavora-con-noi + /{lang}/, hero stile Apple/Stripe, lista posizioni (1: BDR Italia, testo esatto), CTA Candidati=mailto info@, link nel FOOTER (colonna Risorse, non navbar), 16 chiavi Careers_*/Meta_* in tutte 10 lingue, corpo annuncio IT hardcoded. Build ok. NON committato/deployato. Anteprima screenshot (desktop+mobile) inviata a Stefano (msg 5370-5372) via chromium headless su localhost:8003 (cat.puntify.it ha basic-auth → 401). Da validare: link anche in navbar? ritocchi design?
CAREERS mods (Stefano msg 5373-5376) FATTE dal subagent: filtri Nazione/Regione/Città (data-driven, InteractiveServer, country-wide matcha tutto), IT hero invariato, 9 lingue non-IT hero/meta senza "PMI italiane" (generico "piccole imprese"), rimossa nota "Scrivici a info@puntify.it" (resta solo tasto Candidati). Build verde. 5 chiavi filtro nuove (Careers_FilterCountry/Region/City/All, Careers_NoResults) in 10 lingue.
SITEMAP/MAPPA (msg 5376): subagent a614af9be36a51d8b in corso — aggiungere /lavora-con-noi a SitemapController.cs (staticPages 0.7) + sitemap-static.xml (blocco hreflang come prezzi) + MappaDelSito.razor (Lk Careers_NavLabel). Solo quei 3 file.
DA FARE dopo entrambi i subagent: restart pulito vetrina + nuova anteprima screenshot a Stefano.
SITEMAP/MAPPA FATTO (subagent a614): careers in SitemapController.cs (0.7), sitemap-static.xml (9 url hreflang come prezzi), MappaDelSito.razor (Lk Careers_NavLabel). Build+XML ok.
CAREERS anteprima2 inviata a Stefano (msg 5378) — filtri renderizzano ok.
NUOVA RICHIESTA (msg 5377): ogni annuncio deve avere PROPRIA PAGINA (URL) + SEO (JSON-LD JobPosting) + sitemap DINAMICO. DELEGATO subagent a63fe2f43a47e8374 (bg): route /lavora-con-noi/{slug}, CareersData condiviso, HeadContent per-job, sitemap generato da lista. Supersede il dettaglio ad ancora.
NUOVA RICHIESTA (squillo): sulla pagina biglietto coda il turno non ha fatto squillo su telefono. Causa: coda-ring.js usa Web Audio (iOS richiede unlock da gesto utente) + navigator.vibrate (non su iOS); schermo bloccato/background = web non può suonare (serve PWA push o app). DELEGATO subagent ada3bdbaace5f5b05 (bg): unlock audio al primo tap + pulsante "Attiva avvisi" + beep più forte, in coda-ring.js + CodaBiglietto.razor, 10 lingue. Chiesto a Stefano se telefono era bloccato/foreground. Step2 (locked): push/PWA.
EMAIL 8 candidati: SCHEDULATE via Resend per 2026-07-06T09:00:00+02:00, una per candidato (To: candidato, BCC: imodejam@gmail.com), personalizzate "Buongiorno {Nome},", ruolo BDR. 8/8 OK (id Resend salvati nel log Bash). Stefano ha approvato la prova. Riceverà 8 copie BCC alle 9. Cancellabili via Resend API se serve.
SQUILLO foreground FATTO (subagent ada3bdbaace5f5b05): coda-ring.js unlock()/armAutoUnlock() + beep doppio-tono gain 0.8 ripetuto; CodaBiglietto.razor pulsante "Attiva avvisi sonori" (Cb_EnableSound/SoundEnabled/SoundHint in 10 lingue) + auto-unlock al primo tocco. Build ok. Copre solo pagina-aperta-foreground.
SQUILLO telefono BLOCCATO: Stefano conferma era bloccato, vuole "un modo per farlo squillare". Verità: web non può suonare iPhone bloccato. Opzioni date: SMS al turno (affidabile cross-platform, serve numero+provider Twilio a costo) vs Push PWA (gratis, ma iOS solo se installata in Home). Consigliato SMS. ATTENDO scelta Stefano.
PAGINE PER-JOB FATTE (subagent a63fe2f43a47e8374): CareersData.cs in Punto.Shared (fonte unica, referenziata da Vetrina+Server); Pages/LavoraConNoiDettaglio.razor route /lavora-con-noi/{slug} (+/{lang}/); slug business-development-representative-italia; SEO completo (HeadContent title/desc/canonical/hreflang/OG + JSON-LD JobPosting); sitemap DINAMICO — scoperta: il sitemap servito su /sitemap.xml è Puntify.Vetrina/Services/SitemapService.cs (non SitemapController), reso dinamico lì (+ anche controller). Lista linka alle pagine, non più ancore. Build Vetrina+Server 0 errori. Verificato: detail 200, in sitemap, JSON-LD presente. Screenshot inviato a Stefano (msg 5386). FIX mio: DatePosted 2026-01-15→2026-07-06. DA VALIDARE Stefano: meta desc SEO, employmentType=CONTRACTOR, traduzioni suffisso "Lavora con Puntify".
WORKING TREE ORA (NON committato): Fase B coda + 5 fix sicurezza coda + (mancano REVOKE+near consolidation) + app culture fix (Program.cs #if DEBUG) + careers (lista+dettaglio+CareersData+sitemap+MappaDelSito) + squillo foreground (coda-ring.js+CodaBiglietto). Da committare in chunk logici quando Stefano indica.
ANCORA APERTO: queue REVOKE+near+commit; Google auth prod grep; deploy careers (dopo validazione); scelta SMS/Push squillo bloccato.
ANCORA DA CHIUDERE (queue): applicare REVOKE grant + consolidazione near su near_notified_at, poi commit queue+app-fix. In pausa mentre gestisco A/B.
PROD: Stefano ha eseguito LUI le 2 migration su DB PROD (device-vuoto + queue_remote_pending). Confermato: prod = DB separato che gestisce lui, io da qui NON la raggiungo. Assessment dato: additive/safe (confirmed DEFAULT true protegge in-loco; queue_call_next REPLACE retrocompatibile). ATTENZIONE: DB prod ora AVANTI al codice (Fase A 155a13d non deployata, Fase B non committata) → feature remota inerte finché non deploya il codice; fix QR device-vuoto completo solo dopo deploy app 6fce46c. Chiesto a Stefano se prod-app è già deployata ai commit di oggi.
APERTO: dov'è la PROD vera di Puntify? Da questa box vedo solo collaudo/CAT (cat/app-cat/api-cat.puntify.it, DB puntify_cat). Nessun container/DB prod qui → correzione: la migration in prod da qui NON la posso applicare. Chiesto a Stefano.
DA CONFERMARE con Stefano: applicare le 2 migration coda (device-vuoto + remote) — dove/come sta la prod.

## 2026-07-06 (00:40) — COMMIT & PUSH fatto
Commit 0b06a60 pushato origin/master (46 file). Coda remoto UI+6 fix sicurezza (REVOKE su tutte e 6 RPC coda incl queue_call_next_across, near su near_notified_at), pagina Lavora con noi (per-annuncio SEO/JobPosting+sitemap dinamico), fix boot app WASM cultura (#if DEBUG), squillo foreground. Build 3 progetti ok. NO attribuzione Claude.
PROD DA FARE (Stefano): deploy codice master + applicare in prod `docs/DB Migrations/2026-07-05_queue_remote_review_fixes.sql`. queue_remote_pending già messa.
APERTO: Google auth prod grep; scelta SMS/Push squillo bloccato; validazione+deploy careers; email 8 candidati partono 09:00.

## 2026-07-06 (16:20) — richiesta tab "Utilizzo" AssistantAI (Nemi minuti+acquisto)
Stefano vuole su /merchant/{id}/assistantai un tab "Utilizzo" (minuti acquistati vs consumati, barre stile schermata usage) + pagina d'acquisto pacchetti (scaglioni+sconto+IVA+Paga, 2 foto riferimento).
MAPPATURA (Explore a3ca41cfc): AssistantAi.razor ha già tab (chat/config, pattern bk-tabs). Pacchetti Nemi definiti in Vetrina SharedResource: R1 25€/70min(0,36/min), R2 50€/150min(0,33), R3 100€/330min(0,30), R4 200€/690min(0,29) + attivazione 49€ + canone mensile + €/min. MANCA: tabella crediti minuti (acquistati/consumati) = da creare; consumo NON tracciabile finché Nemi Voce telefonia non esiste (vaporware); flusso acquisto esercente NON esiste (MerchantBilling è mock; Stripe solo incassi clienti Connect; serve Stripe Customer+Checkout+webhook+IVA per pagamento reale).
CHIESTE 2 DECISIONI a Stefano: (1) acquisto UI-first (modello crediti + UI, pagamento Stripe reale come step dopo) [consigliato] vs Stripe reale subito; (2) consumo placeholder 0 finché Nemi Voce non live [consigliato]. Attendo risposta per partire.
DECISO Stefano (18:40): (1) PRIMA UI (pagamento Stripe reale = step dopo, bottone Paga = stato "in arrivo", no addebito/no crediti finti); (2) consumo placeholder 0 ok. IN COSTRUZIONE (subagent a1d21f975df91dd0f): tab Utilizzo (barre minuti) + pagina acquisto (pacchetti R1-R4 + IVA + Paga stub) + tabella shop_nemi_credits (migration 2026-07-06_nemi_credits, collaudo; DA APPLICARE PROD) + endpoint GET credits. i18n 10 lingue.
FATTO (subagent a1d21f975df91dd0f): tab Utilizzo AssistantAI + pagina acquisto UI-first. File: migration 2026-07-06_nemi_credits.sql (tabella shop_nemi_credits, RLS+grant come account_telegram_links; APPLICATA collaudo) + Punto.Shared/Models/NemiCredits.cs + ShopAiController GET /api/shop/{id}/ai/nemi/credits + SocialStudioApiService.NemiCreditsAsync + AssistantAi.razor tab utilizzo (toggle _showBuy) + config.css nemiuse-* + 28 chiavi ×10 lingue. Paga = stub "in arrivo" (no addebito/no crediti finti). Consumati=0 placeholder. Build App+Server ok, server+app riavviati. credits endpoint 404 senza auth come history (corretto).
PROD LIST aggiornata: oltre a queue_remote_review_fixes, applicare anche `2026-07-06_nemi_credits.sql`.

## 2026-07-06 (21:30) — diagnosi Nemi: risposta fuori contesto a "Ci sei?"
Stefano: a "Ci sei?" Nemi ha risposto con una descrizione del locale (non richiesta). CAUSA (NemiChatService.ExecuteSimple:210): ad ogni messaggio ricarica TUTTO lo storico chat del negozio (nemi_chat_history limit=500, tutte le sessioni) come contesto → nessun concetto di "nuova conversazione" né finestra limitata. Un compito precedente (genera descrizione locale) era ancora nel contesto → il modello l'ha continuato. Il system prompt non vieta di generare contenuti non richiesti né impone risposte brevi ai saluti.
FIX proposto a Stefano (attendo ok): (1) limitare finestra a ~10-15 msg invece di 500; (2) regola system prompt "non generare contenuti se non richiesto nell'ultimo msg, saluti=risposta breve"; (3) opz. pulsante "Nuova conversazione".
STEFANO "ok a tutto" (21:35) → IN COSTRUZIONE (subagent ae64b37a2ff369c05): (1) finestra contesto ~15 msg in NemiChatService.ExecuteSimple (non toccare conteggio Richieste AI); (2) regola system prompt anti-generazione-non-richiesta + saluti brevi; (3) pulsante "Nuova conversazione" (reset NON distruttivo: micro-tabella nemi_chat_resets + endpoint reset + UI chat tab/widget svuota vista). Migration 2026-07-06_nemi_context_reset DA APPLICARE PROD. i18n 10 lingue.
FATTO (subagent ae64b37a2ff369c05): fix contesto Nemi. NemiChatService: ContextWindow=15, LoadContextWindow (desc/limit15+reset filter), regola system prompt anti-generazione+saluti brevi. ShopAiController: NemiHistory filtra created_at>reset_at + POST /ai/nemi/reset. NemiChatReset model + tabella nemi_chat_resets (RLS/grant come shop_nemi_credits). UI "Nuova conversazione" in AssistantAi.razor (menu chat) + AiAssistantFab.razor (widget FAB header), svuota vista. 2 chiavi ×10 lingue. Build ok, server+app riavviati. Storico intatto (Richieste AI non toccato).
PROD MIGRATIONS al deploy = TRE: queue_remote_review_fixes.sql, 2026-07-06_nemi_credits.sql, 2026-07-06_nemi_context_reset.sql.

## 2026-07-07 — tab Webhook super admin (monitoraggio)
Stefano: pagina /admin/monitoring → tab bar Generale(attuale)/Log applicativi(attuali)/Webhook(nuovo, chiamate webhook in ingresso vari servizi incl Nemi/Vapi + filtro). +Vuole vedere TUTTO: json input, json output.
Mappatura (Explore a8a4654): AdminMonitoring.razor+AdminMonitoringController (stato+dipendenze+metriche+log viewer da file /Logs/{kind}-{date}.log). Tab pattern AdminClientDetail (route {Tab}, admin-tabs css, tab nel path). Webhook oggi: Stripe/Telegram loggano a file, Vapi scrive nemi_calls; NO log webhook centralizzato. Admin access: AdminController.Guarded(perm "monitoring")+AdminService+ISupabaseClient service_role+AdminApiService.
IN COSTRUZIONE (subagent a141bd78365fcf8af): migration 2026-07-07_webhook_events (tabella webhook_events service/event_type/method/path/status/request_json/response_json/duration/ip/shop_id/received_at, RLS service_role) + WebhookLoggingMiddleware (cattura req+resp integrali dei path /api/vapi|stripe|telegram webhook, EnableBuffering+wrap response, fail-safe, cap ~256KB) + AdminService.GetWebhookEventsAsync + AdminMonitoringController GET monitoring/webhooks + AdminApiService + AdminMonitoring.razor tab bar (Generale/Log/Webhook route {Tab}) + vista Webhook filtro servizio + righe espandibili input/output. Build+migration collaudo.
PROD MIGRATIONS al deploy = 5: queue_remote_review_fixes, nemi_credits, nemi_context_reset, vapi_calls, webhook_events.
FATTO (subagent a141bd78365fcf8af): tab Webhook super admin. Migration 2026-07-07_webhook_events (webhook_events, RLS service_role) + WebhookLoggingMiddleware (cattura req/resp integrali su /api/vapi|stripe|telegram webhook, EnableBuffering+wrap response, event_type best-effort, headers segreti oscurati, cap 256KB, fail-safe Task.Run) registrato in Program.cs prima di ApiKeyAuth. AdminService.GetWebhookEventsAsync + AdminMonitoringController GET api/admin/monitoring/webhooks (guarded monitoring) + AdminApiService + AdminMonitoring.razor tab bar {Tab} Generale/Log/Webhook (tab nel path) + vista Webhook filtro servizio + righe espandibili input/output pretty. Verificato con webhook reali (Telegram 200/403, Vapi 401). Build ok. Server riavviato per test.
IN CORSO (subagent a6dd19cd414211da0): TASK1 orari strutturati in BuildVoiceContextAsync (booking_availability service_id null per giorno + booking_exceptions future + booking_manual_blocks futuri); TASK2 campo "Numero Nemi Voce" in ShopEdit sezione PV (distinto da Shop.Phone, salvato shop_vapi_config.phone_number, endpoint GET/POST).
PROD MIGRATIONS al deploy = 5: queue_remote_review_fixes, nemi_credits, nemi_context_reset, vapi_calls, webhook_events (poi ricaricare PostgREST prod).
FATTO (subagent a6dd19cd414211da0): TASK1 BuildVoiceContextAsync usa booking_availability(service_id+operator_id null, per giorno)+booking_exceptions futuri 60gg+booking_manual_blocks futuri, formato prompt, fuso shop, fallback opening_hours. TASK2 campo "Numero Nemi Voce" in ShopEdit tab anagrafica (bind _nemiPhone distinto da Shop.Phone) → shop_vapi_config.phone_number via GET/PUT /api/shop/{id}/ai/nemi/phone (upsert onConflict shop_id, non tocca vapi ids). SocialStudioApiService NemiGet/SavePhoneAsync. 2 chiavi ×10 lingue. Build ok.
INCIDENTE DIAGNOSI (evitare): puntify-server è su :8001, NON :5000 (=ClawRoom). Ho perso tempo testando :5000 (404 fuorvianti). Verificato su :8001: vapi webhook 401/405 ok, nemi phone 401, cat.puntify.it/api/vapi/webhook via Caddy 401 (già esposto). Salvato memoria reference_puntify_server_port. Server bin/Debug rimosso+ricostruito pulito durante diagnosi.
STATO: tutto live collaudo (server+app+vetrina active). Server riavviato. Restano da fare in PROD: 5 migration (queue_remote_review_fixes, nemi_credits, nemi_context_reset, vapi_calls, webhook_events)+reload PostgREST; segreto Vapi reale+account/numeri.
IN CORSO (subagent a141bd78365fcf8af): grafico "Andamento webhook per tipologia" nel tab Generale di AdminMonitoring (ApexCharts multi-serie per servizio, bucket giornaliero da webhook_events; endpoint AdminMonitoringController GET webhook-stats + AdminService.GetWebhookStatsAsync). Riuso window.puntifyCharts (non rompere firme Panoramica).
NUOVA richiesta (msg 5454): "Vedi come esercente" nella scheda admin esercente (/admin/esercenti/{id}), come "Vedi come cliente" nella scheda cliente. Esplorazione meccanismo in corso (Explore ae0fcd4c471c95f1b): come impersona il cliente (token/route/sicurezza), scheda admin esercente, auth sessione esercente, relazione esercente↔account↔shop. Poi replico per esercente (apertura area merchant come quell'account).

## 2026-07-07 (14:00) — batch: grafico webhook + vedi-come-esercente + vapi no-key + numero nemi anagrafica
FATTO grafico webhook (subagent a141bd78365fcf8af): AdminMonitoring tab Generale card "Andamento webhook per tipologia", AdminService.GetWebhookStatsAsync(days=14) bucket giorno×servizio da webhook_events, endpoint GET api/admin/monitoring/webhook-stats guarded, puntifyCharts.renderMultiArea (nuova, stacked area, non rompe firme Panoramica), empty-state. Build ok, app riavviata.
FATTO vedi-come-esercente (io, AdminMerchantDetail.razor): bottone "👁 Vedi come esercente" (pattern identico a AdminClientDetail: Impersonation.Start(_m.AccountId, businessName)+NavigateTo /merchant-shops), inject ImpersonationState, disabled se 0 shop. Build ok. CAVEAT segnalato a Stefano: impersonazione (anche cliente) è cosmetica — dati caricati con sessione admin, non account impersonato (ImpersonationState.AccountId non usato dai service). Chiesto: "vedi come cliente" mostra dati reali? Se no, serve far caricare ai merchant page i dati dell'account impersonato via admin API (lavoro più profondo, per cliente+esercente). Attendo.
VAPI no-key (msg 5458): Stefano NON vuole la private key per ora. Confermato: flusso funziona SOLO coi webhook (assistant-request inizio + end-of-call-report fine), senza API key. La key serviva solo al detach-a-saldo-0 (non necessario, il blocco è già nell'assistant-request). Serve solo: segreto webhook (dato) + numero Nemi per PV (da anagrafica) + assistant Vapi con metadata shopId. Identifico shop dal numero (inizio) e da metadata.shopId (fine).
Numero Nemi anagrafica (msg 5459): GIÀ fatto stamattina (ShopEdit tab anagrafica sotto Telefono/Email) → Ctrl+F5.
Vedi-come-esercente: bottone spostato in fondo a destra (msg 5462). Stefano conferma (5464): con impersonazione vede i SUOI dati, non quelli dello shop → serve impersonazione dati reale. Proposte 2 strade: A) read-only via API admin/service-role (i service merchant leggono lo shop impersonato; sola lettura; più lavoro) [consigliata]; B) login-as reale sessione esercente (GoTrue admin, fedeltà totale ma NON read-only, rischioso). Chiesto A/B + scope (pagine chiave vs tutte). Attendo decisione prima di build (grande+sensibile).
IMPERSONAZIONE read-only STRADA A (Stefano 5466: config/shops/servizi/agenda/menu): IN COSTRUZIONE (subagent a0f73375354e7670b). Plumbing: app AuthHeaderHandler aggiunge header X-Impersonate-Account quando Impersonation.IsActive; server (punto centrale ownership) se GET+admin verificato+permesso merchants+header → risolve ownership sull'account impersonato via service-role, blocca scritture (403), audit log; /merchant-shops risolve negozi impersonati. Così le pagine GET ereditano senza toccarle. Sicurezza: verificare JWT admin reale, solo GET.
VAPI saldo-zero (msg 5467): spiegato a Stefano. assistant-request decide PRIMA di rispondere. Opz1 rifiuto secco (0 consumo). Opz2 messaggio cortesia+hangup (2-3s costo Vapi Puntify, NON scala minuti negozio). Credito NEGOZIO non toccato in entrambi. +maxDurationSeconds protegge da sforo. Consiglio opz2. Attendo scelta 1/2.
FATTO impersonazione read-only (subagent a0f73375354e7670b): punto centrale ShopAuthorizationService.EvaluateImpersonationAsync (header X-Impersonate-Account; GET-only, verifica admin reale via AdminService+permesso merchants, account esercente role 2/3, audit impersonate_read; scritture→403). Filtri ShopOwnershipFilter/RequireShopOwner/RequireAccountOwner risolvono su account impersonato. Nuovo MerchantController /api/merchant/{shops,account,shop/{id}} read-only service-role (perché GetShopperShops/GetById erano direct-Supabase→bypassavano server). App: AuthHeaderHandler aggiunge header se Impersonation.IsActive; SupabaseService reso impersonation-aware per le 3 direct-Supabase. UI read-only: nascosti Aggiungi negozio/Salva; ImpersonationState.ReturnUrl→banner Exit torna a /admin/esercenti/{id}. Build Server+App 0 errori. NON runtime-tested (Stefano deve smoke-testare). Limiti: realtime/direct-Supabase in altre pagine non impersonate.
FATTO Vapi opzione 1 (io, VapiWebhookController): saldo<=0 → return error vuoto (chiusura silenziosa) + guard wasRejected (endedReason contiene "assistant-request") → minutesBilled=0 (rifiuti non scalano minuti). Server riavviato, vapi 401 ok.
Tutto live collaudo (server :8001, app :8002). Prod migrations invariate (5).

## 2026-07-07 (19:15) — fix badge+ora notifiche admin Telegram
Stefano: le notifiche admin (Negoziante accesso/Nuovo negozio ecc.) avevano testo [COLLAUDO]/[PRODUZIONE] invece del pallino, e la data 2h indietro (UTC).
FIX (io): TelegramService.cs env badge PRODUZIONE/COLLAUDO→🔴/🟠 + formato text da "[tag]\n" a "dot msg". AdminMonitorHelper.cs: DateTime.Now (server UTC) → NowRome (TimeZoneInfo Europe/Rome), replace_all su tutte le occorrenze dd/MM/yyyy HH:mm. Build ok, server :8001 riavviato. Live collaudo dal prossimo messaggio. NB: i msg [PRODUZIONE] arrivano dal server PROD → serve DEPLOY per applicare le 2 fix anche in prod.

## 2026-07-07 (19:49) — NUOVO progetto: app Android WebView Puntify + stampante
Stefano vuole un'app Android che ospita app-cat.puntify.it in WebView (inizialmente cat/collaudo), Android 12+, e se disponibile mette a disposizione la STAMPANTE del dispositivo (comande cucina + biglietti totem coda). Un altro Claude la sta già creando; io devo: fare le domande + creare un prompt con le mie note lato Puntify.
CHIESTE domande (msg 5515): tipo stampante (POS integrata Sunmi/iMin vs esterna ESC/POS BT/USB/rete), uso (totem kiosk vs comande esercente vs entrambi), chi genera il layout stampa (web manda testo pronto vs Android formatta da dati), distribuzione (Play/APK), switch ambiente, funzioni native (QR/kiosk/cassetto/schermo acceso/push).
IN CORSO Explore a489a1b78e443b207: note lato Puntify (totem/coda route+dati biglietto, comande/ordini dati, JS interop convenzione+bridge nativo, PWA/service worker, URL/ambienti, permessi web) per il prompt.
ATTENDO risposte Stefano → poi consegno prompt completo.
NOTE Puntify per app Android (Explore a489a1b78e443b207) PRONTE per il prompt: Totem route /coda/totem/{slug}?k={kioskToken} (biglietto: number/queueName/created_at/status, NON stampato oggi). Comande: /merchant/{shopId}/display/kitchen, MenuPublicOrder (order_code, items JSON [name/qty/unitPrice/itemNote], table_label, order_mode dine_in/takeaway/delivery, customer_note, total, created_at). JS interop: window.PuntifyNative (esiste già bridge nativo OAuth in native-auth.js: window.puntifyNativeAuth, postMessage action). Convenzione window.QRScanner/ReceiptCamera. PWA manifest standalone, SW disattivo su CAT (hard-refresh), cache images prod. Base href / dev, /app/ prod; env by hostname (cat→collaudo). URL: cat.puntify.it/app (collaudo), app.puntify.it/app (prod), ServerUrl da appsettings, JWT via AuthHeaderHandler. Permessi: camera(QR), geo, notifiche FCM, storage. Da aggiungere: window.PuntifyNative.printTicket()/printComanda() + interface Android.
ATTENDO risposte 6 domande Stefano → prompt.

## 2026-07-07 (19:55) — risposte Stefano app Android + prompt consegnato
Risposte: 1) stampante USB (/dev/bus/usb/005/005 "AB USB(PRN)"); 2) UNA app WebView per tutti (clienti/esercenti/servizi); 3) web app formatta, Android stampa; 4) target Olivetti TAO, APK sideload; 5) switch ambiente sì; 6) funzioni native sì (QR/kiosk/schermo/cassetto/push).
PROMPT consegnato a Stefano (msg 5517-5518, file prompt-android-puntify.md): app Kotlin WebView cat.puntify.it/app +switch prod, Android12+, permessi camera/geo/upload, UA "PuntifyAndroid", ponte window.PuntifyNative + JavascriptInterface PuntifyAndroid (printRaw base64 ESC/POS/printText/openCashDrawer/keepScreenOn/setKiosk), stampa USB via UsbManager BULK OUT+ESC/POS auto-permesso (alt SDK Olivetti), kiosk/keep-screen-on, FCM nativo step2. Note Puntify incluse (totem /coda/totem/{slug}?k=, comande /merchant/{shopId}/display/kitchen MenuPublicOrder, bridge OAuth esistente native-auth.js, PWA/SW, URL/env, permessi).
DA FARE LATO PUNTIFY (io, quando app Android pronta): web rileva PuntifyNative.capabilities.print, genera ESC/POS biglietto/comanda, chiama bridge. Aggancio successivo.

## 2026-07-07 (20:48) — design kiosk/flotta dispositivi (ragionamento con Stefano)
Stefano vuole: terminale = totem cucina o biglietti coda; comando dal WEB abilita kiosk; auto-avvio su pagina assegnata; 10-tap→PIN per uscire; problema monitor SENZA touch.
MIA PROPOSTA (msg 5520): modello "dispositivo arruolato" gestito centralmente.
1) Arruolamento dal web: sceglie RUOLO (totem coda/schermo cucina/display cliente/bacheca ordini…) → bridge nativo entra kiosk su quella pagina. + salvare assegnazione LATO SERVER (tabella "devices": id, shop, ruolo, pagina, pin) → sopravvive reinstalli + pannello gestione.
2) Auto-avvio BOOT_COMPLETED; per kiosk blindato = Android DEVICE OWNER provisioning (per test basta lock-task best-effort).
3) USCITA kiosk: touch=10-tap+PIN; NON-touch (schermi cucina HDMI/box)=SBLOCCO REMOTO dal pannello (esercente da suo telefono/PC → comando via realtime/push/polling → device esce kiosk); ripiego combo tasti hw/tastiera USB.
4) Generalizzazione: tabella devices + controllo remoto (cambia ruolo/pagina, sblocca, ricarica, riavvia, online status) = gestione FLOTTA appliance centralizzata.
Chiesto ok direzione → poi specifiche (tabella devices + comandi bridge + pannello) da integrare nel prompt Android + lato Puntify. Attendo.
DISPOSITIVI flotta (msg 5522): non solo tablet. Android 12+ stessa app, distinzione touch-vs-nontouch e con-stampante-vs-senza. TOUCH: tablet (versatile), POS con stampante (Olivetti TAO — ruoli che stampano biglietto/comanda), tablet in colonnina totem. NON-TOUCH (→sblocco remoto): box Android/HDMI+TV (schermo cucina/display muro economico), signage Android integrato. Mappa ruolo→device: totem coda=tablet/POS; schermo cucina=box+TV o POS; display cliente/bacheca=box+TV o tablet. Solo Android. Device dichiara capacità (touch/stampante/schermo) → pannello propone ruoli adatti. Attendo ok Stefano.

## 2026-07-07 (21:00) — trasformare pagina Monitor/screens in Devices + Monitors
Stefano: trasformare /merchant/{shopId}/screens in "devices" con tab bar: tab DEVICES (lista terminali arruolati, click→modifica/rimuovi) + tab MONITORS (anteprima monitor attuali inclusi totem, in un unico posto). Dubbio: come associo device↔monitor.
CHIARITO a Stefano (msg 5524): MONITOR = ruolo+URL (totem/cucina/display cliente/bacheca, pagine già esistenti = "COSA"); DEVICE = terminale fisico ("DOVE"); ASSOCIAZIONE = device→1 monitor → apre quell'URL in kiosk (o no + PIN). UI: nel dettaglio device scegli il monitor + kiosk + PIN. 1 device→1 monitor.
Explore a9a7007292e374aaa in corso: pagina screens attuale (entità vs URL generate), URL kiosk di ogni display (totem /coda/totem/{slug}?k=, cucina /merchant/{shop}/display/kitchen, customer, board), feature Screens=32, modello dati esistente, pattern tab.
POI: propongo struttura (tab Devices/Monitors + tabella devices: id/shop/nome/capabilities/monitor_id/kiosk/pin/online + arruolamento/pairing) e confermo prima di build.
AGGIUNTA Stefano (msg 5525): i Monitor devono poter essere anche URL/siti web custom, non solo i display Puntify → "da un solo posto piloto tutto" = gestore cartellonistica/flotta generale. Modello monitor: type = puntify_role (URL generata) | url_custom (URL digitata). Device→qualunque monitor (Puntify o esterno) in kiosk o no. Integrare nella tabella monitors + UI.

## 2026-07-07 (21:03) — mappatura Screens + design Devices/Monitors proposto
Screens attuale (Screens.razor /merchant/{id}/screens): linka solo Customer/Kitchen display. Display kiosk (Vetrina, ?k=kiosk_token): Totem /coda/totem/{slug}?k=&screen=totem, Bacheca /coda/display/{slug}?k=&screen=display, Display singola coda /coda/{qrToken}/display. Interni merchant: /merchant/{shopId}/display/customer|kitchen. Shop.KioskToken (Guid, no regen endpoint), flag kiosk_totem_enabled/display_enabled. Feature Screens=32. NESSUNA tabella schermi (URL generate). Tab pattern bk-tabs+{Tab}.
DESIGN proposto (msg 5526): tab MONITORS (tabella monitors: shop_id/nome/tipo puntify_*/url_custom/url/kiosk_default; preimpostati Puntify auto + custom URL libere; anteprima iframe) + tab DEVICES (tabella devices: shop_id/uuid/nome/capabilities/monitor_id/kiosk_enabled/pin/last_seen/comando_pendente; pairing via codice; associa monitor; azioni remote sblocca/ricarica/riavvia/cambia). Associazione device.monitor_id→URL kiosk. Controllo remoto via polling (copre non-touch). Build lato web ora (tabelle+pagina 2 tab+pairing+coda comandi), app Android consuma gli endpoint.
CHIESTO GO a Stefano per costruire il lato web.
GO Stefano (msg 5527). BUILD backend flotta IN CORSO (subagent aeb246789b6ba8c61): migration 2026-07-07_devices_monitors (tabelle monitors [type totem/board/kitchen/customer/queue_display/url_custom, url, kiosk_default] + devices [device_uuid, capabilities, monitor_id, kiosk_enabled, pin, status pending/active, pairing_code, last_seen, pending_command]); modelli Monitor/Device; ResolveMonitorUrl (puntify tipi da slug/kiosk_token/shopId/qrToken; url_custom); endpoint merchant (monitors CRUD+auto-seed preimpostati, devices list/put/delete/command/claim) + device-facing (pair/start genera codice, pair/status, poll heartbeat+assegnazione+comando). RequireShopOwner (GET rispetta impersonazione). Poi FASE UI 2 tab.
PROD migrations al deploy = 7 (aggiungere devices_monitors).

## 2026-07-07 (21:20) — backend flotta FATTO, UI in corso
BACKEND FATTO (subagent aeb246789b6ba8c61): migration 2026-07-07_devices_monitors (monitors+devices, RLS service_role) APPLICATA collaudo. Modelli Fleet/Monitor.cs(+MonitorTypes) /Device.cs(alias Monitor per non collidere System.Threading.Monitor). FleetService.ResolveMonitorUrlAsync (totem/board/queue_display via slug+kiosk_token+qr_token; kitchen/customer via shopId; url_custom). Endpoint merchant api/shop/{id}/{monitors CRUD+auto-seed preset, devices list/put/delete, devices/{id}/command, devices/claim} RequireShopOwner. Device-facing api/devices/{pair/start genera code 6char scad 10min, pair/status, poll heartbeat+monitor risolto+command once} bypass ApiKey. ISupabaseClient.UpdateRawAsync aggiunto (per settare jsonb null). Verificato live: pair→claim→poll ok, comando once, URL risolte, sicurezza owner. Build 0 err, server :8001 up (monitors 401, pair/start 200).
UI IN CORSO (subagent ae5b244f1ad7be8fd): Screens.razor→2 tab {Tab} Monitors(lista+anteprima iframe+custom URL)/Devices(lista online+dettaglio assegna monitor/kiosk/PIN/azioni remote unlock-reload-reboot-refresh/claim codice). FleetApiService. cfg-*, 10 lingue, gating HasScreens.
PROD MIGRATIONS = 7: +2026-07-07_devices_monitors.

## 2026-07-07 (21:35) — UI flotta FATTA + doc app Android ricevuta
UI FLOTTA FATTA (subagent ae5b244f1ad7be8fd): FleetApiService (pattern merchant, JWT owner+X-API-Key) registrato in Program.cs DI. Screens.razor→pagina 2 tab route /merchant/{ShopId:guid}/screens/{Tab} (default monitors, tab nel path, titolo "Dispositivi", gating HasScreens). Tab MONITORS: card nome/tipo leggibile/badge kiosk default/conteggio device/URL risolta con Copia+Apri, anteprima thumbnail iframe sandbox, +Aggiungi monitor (nome+URL validazione Uri), modifica/elimina solo custom (preset non eliminabili). Tab DEVICES: righe dot ONLINE/offline+capacità chip, click→modal dettaglio (rinomina/assegna monitor dropdown/toggle kiosk cfg-toggle iOS/PIN/azioni remote Sblocca-Ricarica-Aggiorna-Riavvia/Rimuovi conferma), +Aggiungi dispositivo claim codice+nome. Anteprima X-Frame: placeholder dietro iframe (no JS fragile)+fallback Apri scheda. 76 chiavi fleet_* ×10 lingue. Build App 0 errori. App :8002 riavviata, app-cat 200. NON committato/deployato.
DOC APP ANDROID RICEVUTA da Stefano (file DOCUMENTAZIONE.md): app it.puntify.appcat PRONTA+testata, stampa USB ESC/POS confermata (MINT POS A8 USB(PRN) VID045b/PID5311). Bridge window.PuntifyNative (printRaw base64/printText/openCashDrawer/getPrinterStatus/keepScreenOn/setKiosk), evento puntifynativeready, UA PuntifyAndroid/1.0, admin 7tap+PIN 246810. Carica app-cat.puntify.it. Creata wiki/projects/puntify-android-app + memoria reference_puntify_android_app.
DA FARE LATO WEB (io, TODO#1 app): generare ESC/POS comande cucina (MenuPublicOrder) + biglietti totem coda, rilevare capabilities.print + ascoltare puntifynativeready, chiamare PuntifyNative.printRaw. È il pezzo che completa la stampa end-to-end.
PROD MIGRATIONS = 7 (invariato).

## 2026-07-07 (21:50) — stampa biglietto totem + rinomina /devices + tab bar/footer (subagent a940a2e1d86370af9) FATTO
BATCH FATTO (1 solo restart pulito, build 3 progetti 0 err, app-cat 200):
(1) STAMPA BIGLIETTO TOTEM: nuovo toggle per-coda "Stampa biglietto al totem" (colonna DB `totem_print_enabled` bool default false su queues, migration docs/DB Migrations/2026-07-07_queue_totem_print.sql APPLICATA collaudo; QueueDef.TotemPrintEnabled; QueueController QueueUpsert+Create/Update; QueueApiService QueueInput+BuildBody; QueueEdit.razor toggle switch/slider come "Mostra stima"; 2 chiavi queueedit_totemprint_title/desc ×10 lingue). Endpoint pubblico display: ShopQueueRow+TotemPrintEnabled. Pagina totem CodaNegozioDisplay.razor: in TakeNumber, se q.TotemPrintEnabled → best-effort JS totemPrinter.printTicket (try/catch, non blocca). Nuovo wwwroot/js/totem-printer.js (isAvailable=capabilities.print; buildEscPos: init/center/shop bold/coda/NUMERO grande GS!0x11/data-ora/in attesa/eta/taglio GS V 66 0; base64→PuntifyNative.printRaw) incluso in App.razor. Best-effort: no stampante o toggle off → solo a schermo, nessun errore.
(2) ROUTE /screens→/devices: Screens.razor @page (default tab monitors) + SetTab; link MerchantHome.razor tile + AiAssistantFab.razor check route. Filename Screens.razor invariato.
(3) TAB BAR riposizionata dentro <main class="cfg-main"> in cima (pattern AssistantAi/MerchantOperators).
(4) BOTTONE "Aggiungi monitor/dispositivo" in FOOTER BAR fissa cfg-actionbar/cfg-actionbar-inner (pattern Booking/ShopOperators.razor pagina operatori), label/azione per tab attivo, rimosso inline.
NON committato, NON deployato. Serve Ctrl+F5 lato Stefano (WASM).
PROD MIGRATIONS ora = 8: +2026-07-07_queue_totem_print. (ordine: queue_remote_review_fixes, nemi_credits, nemi_context_reset, vapi_calls, webhook_events, devices_monitors, queue_totem_print + reload PostgREST).

## 2026-07-07 (22:02) — tile home "Monitor"→"Dispositivi" (io)
Stefano: sulla home esercente il tile che porta a /devices era icona+label "Monitor". Chiesto se fornisce icona "Devices" (set icone 3D lucido bespoke, no image-gen qui) → Stefano "per ora lascia quella e chiamala dispositivi". FATTO: MerchantHome.razor label hardcoded "Monitor" → L["merchanthome_icon_dispositivi"]; icona resta Monitor.webp. Nuova chiave merchanthome_icon_dispositivi in tutte 10 resx App (IT Dispositivi/en Devices/es Dispositivos/de Geräte/fr Appareils/nl Apparaten/pl Urządzenia/ro Dispozitive/ru Устройства/uk Пристрої). Build App 0 err, app riavviata, app-cat 200. Non committato. Icona Devices dedicata = TODO quando Stefano la manda.

## 2026-07-07 (22:30) — Q&A pairing device + prompt Android FLOTTA consegnato
Stefano ha chiesto di capire il flusso creazione device. Spiegato: pairing OPZIONALE (default=app normale; collegato=monitor gestito in kiosk), "il device mostra codice→esercente lo inserisce dal web Dispositivi→Aggiungi dispositivo→assegna monitor". Chiarito che l'app Android CONSEGNATA non ha ancora pairing/polling (solo WebView+stampa+kiosk locale). TV → box Android HDMI (non-touch, sblocco remoto). Chromecast: Stefano ricorda che dopo il cast va da solo → confermato (receiver Cast gira in autonomia); deciso di AGGIUNGERE Chromecast come tipo monitor display-only via Google Cast receiver (traccia separata), caveat no auto-relaunch al riavvio.
PROMPT ANDROID FLOTTA consegnato (msg 5561-5562, file scratchpad + raw/docs/puntify-android-flotta-PROMPT.md): pairing area admin + polling + kiosk/autostart + non-touch unlock + capabilities; Chromecast receiver CAF che riusa /api/devices/*. Contratto endpoint verificato dal codice reale (DevicesController/ShopDevicesController/ShopMonitorsController/FleetService/Device.cs/Monitor.cs) e raggiungibilità https://cat.puntify.it/api/devices/* (400=ok). Sezione flotta aggiunta a wiki/projects/puntify-android-app.

## 2026-07-07 (22:50) — BUG toggle stampa totem non salvava = PostgREST schema cache vecchia
Stefano: attivava "Stampa biglietto al totem", salvava, rientrava→OFF; e la stampa non partiva. DIAGNOSI (log server): `UPDATE queues → 400 PGRST204 "Could not find 'totem_print_enabled' column in schema cache"` + il controller rispondeva 200 (perdita silenziosa). CAUSA: dopo la ALTER TABLE (migration collaudo) PostgREST non aveva ricaricato lo schema → colonna sconosciuta (write E read fallivano/defaultavano false). Codice app+server corretti (verificato WASM servito ha totemPrintEnabled, server dll ha il mapping).
FIX: (1) `NOTIFY pgrst,'reload schema'` su puntify_cat → verificato colonna ora nota (GET 200); PATCH service_role Gastronomia totem_print_enabled=true OK (attivato per test stampa di Stefano). (2) QueueController Create/Update ora propagano fallimento (500 se !ok) invece di 200 silenzioso; build 0 err, server riavviato; display ora ritorna totemPrintEnabled=true per Gastronomia. Registrato in mistakes.md.
REGOLA PROD ribadita: dopo le migration RICARICARE PostgREST (NOTIFY reload schema / restart) SEMPRE, o le colonne nuove danno PGRST204. Le migration collaudo già applicate sono ora tutte in cache (reload globale). NON committato il fix QueueController (fa parte del batch da committare).

## 2026-07-07 (23:20) — stampa totem: esce ma BIANCA (bug lato Android printRaw, NON web)
Dopo il fix schema cache, la stampa PARTE (carta+taglio) ma esce bianca. DIAGNOSI: i byte ESC/POS generati dal web sono CORRETTI (simulato buildEscPos+base64 in python: contiene "Caffè Sant'Eustachio"/"Gastronomia"/"NUMERO"/"5"/data/"In attesa: 1", roundtrip base64 ok). Prova logica: il bigliettino include SEMPRE il letterale "NUMERO"+data → se esce TUTTO bianco non è un problema di dati, è che printRaw non rende i byte grezzi. printText (Stampa di prova) stampa ok → printRaw bianco = BUG LATO ANDROID nel path printRaw. Dato a Stefano il test vector base64 + i controlli per lo sviluppatore Android (Base64 STANDARD non URL_SAFE; scrivere TUTTI i byte via bulkTransfer; init ESC @). Offerto ripiego printText (senza numero gigante) se vuole intanto. Web NON va toccato (è corretto).

## 2026-07-07 (23:20) — doc Android v2 + feedback UI Devices in corso (subagent a72dd53b52ff725d6)
Doc Android v2 in vault (flotta implementata). Subagent a72dd53b costruisce: fix IsOnline 90→120s + auto-refresh lista Devices 15s; lista stile come ShopOperators/QueueList; icone capability+azioni stile Lucide outline (come icona indirizzo pubblica); popup più largo; istruzioni Aggiungi dispositivo passo-passo (10 lingue). Server+App, restart pulito, no commit.
FATTO (subagent a72dd53b): ShopMonitorsController IsOnline 120s; Screens.razor auto-refresh 15s (IDisposable timer solo tab devices), lista cfg-row-list come ShopOperators/QueueList, capability pill Lucide outline (pointer/printer/monitor), azioni Lucide outline (lock-open/rotate-cw/refresh-cw/power), modal fleet-modal-wide 600px (fix conflitto CSS cfg-modal 420px), claim con ol 4 step + 5 chiavi fleet_claim_* ×10 lingue. Build 0 err, restart server+app, app-cat 200. Verificato: POS ora online (last_seen 5s, soglia 120s). NON committato.
BATCH DA COMMITTARE (non ancora): stampa totem (toggle+totem-printer.js+CodaNegozioDisplay+migration queue_totem_print) + rinomina /devices + tab bar/footer + tile Dispositivi 10 lingue + fix QueueController 200-silenzioso + UI Devices rifiniture + IsOnline 120s. Deploy prod: attende ok Stefano (8 migration + reload PostgREST).

## 2026-07-07 (23:38) — stampa totem OK (printRaw sistemato lato app) + fix accenti CP437 + footer
Lo sviluppatore Android ha sistemato printRaw: la stampa ora esce (foto di Stefano, numero 4 Panetteria). Restavano: (1) "è" usciva come ⊕ = la stampante usa CP437 non Latin-1 (0xE8=Φ in CP437). FIX in totem-printer.js: mappa accenti→byte CP437 (è=0x8A, à=0x85, ò=0x95, ù=0x97, ç=0x87, é=0x82, É=0x90, +altri) con fallback strip-diacritici; forzato ESC t 0 (CP437). (2) aggiunto footer "powered by Puntify.it" piccolo (Font B ESC M 1) centrato. Cache-bust script App.razor ?v=20260708b. Restart Vetrina, JS servito verificato (con basic-auth user:puntify83!). Byte simulati OK (Caffè→...8a).
DOMANDA LINGUA (Stefano msg 5580): le label del biglietto (NUMERO/In attesa/Attesa stimata/powered by) sono HARDCODED in ITALIANO nel JS. Proposto a Stefano: localizzare alla lingua SELEZIONATA sul totem (scelta cliente), fallback account. Attendo conferma (totem vs account) per localizzare in tutte le lingue (passare le stringhe L[] da CodaNegozioDisplay.razor a printTicket).

## 2026-07-07 (23:42) — QR totem in modalità stampa = rischio doppione (decisione aperta)
Stefano: sulla conferma totem il QR (→ /coda/biglietto/{ticketToken}, "segui il tuo numero") resta a video ~30s; in modalità stampa un estraneo può scansionarlo → segue lo stesso numero del cartaceo = doppione. Proposte a Stefano: OPZ1 (consigliata, subito) se totem_print_enabled → niente QR a video, solo numero+"ritira biglietto", countdown corto; OPZ2 (migliore) stampare il QR SUL biglietto via ESC/POS GS(k (da verificare supporto stampante). Regola: QR a video solo se stampa NON attiva. ATTENDO scelta Stefano. NON implemento ora: il subagent a357c2a6d3f873366 (localizzazione biglietto) sta editando CodaNegozioDisplay.razor → evito edit concorrenti.

## 2026-07-07 (23:48) — biglietto localizzato (FATTO) + Opzione 2 QR su biglietto (in corso)
LINGUA BIGLIETTO FATTO (subagent a357c2a6): label NUMERO/In attesa/Attesa stimata localizzate alla lingua del totem con guard latino (non-latine ru/uk/ar/hi/bn/zh → en per la stampa CP437). Vetrina ha 10 lingue reali: it/en/es/fr/pt/ar/hi/bn/zh/uk (NO de/nl/pl/ro/ru). BuildPrintStrings imposta temp CurrentUICulture. JS ?v=20260708c. Build+restart Vetrina ok.
OPZIONE 2 QR (Stefano ha scelto, caption "Inquadra e segui il tuo turno" confermata) IN CORSO (subagent a90885f66d6b1e544): stampa QR sul biglietto via ESC/POS GS(k + caption; QR a video SOLO se non stampato (ShowQr=!printed, fallback se stampante assente); config coda icona info+tooltip queueedit_totemprint_tip; chiavi Totem_Print_FollowCaption + Coda_TicketPrinted (10 lingue Vetrina) + tooltip App (10 lingue). JS ?v=20260708d. Build Vetrina+App, restart, no commit. NB QR ESC/POS GS(k da testare sulla stampante di Stefano (A8 generica).

## 2026-07-08 (00:00) — Opzione 2 QR FATTA + feature device a destra + width fix
OPZIONE 2 FATTA (subagent a90885f66d6b1e544): totem-printer.js pushQr (ESC/POS GS(k Model2, module 6, EC M) + caption Font B; QR stampato solo se data.followUrl. CodaNegozioDisplay: Confirmation+ShowQr/Printed, printed best-effort, ShowQr=!printed (QR a video solo se non stampato/fallback), "Ritira il biglietto"=Coda_TicketPrinted. followCaption=Totem_Print_FollowCaption "Inquadra e segui il tuo turno" (guard latino). Tooltip config coda queueedit_totemprint_tip (pattern pan-tip riusato, pan-tips.js globale). Chiavi Vetrina(10)+App(10). JS ?v=20260708d. Width fix Screens 820→1160 (fleet-cards/subtitle/cfg-row-list). Build 0 err, restart Vetrina+App, app-cat 200, JS servito verificato.
FEATURE DEVICE A DESTRA (io, Screens.razor): spostate le fleet-caps fuori da cfg-row-main → riga .fleet-drow flex, caps margin-left:auto justify-end (a destra, allineate a destra, prima del chevron); label wrappate in span, media 560px icon-only. Build 0 err, app riavviata, app-cat 200.
NB QR ESC/POS GS(k DA TESTARE sulla stampante di Stefano (A8 generica). Batch da committare cresciuto (stampa totem+accenti CP437+footer+localizzazione+Opzione2 QR+tooltip+UI Devices+width+feature-destra+IsOnline+QueueController fix). Deploy prod: attende ok (8 migration + reload PostgREST).

## 2026-07-08 (00:12) — rifiniture totem/biglietto + FAQ (batch Vetrina subagent a0eb17dc15ba1a457)
- Ritorno rapido se stampato: StartAutoReturn _countdown = Printed?3:30 (FATTO io, CodaNegozioDisplay, Vetrina riavviata).
- IN CORSO subagent a0eb17dc: (1) CodaBiglietto togli ripetizioni: unite Cb_SoundHint+Cb_KeepOpen in una frase ("si aggiorna da sola e squilla al tuo turno"), Cb_Waited "Attesa"→"In attesa da"; (2) clientUrl con prefisso lingua selezionata ({CurrentLang}/coda/biglietto/token) → pagina segui-numero nella lingua del cliente (URL usa lingua REALE, non guard latino); (3) FAQ EliminaCode +4 voci (stampa totem/segui dal telefono/lingua/serve app) 10 lingue; (4) QR STAMPATO module size 6→4 (metà area, totem-printer.js) + App.razor ?v=20260708e. Build+restart Vetrina.
- UCRAINO stampato in inglese = ATTESO (CP437 no cirillico). Spiegato a Stefano: carta=en, follow-page=uk (dopo fix lingua). Opzione CP866 per cirillico su carta offerta (da testare).
- QR size: Stefano vuole il QR STAMPATO più piccolo (msg 5600). Fatto module 4.

## 2026-07-08 (00:15) — PENDING: cirillico CP866 su biglietto (Stefano: proviamoci, se non va torniamo indietro)
DA FARE appena atterra il subagent a0eb17dc (tocca stessi file): implementare stampa cirillico per uk/ru. Piano: BuildPrintStrings → CyrillicLangs {uk,ru} usa la lingua reale + codepage "cp866"; {ar,hi,bn,zh}→en cp437; latine→cp437. totem-printer.js: buildEscPos seleziona ESC t 17 (0x1B 0x74 0x11) per cp866 vs ESC t 0 per cp437; pushText codepage-aware con mappa CP866 (range Russo 0410-041F→80-8F, 0420-042F→90-9F, 0430-043F→A0-AF, 0440-044F→E0-EF; Ё0401→F0 ё0451→F1; UA Є0404→F2 є0454→F3 Ї0407→F4 ї0457→F5 Ў040E→F6 ў045E→F7; І0406→Latin I, і0456→i, Ґ0490→Г83, ґ0491→г A3). ASCII resta <128 (nomi latini ok). Bump ?v a 20260708f. Fallback: se stampante non supporta CP866, revert uk→en.

## 2026-07-08 (00:20) — batch Vetrina ATTERRATO + CIRILLICO CP866 FATTO
BATCH (subagent a0eb17dc) FATTO: CodaBiglietto tolto Cb_SoundHint (una sola frase Cb_KeepOpen "si aggiorna da sola e squilla al tuo turno"), Cb_Waited "Attesa"→"In attesa da"; clientUrl con langPrefix (pagina segui-numero nella lingua cliente); EliminaCode FAQ +4 (Elc_Faq8..11, 10 lingue); QR stampato module 6→4; App.razor ?v=20260708e. Build 0 err, Vetrina riavviata.
CIRILLICO CP866 FATTO (io): totem-printer.js cp866(code) map (range russo+UA Ї/Є/Ў; І/і→I/i, Ґ/ґ→Г/г) + pushText codepage-aware (_useCp866) + ESC t 17 se cp866; CodaNegozioDisplay BuildPrintStrings split CyrillicTicketLangs{ru,uk}→lingua+cp866, OtherNonLatin{ar,hi,bn,zh}→en+cp437, latine→cp437, ritorna+passa codepage a printTicket; App.razor ?v=20260708f. Build 0 err, Vetrina riavviata, totem 200, JS servito ha cp866/ESC t17. Simulazione byte CP866 OK (НОМЕР→8d8e8c8590). DA TESTARE Stefano: stampa ucraino → se illeggibile revert uk→en (facile).
DA COMMITTARE (batch enorme stasera): stampa totem+accenti CP437+CP866+footer+localizzazione+Opzione2 QR+tooltip+UI Devices(width,feature-destra,online120s,auto-refresh)+QueueController fix 200+dedup testi+FAQ+ritorno-rapido. Deploy prod: attende ok (8 migration + reload PostgREST + ricordare ?v bump).

## 2026-07-08 (00:28) — overlay "Sistema in manutenzione" + riconnessione robusta totem (io)
Stefano: sul totem se il server va giù compare la barretta gialla Blazor; vuole messaggio manutenzione + ripristino automatico. FATTO (Puntify.Vetrina/App.razor): custom #components-reconnect-modal overlay full-screen "Sistema in manutenzione" + spinner (CSS su .components-reconnect-show/failed/rejected). blazor.web.js autostart=false + Blazor.start({circuit:{reconnectionOptions:{maxRetries:20,retryIntervalMilliseconds:3000}}}) → blip riprende senza reload. Su "rejected" (server riavviato, circuito perso): waitServerThenReload() fa polling HEAD sulla pagina e ricarica SOLO quando il server risponde (mai col server giù → no pagina errore nel WebView). Sostituito il vecchio handler che ricaricava 30x anche col server giù. Build 0 err, Vetrina riavviata, totem 200, overlay+script serviti. Testo overlay in IT (localizzabile su richiesta).

## 2026-07-08 (06:25) — fix stampa ucraina: "?"→translitterazione + numero centrato
Stefano: stampa ucraina OK (cirillico giusto). Due fix: (1) in modalità cp866 la "è" latina del nome negozio usciva "?" → ora translittera in ASCII (Caffè→Caffe) invece di 0x3F (branch _useCp866 di pushText); (2) numero decentrato = doppia larghezza non centrata dalla stampante → cambiato GS ! 0x11 (W+H x2) in GS ! 0x02 (altezza x3) + grassetto, senza larghezza → centrato esatto. App.razor ?v=20260708g. Build 0 err, Vetrina riavviata (dopo ~40s watch), totem 200, JS servito verificato, simulazione Caffè→Caffe + Черга cirillico ok. DA CONFERMARE Stefano ristampa ucraina.

## 2026-07-08 (06:30) — aggiornamento vetrina Elimina Code con le novità code (subagent ad52a854)
Stefano: valuta ciò che abbiamo introdotto per le code e riportalo nella vetrina Elimina Code. VALUTAZIONE data a Stefano (msg 5610): totem multi-coda+lingua, stampa biglietto cartaceo USB, QR segui-turno+squillo, coda da remoto, multi-lingua (cirillico incluso), monitor/display, pannello operatore+stima+notifiche, affidabilità (overlay manutenzione+auto-recovery). IN CORSO subagent ad52a854: EliminaCode.razor ritocca HeroDesc (biglietto digitale o stampato + lingua cliente), Feat1 (+stampa), Feat2 (+squillo al turno), aggiunge Feat5 "Biglietto stampato" + Feat6 "Nella lingua del cliente" (icone stampante/globo), 10 lingue; griglia a 6 card. FAQ già ampliata (Faq8-11). Build+restart Vetrina, no commit.

## 2026-07-08 (06:35) — distinta hardware Elimina Code (senza totem) per Stefano
Stefano: pezzi+costi+link Amazon per elimina code minimale senza totem. Dato (msg 5612): Fase0 solo QR ~0€; Fase1 tablet Android 10"+supporto antifurto ~120-150€; Fase2 +stampante USB ESC/POS 80mm ~80-100€; opzione display TV via box Android HDMI ~40€. Esempio tipico ~270€. Vincoli: stampante USB ESC/POS (non BT), Android 12+, TV serve box. Link amazon.it reali (WebSearch). Salvato wiki/projects/elimina-code-hardware-bom.

## 2026-07-08 (06:38) — vetrina Elimina Code aggiornata FATTO (subagent ad52a854)
EliminaCode.razor: HeroDesc rivista (biglietto digitale o stampato + lingua cliente), Feat1Desc (+stampa carta), Feat2Desc (+squillo al turno), nuove Feat5 "Biglietto stampato, se serve" + Feat6 "Nella lingua del cliente" (icone stampante/globo), griglia a 6 card, 10 lingue. Build 0 err, Vetrina riavviata, /elimina-code 200, card servite verificate. Non committato. (Subagent aveva chiuso col restart ancora in corso → verificato io.)
NOTA: TUTTO il lavoro di stanotte (7-8 lug) è su collaudo, NON committato/deployato. Al deploy prod: batch codice enorme (vedi voci precedenti) + 8 migration + reload PostgREST + ricordare i ?v bump. Attende ok Stefano.

## 2026-07-08 (06:45) — COMMIT+PUSH batch (9ff2136) dopo review sicurezza
Stefano: committa e pusha tutto. Review pre-commit (subagent a6c062d0): H1 HIGH = WebhookLoggingMiddleware salvava il body risposta integrale col segreto Vapi in chiaro (assistant.server.secret/function.server.secret) in webhook_events → FIXATO: RedactSensitive ricorsivo su chiavi secret/token/apikey/authorization/password in ToJsonPayload (JsonNode). Server ricostruito+riavviato. Resto OK (no injection/IDOR/bypass impersonazione, comandi allowlist, JWT/ApiKey timing-safe). Committato 98 file (9ff2136), pushato origin/master. Nessun segreto/appsettings nel commit (verificato). NO attribuzione Claude.
FOLLOW-UP aperti (segnalati a Stefano): M1 MEDIO device re-pair takeover via device_uuid (serve device-secret + ritocco Android); L1 no rate-limit claim; L2 tool voce fidano caller-ID (conferma su distruttive).
DEPLOY PROD (quando Stefano ok): 7 migration (nemi_credits, nemi_context_reset, vapi_calls, webhook_events, customer_profiles, devices_monitors, queue_totem_print) + reload PostgREST. Nota: queue_remote_review_fixes era gia' nel push precedente 0b06a60? verificare al deploy. I ?v bump JS sono nel codice.

## 2026-07-08 (09:35) — fix bug back registrazione esercente (commit 048e686)
Stefano: da role-selection→esercente→prima config, il tasto Indietro non tornava alla scelta ruolo (ributtava nel flusso esercente). CAUSA: SelectMerchantRole salva role=3 subito; RoleSelection.OnInitializedAsync con role!=0 chiamava RedirectFromCache che per esercente-senza-profilo reindirizzava a merchant/registration → loop. FIX: RedirectFromCache ora ritorna bool; per role 2/3 reindirizza a merchant-shops SOLO se HasMerchantProfile, altrimenti false → mostra la selezione (utente può tornare indietro / scegliere cliente). Build App 0 err, app riavviata, app-cat 200. Commit 048e686 pushato origin/master.

## 2026-07-08 (12:20) — Stefano in deploy PROD: data checklist post-migration
Stefano sta aggiornando il DB prod. Data checklist (msg 5622): (1) RICARICA PostgREST prod = restart container (o NOTIFY, meno affidabile) — senza dà PGRST204, colonne nuove non salvano; (2) deploy codice prod ai commit 9ff2136+048e686 (pull+build+restart servizi); (3) impostare Vapi:WebhookSecret in env prod; (4) hard-refresh WASM app.puntify.it; (5) verifica salvando qualcosa che usa colonna nuova (toggle stampa totem). 7 migration additive di stanotte. Prod = box separato che gestisce Stefano.

## 2026-07-08 (14:20) — PROD: upload logo PV → 403 Forbidden (diagnosi storage MinIO)
Stefano prod: "Errore caricamento logo … 403 Forbidden" caricando logo da PV. DIAGNOSI: upload = 2 passi — (1) POST /api/storage/upload-url al server (X-API-Key) → presigned; (2) PUT file direttamente a MinIO. NON è il deploy (StorageController/S3StorageService non toccati; appsettings per-ambiente). Il 403 è quasi certo sul PUT a MinIO (passo1 usa X-API-Key che già funziona per tutto). S3StorageService firma con Endpoint/Region/AccessKey/SecretKey/ForcePathStyle/PublicBaseUrl. Dato a Stefano: confermare via log server ("Storage: upload-url requested") + log MinIO (SignatureDoesNotMatch/AccessDenied/NoSuchBucket); verificare in prod: (1) AccessKey/SecretKey con permesso PutObject sul bucket shopimages [causa#1 se le img si vedono ma upload no], (2) bucket esiste, (3) region coerente, (4) endpoint firmato = host raggiunto dal browser, (5) clock skew server. Chiesto log per pinpoint. Prod non raggiungibile da qui.

## 2026-07-08 (14:24) — RISOLTO upload logo prod: nomi bucket errati
Stefano: era la causa #2 = i nomi bucket in config non combaciavano coi bucket reali MinIO prod → 403 NoSuchBucket. Fixato lui. REGOLA: StorageClient:Buckets (Shops/Accounts/Receipts; default shopimages/accountimages/receiptimages) DEVE combaciare coi bucket creati in MinIO (per ambiente). Diagnosi 403 storage chiusa.

## 2026-07-08 (14:41) — PROD upload logo: 403→400 (bucket suffisso -prod)
Dopo fix 403, ora 400 Bad Request. CAUSA: StorageController.upload-url risponde BadRequest("Invalid bucket") se il bucket richiesto dall'APP non è in _allowedBuckets (server StorageOptions.Buckets.Values). Prod ha bucket con suffisso "-prod". Disallineamento: app manda un nome, server ne consente un altro. FIX dato a Stefano: allineare a nomi reali "-prod" in ENTRAMBE le config (server Storage:Buckets + app StorageClient:Buckets) + PublicBaseUrl; devono combaciare app=server=MinIO. Nota: anche content-type filtrato (solo image jpeg/png/webp/heic; svg+html bloccati per XSS). Log server "Storage: invalid bucket '…'" per confermare il nome in arrivo.

## 2026-07-08 (14:52) — PROD 400 bucket: server ok, client manda vecchio nome (cache WASM)
Log prod (foto Stefano): server rifiuta "shopimages" perché ora accetta solo "shopimages-prod"; il client continua a mandare "shopimages" = config WASM vecchia (cache/service worker app.puntify.it). FIX immediato: verificare appsettings.json app deployato ha -prod, poi Clear site data + unregister SW su app.puntify.it → client manda -prod (fix completo, config già giusta). SCONSIGLIATA la "difesa server-only" (backend ignora bucket client): incompleta perché il client costruisce anche l'URL pubblico ({PublicBaseUrl}/{bucket}/{key}) con IL SUO bucket → salverebbe URL rotto. FIX DEFINITIVO proposto (da fare collaudo, prossimo deploy): client manda chiave logica, server risolve bucket per-ambiente E ritorna l'URL pubblico finale (client env-agnostic). Chiesto a Stefano se implementarlo. ATTENDO.

## 2026-07-08 (14:57) — fix definitivo storage env-agnostic (subagent a1a7150b) IN CORSO
Stefano "si": implementare il fix. Design: client manda CHIAVE LOGICA (Shops/Accounts/Receipts), server risolve bucket fisico per-ambiente (ResolvePhysicalBucket tollerante: chiave logica | fisico esatto | base-name senza suffisso -prod/-cat/-dev/-collaudo/-staging) e RESTITUISCE l'URL pubblico; client usa quello. File: StorageController (PresignedUrlResponse+PublicUrl, ResolvePhysicalBucket, upload/download risolvono), IStorageClient/S3StorageClient (UploadAsync→Task<string> ritorna PublicUrl), SupabaseService (3 upload mandano chiave logica + usano URL server). Backward-compat: client vecchio cachato (manda "shopimages") risolto via base-match → upload OK ma URL client-built errato finché non aggiorna cache. IMMEDIATO PROD: serve comunque UNA cache-clear/unregister SW su app.puntify.it (client cachato gira codice vecchio); dopo, mai più. Va nel PROSSIMO commit/deploy. Build+restart collaudo, no commit.

## 2026-07-08 (15:05) — storage env-agnostic FATTO + committato (ae4c9b6)
Subagent a1a7150b: StorageController PresignedUrlResponse+PublicUrl + ResolvePhysicalBucket (chiave logica|fisico|base-match, suffissi -prod/-cat/-collaudo/-dev/-staging); IStorageClient.UploadAsync→Task<string>; S3StorageClient ritorna PublicUrl; SupabaseService 3 upload mandano chiave logica ("Shops"/"Accounts"/"Receipts") + usano URL server. Build 0 err, restart server+app, app-cat 200. Config collaudo -cat verificata (Shops→shopimages-cat; legacy shopimages→base-match). Resolver mappa solo bucket configurati (no injection), path/content-type invariati. Commit ae4c9b6 pushato. PROD: al deploy assicurare Storage:Buckets server = nomi -prod + PublicBaseUrl; poi 1 cache-clear su app.puntify.it. Nota: appsettings sono per-ambiente (gitignored), il commit NON contiene i nomi bucket.

## 2026-07-08 (15:32) — Nemi chat "Verifica la chiave LLM" (JSON menu grande)
Stefano ha incollato un JSON menu nella chat Nemi → "Si è verificato un errore. Verifica la chiave LLM nelle impostazioni." SPIEGATO: è il fallback GENERICO client (chiave aifab_error_message), mostrato su QUALSIASI errore della chiamata AI, non solo la chiave. Nemi usa chiave LLM per-negozio (shop_credentials → ShopAiClient, provider Anthropic/OpenAI/Gemini/MiniMax). ShopAiClient timeout=60s; su non-success LLM → HttpRequestException con body. CAUSA PROBABILE (JSON grande + "dopo un po"): TIMEOUT 60s o INPUT troppo grande (token/contesto), NON la chiave (sarebbe errore immediato). Consigliato: verificare chiave (Impostazioni→Credenziali, c'è test) ma soprattutto non incollare JSON enorme in chat (Nemi chat non è import menu). Sui log collaudo NON vedo la chiamata → probabile prod. Chiesto a Stefano collaudo/prod + riga log server ("Anthropic 4xx" / "LLM non configurato"). ATTENDO.

## 2026-07-08 (15:35) — proposta: Nemi agentica in loop (background + step-by-step)
Stefano vuole Nemi che agisca in loop su compiti articolati: suddividere, rispondere step-by-step, procedere. ANALISI: la base c'è già — NemiChatService.SendAsync classifica (LLM→{type,steps}); se complex crea NemiTask persistente (Plan/CurrentStep/TotalSteps) + posta piano + ExecuteRemainingSteps loop coi tool (_tools INemiToolRegistry) salvando ogni step in nemi_chat_history; NemiTaskResumeService (hosted, Program.cs:236) riprende task interrotti. LIMITE = ExecuteRemainingSteps gira SINCRONO dentro la richiesta HTTP → compiti grossi → timeout 60s (= errore JSON menu di oggi) + UI non mostra step in tempo reale (AiAssistantFab non fa polling durante). PROPOSTA (msg 5640): (1) ACK immediato col piano, richiesta chiusa subito; (2) esecuzione in BACKGROUND (task pending → worker esegue e posta ogni step); (3) chat polling tempo reale; (4) robustezza: timeout per-step, cap step, gestione errore per-step, tool IMPORT dedicato per input grandi (menu JSON) per non rimasticare token. Risolve anche il timeout. ATTENDO GO di Stefano per costruire (poi delego).

## 2026-07-08 (15:40) — cover >2MB FATTO + Nemi agentica IN CORSO
COVER FIX (io, commit b96d186): UploadShopImageAsync ora RequestImageFileAsync("image/jpeg", maxDim) prima dell'upload — cover 1920/logo 1024, ricompressione JPEG, fallback originale, cap 20MB. Fine errore "exceeds 2MB". Build ok, app riavviata, pushato. PROD: prossimo deploy + cache-clear; intanto Stefano riduce a mano.
NEMI AGENTICA IN CORSO (subagent a472021789bd5279c, Stefano "procedi"): (A server) SendAsync per task complessi crea NemiTask status=pending + posta piano + ritorna SUBITO (no ExecuteComplex sincrono → basta timeout); nuovo ProcessPendingTasksAsync (claim pending→running, un task/shop, ExecuteRemainingSteps posta step in nemi_chat_history, completed/failed+riepilogo); NemiTaskResumeService → worker CONTINUO (loop ~5s); robustezza: timeout/step, cap 10 step, errore per-step→failed, troncamento OriginalRequest>4000char (fix token menu JSON). (B app) endpoint GET /ai/nemi/active-task; AiAssistantFab+AssistantAi polling ~3s (history+active-task) con indicatore "Nemi sta lavorando passaggio X/N", stop a fine task. Task semplici invariati. Build+restart, no commit. Risolve anche l'errore chat di stamattina.

## 2026-07-08 (15:58) — Nemi agentica FATTA (subagent a472021789bd5279c), su collaudo, DA TESTARE
FATTO: SendAsync complessi → piano+NemiTask pending+ritorno immediato (no timeout); ProcessPendingTasksAsync (claim pending/planning→running, un task/shop, ExecuteRemainingSteps posta ogni step, completed/failed+riepilogo); NemiTaskResumeService=worker continuo loop 5s (verificato nei log: GET nemi_tasks status in pending/planning/running ogni 5s); troncamento OriginalRequest>4000; running orfani >2min ripresi retry_count++ fino MaxRetries. Endpoint GET /ai/nemi/active-task (401 ok). App: SocialStudioApiService.NemiActiveTaskAsync; AiAssistantFab+AssistantAi polling 3s (history+active-task)+indicatore "sta lavorando X/N"+IDisposable/CTS; 10 lingue aifab_working; css. Build 0 err server+app, restart, server 200/app-cat 200. Colonne nemi_tasks (updated_at/retry_count) GIÀ presenti → NESSUNA nuova migration. NON committato (aspetto test Stefano: è cuore chat). Risolve anche errore chat menu JSON di stamattina.
DA COMMITTARE quando Stefano ok: Nemi agentica (server+app). Deploy prod: nessuna nuova migration; reload PostgREST non necessario (nessuna colonna nuova).

## 2026-07-08 (16:50) — fix "Errore Sconosciuto" generazione foto AI menu (commit f930fd4)
Stefano (prod menu editor): generando immagine AI vede solo "Errore Sconosciuto" + vuole loading come tasto descrizione. CAUSA: MenuApiService.GenerateDishPhotoAsync/GenerateSectionPhotoAsync facevano return null sul 502 → scartavano il body con l'errore reale → UI "Errore sconosciuto" (res null). Il server (OpenAIImageGenerator) metteva già errori (openai_401/ex.Message "OpenAi:ImageApiKey not configured"/"richiede OpenAI provider"). FIX: client legge GeneratePhotoResponse a prescindere dallo status (fallback http_{code}), su eccezione ritorna ex.Message; OpenAIImageGenerator estrae {error.message} OpenAI invece di openai_401. Loading già presente (spinner+overlay _generating/_generatingSectionId). Build server+app 0 err, restart, app-cat 200 (restart dotnet-watch lento, 1° comando timeout ma commit poi fatto). Commit f930fd4 pushato (solo 2 file; Nemi agentica resta non committata). Causa prod probabile: chiave OpenAI immagini non configurata → ora l'errore lo dice.
NOTA: Nemi agentica ancora NON committata (aspetta test Stefano). Working tree ha: Nemi (AiAssistantFab/AssistantAi/NemiChatService/NemiTaskResumeService/ShopAiController/SocialStudioApiService/INemiChatService/resx/css).

## 2026-07-08 (17:00) — COMMIT+PUSH Nemi agentica (c0e44bf) + riepilogo commit oggi
Stefano "committa e pusha". Review mirata ok: active-task sotto [RequireShopOwner] (no leak cross-tenant); worker agisce su task.ShopId (owner-created), no cross-tenant; loop sequenziale cap step. Build 0 err x3. 18 file Nemi committati (c0e44bf), pushato. Niente segreti.
COMMIT OGGI su origin/master: 9ff2136 (batch enorme: Nemi voce/vapi, flotta, stampa totem, panoramica, admin monitoring, profili clienti, impersonazione + fix H1 webhook secret), 048e686 (back registrazione esercente), ae4c9b6 (storage env-agnostic), b96d186 (resize immagini), f930fd4 (foto AI errore reale), c0e44bf (Nemi agentica).
DEPLOY PROD blocco: 7 migration del batch 9ff2136 (nemi_credits, nemi_context_reset, vapi_calls, webhook_events, customer_profiles, devices_monitors, queue_totem_print) + reload PostgREST; Nemi agentica/storage/immagini/foto = NO migration. Config prod: Storage:Buckets -prod (app+server) + PublicBaseUrl; Vapi:WebhookSecret; chiave OpenAI immagini; cache-clear WASM app.puntify.it. Sequenza esatta da preparare su richiesta.

## 2026-07-08 (17:15) — richiesta: prompt AI per-menu (default + accodato a generazione) — DA CHIARIRE
Stefano: nella pagina menu, sotto "dati menu", una textarea "Prompt AI" per-menu con un prompt standard di default (tono/stile/lingua testi + stile immagini), accodato alla generazione testo (descrizione/ingredienti) e immagini (piatto/sezione). Messaggio TAGLIATO su "accodare anche il seguente prompt" (manca il prompt). Design confermato a Stefano + proposta bozza default. CHIESTO: (1) "il seguente prompt" = contenuto textarea o un prompt fisso aggiuntivo da incollare? (2) ok bozza default o testo suo? ATTENDO prima di costruire. FATTIBILITÀ verificata: ShopMenu NO campo ai_prompt (da aggiungere+migration); MenuEditor sezione dati-menu (Name+Description textarea, righe 67-80); immagini BuildPhotoPrompt (MenuController:753); testi ClaudeDishAiService/MiniMaxDishAiService (IDishAiService). Build: campo menu + include in create/update + append prompt menu in GeneratePhoto/text gen (server fetch menu.ai_prompt) + textarea UI con default.

## 2026-07-08 (17:20) — Prompt AI per-menu (A) + traduzione menu (B) IN CORSO (subagent a9f3adb56dac447fa)
Chiarimenti Stefano: (A) si accoda il prompt della textarea; default nella LINGUA ACCOUNT (non IT), immagini professionali+molto realistiche. (B) tradurre pagina menu pubblica in lingua cliente inclusi ingredienti.
SCOPERTA B: la pagina pubblica MerchantMenuPreview traduce GIÀ in tempo reale (endpoint /api/public/menu?lang=X → MenuController chiama EnsureMenuTranslatedAsync) nomi sezioni + nome/descrizione/INGREDIENTI piatti (TranslateField/TranslateIngredients) + allergeni. Quindi ingredienti GIÀ coperti. Gap possibile: nome/descrizione MENU-level + tag dietetici testo.
IN CORSO subagent a9f3adb: A = migration shop_menus.ai_prompt (+reload PostgREST) + ShopMenu.AiPrompt + MenuController create/update + MenuEditor textarea sotto dati-menu con default menueditor_ai_prompt_default per-lingua (10) che istruisce a scrivere in quella lingua + immagini pro/realistiche; append menu.AiPrompt in GeneratePhoto/GenerateSectionPhoto e nella generazione testo (server recupera menu.AiPrompt, non dal client). B = verifica+estendi solo menu-level name/desc se mostrati (ShopMenu.Translations) + tag dietetici. Build+restart, no commit.

## 2026-07-08 — Puntify MENU: Prompt AI per-menu (A) + verifica traduzione menu (B) — COMPLETO (no commit/deploy)
FEATURE A: colonna `ai_prompt` su shop_menus (migration docs/DB Migrations/2026-07-08_menu_ai_prompt.sql, APPLICATA collaudo + PostgREST restart + verificata \d e GET select ai_prompt no PGRST204). Model ShopMenu.AiPrompt. Server MenuController: Create/Update automatici (serializza tutto il modello), Duplicate copia AiPrompt, GeneratePhoto(dish)+GenerateSectionPhoto accodano menu.AiPrompt via AppendMenuPrompt. TESTI: ResolveMenuPromptAsync recupera il prompt SERVER-SIDE dal menuId (non spoofabile) e lo passa come extraInstructions a IDishAiService.Suggest/GenerateDescription (Claude+MiniMax impl aggiornate). MenuEditor: textarea Prompt AI + default per-lingua (menueditor_ai_prompt_default nella lingua account se vuoto). DishEditor riceve MenuId → AiDescribeButton(MenuId)+AiSuggest(menuId). 3 chiavi resx ×10 lingue.
FEATURE B: VERIFICA. Nome/descrizione MENU NON sono mostrati sulla pagina pubblica (MerchantMenuPreview usa shop.BusinessName/Tagline; _menu mai renderizzato) → nessuna azione. Tag dietetici/feature/allergeni già label localizzate (T("tag.*")/T("feat.*")/T("all.*")) → nessuna azione. Traduzione esistente sezioni/piatti invariata.
BUILD 0 errori (Server/Shared/App/Vetrina, artifacts isolati). Restart sequenziale ok: server :8001, app :8002 (200), vetrina :8003 (302). NON committato, NON deployato.

## 2026-07-08 (17:35) — Prompt AI per-menu (A) + traduzione (B) FATTO (subagent a9f3adb), DA COMMITTARE
A FATTO: migration 2026-07-08_menu_ai_prompt.sql (shop_menus.ai_prompt, applicata+PostgREST riavviato, verificata no PGRST204); ShopMenu.AiPrompt (MenuModels.cs); MenuController create/update auto (serializza modello)+Duplicate; immagini AppendMenuPrompt in GeneratePhoto/GenerateSectionPhoto; testi: menuId opzionale in AiSuggest/AiDescribe DTO → ResolveMenuPromptAsync → extraInstructions in IDishAiService.SuggestIngredients/GenerateDescription (ClaudeDishAiService+MiniMaxDishAiService); client MenuApiService(menuId)+AiDescribeButton+DishEditor(MenuId)+MenuEditor. MenuEditor textarea sotto dati-menu, default menueditor_ai_prompt_default (10 lingue, ciascuna istruisce a scrivere in sé + immagini pro/realistiche) precompilato se vuoto. Build 0 err x4, restart, app-cat 200/vetrina 302. Verificato colonna+resx 10/10.
B: solo verifica — nome/descrizione MENU NON mostrati pubblicamente (MerchantMenuPreview usa shop.BusinessName/Tagline) → nessuna azione; tag/allergeni già label localizzate; ingredienti già tradotti (TranslateIngredients). Traduzione esistente intatta.
DA COMMITTARE (aspetto ok Stefano). DEPLOY PROD: +1 migration menu_ai_prompt (+reload PostgREST). Restano non committati: Nemi già committata (c0e44bf); questo blocco menu AI prompt no.

## 2026-07-08 (20:20) — BUG traduzione menu pubblica (mix lingue) — mia leggerezza, fix in corso
Stefano (foto, menu Pepto in ucraino): mix IT/EN/UK. Avevo detto "B già coperto" fidandomi del CODICE senza aprire → sbagliato. Stefano: "controlla prima di dire che hai fatto". DIAGNOSI REALE: (1) UiStrings (dizionario embedded etichette in MerchantMenuPreview.razor ~1581, T() con fallback lang→en→it) INCOMPLETO: it/en=111 chiavi, es/fr/de/pt/zh/ja/ko/ar=99, bn/hi/ru/pl/nl/tr/vi/th/uk/ro=88 → 23 chiavi mancanti ripiegano su EN (All/Consigliati da/I piatti più amati). (2) badge "Best seller"/"Top" HARDCODED EN (riga ~111-112; feat.featured è T ok). (3) tagline/sottotitolo negozio (ShopHero, _shop.Tagline/SubDescription) testo libero non tradotto. (4) alcune sezioni (Pizze/Fritti) non tradotte.
FIX IN CORSO (subagent aaa12bb8658f5b727): completare UiStrings tutte le lingue a 111 chiavi (tradotte); badge→T("feat.bestseller"/"feat.top") in tutte lingue; tagline/sub: default localizzato se vuoto (a), traduzione testo-libero custom solo se esiste meccanismo shop-level altrimenti segnalare (b); sezioni: indagare se bug batch. VERIFICA RUNTIME OBBLIGATORIA (aprire menu in uk). Build+restart Vetrina, no commit.
LEZIONE (mistakes.md da aggiornare): verificare a RUNTIME prima di dire "fatto/coperto", non solo dal codice.

## 2026-07-08 (20:27) — architettura traduzione confermata da Stefano
Stefano: fisse→pre-tradotte tutte le lingue (tabellate); variabili (tagline/piatti/sezioni)→realtime + CACHE in tabella (no ri-richiesta). Chiede quale servizio (tipo Airbnb). RISPOSTA: l'architettura ESISTE GIÀ — MenuTranslationService traduce variabili on-demand via LLM (chiave AI) e CACHA in colonna Translations di piatti/sezioni (HasLang skip = no ri-chiamata). Nessun servizio esterno (Google/DeepL) necessario; agganciabile se vuole. ESTENDO lo stesso a tagline/sottotitolo negozio.
AGGIORNATO subagent aaa12bb8 (SendMessage): tagline/sub = realtime+cache → migration shops.translations JSONB (+reload PostgREST), Shop.Translations, EnsureMenuTranslatedAsync traduce anche shop Tagline/SubDescription (HasLang+MergeLang+UpdateAsync shops), ShopHero TranslateField (default localizzato se vuoto). +completare UiStrings tutte lingue + badge + sezioni + VERIFICA RUNTIME uk. DEPLOY PROD: +1 migration shop_translations. Registrato in mistakes.md: verificare runtime prima di dire fatto.

## 2026-07-08 (20:40) — fix traduzione menu FATTO+verificato uk MA blocco credito Anthropic
FATTO (subagent aaa12bb8, VERIFICATO RUNTIME uk): UiStrings tutte 20 lingue → 113 chiavi (identiche a IT, 0 mancanti); badge hardcoded→T("feat.bestseller"/"feat.top"); verificato render uk: home.all="Усе", featured_label="Рекомендує", badge="Хіт продажів", niente più mix EN. Tagline/sub: (a) default localizzato se vuoto + (b) realtime+cache: migration 2026-07-08_shop_translations.sql (shops.translations JSONB, applicata+PostgREST reload), Shop.Translations, EnsureMenuTranslatedAsync esteso (param Shop, traduce Tagline/SubDescription, HasShopLang cache UpdateRawAsync), ShopHero TranslateField. Sezioni: nessun bug (batch le include).
⛔ BLOCCO: MenuTranslationService usa chiave Anthropic GLOBALE (Anthropic:ApiKey, non merchant); log "credit balance too low" (400) → traduzione variabile (tagline/sezioni/menu) NON parte. Codice corretto+cache-ready, sblocca appena c'è credito.
DECISIONE a Stefano (msg 5660): A) ricaricare credito Anthropic; B) [consigliata] translator usa chiave LLM MERCHANT (ShopAiClient, come Nemi/immagini) → ogni esercente paga, no chiave globale esaurita, rispetta regola "Anthropic solo Concilium" (memory feedback_anthropic_api_only_for_concilium). Chiesto anche se committare. ATTENDO.
DEPLOY PROD (blocco menu): +migration shop_translations. Uncommitted ora: menu AI prompt (a9f3adb) + traduzione menu (aaa12bb8).

## 2026-07-09 (10:33) — cron mattutini scaduti (sessione precedente chiusa) → RICREATI
Stefano non ha ricevuto i messaggi mattutini. CAUSA: erano cron di SESSIONE; sessione precedente terminata → scaduti (CronList=nessuno). RICREATI in questa sessione (session-only, auto-expire 7gg): f4b2e9dc daily 8:03 controllo errori; a9301850 daily 9:03 stato traduzioni/Vetrina; d44e817c lun 9:07 test plan settimanale. Tutti inviano via mcp reply chat_id 505161324 + aggiornano vault+sync.
LIMITE: session-scoped (muoiono a riavvio Claude) + 7gg. Offerto a Stefano un CRON DI SISTEMA sul server (script check + Telegram bot API, indipendente da Claude) per persistenza reale. ATTENDO se vuole il cron di sistema.

## 2026-07-09 (10:52) — CRON DI SISTEMA report mattutino (persistente) FATTO
Stefano "sì grazie" → cron di sistema. FATTO: script /home/claudebot/scripts/puntify-morning-report.sh (legge BotToken a runtime da appsettings, invia a chat 505161324 via api.telegram.org sendMessage @Puntifynemibot; contenuto: stato servizi puntify-server/app/vetrina/piracity/piracity-web/clawroom, errori 24h [ERR]/Exception/EAGAIN, disco, mem, load; ora IT via TZ=Europe/Rome). Testato: send ok:true (msg 86). Crontab claudebot: "4 6 * * *" = 8:04 IT (box è UTC!). Demone cron active. Rimosso cron sessione 8:00 f4b2e9dc (doppione). Restano session-cron a9301850 (9:03 traduzioni) + d44e817c (lun 9:07 test) come bonus Claude-driven. Offerto a Stefano di rendere di sistema anche 9:00/test se vuole. NB box UTC: per orari IT sottrai 2h (CEST).

## 2026-07-09 (20:10) — richiesta: libreria foto condivisa piatti (popup griglia + match ingredienti) — DA CONFERMARE
Stefano: quando si definisce un menu, popolare foto sezioni/piatti in base al nome; se piatto simile (nome o ingredienti ~80%) usa foto già esistente; popup con MATRICE di foto attinenti (da foto di altri utenti) + upload file/AI; autocomplete nome per non sbagliare digitazione. Obiettivo: non usare AI per creare ciò che esiste già.
SCOPERTA: gran parte ESISTE. product_catalog (Punto.Shared/Models/Menu/ProductCatalogItem: name/name_lower/description/allergens/INGREDIENTS/PHOTO_URL/category/USAGE_COUNT), cross-locale. DishEditor: autocomplete SearchCatalogAsync (dropdown "usato da N locali"); ApplyCatalogItem imposta GIÀ PhotoUrl+Description+Ingredients+Allergens dal catalogo se vuoti. Catalogo si popola su create piatto (MenuController ~465 upsert by name_lower) + AddDishTool (Nemi). SearchCatalog = name_lower ilike *q* order usage_count (solo NOME, substring; niente fuzzy/ingredienti).
MANCA: (1) POPUP "scegli foto" a griglia (foto catalogo attinenti) + schede file/AI, cliccando sul caricamento immagine; (2) matching fuzzy (refusi, pg_trgm) + ingredienti ~80%. Privacy: modello condiviso GIÀ esistente (Stefano l'ha voluto).
PROPOSTO a Stefano (msg 5668): piano popup+matching; chiesto ok pg_trgm + conferma riuso foto altri. ATTENDO GO.

## 2026-07-09 (20:25) — libreria foto piatti: popup griglia + matching fuzzy/ingredienti (subagent a39707f37104d3a02) IN CORSO
Stefano GO (1.si pg_trgm 2.si riuso foto altri). IN CORSO: (1) migration 2026-07-09_catalog_photo_match.sql: pg_trgm + indice trgm name_lower + RPC puntify.match_catalog_photos(p_name,p_ingredients,p_limit) → catalog items CON foto ordinati per similarity(name) + ingredient overlap ~80% (Jaccard/overlap); REVOKE anon/authenticated GRANT service_role; +reload PostgREST. (2) server endpoint GET /api/menu/catalog/photo-suggestions?name=&ingredients= via RPC + MenuApiService.CatalogPhotoSuggestionsAsync. (3) app: popup "Scegli foto" in DishEditor 3 schede (Suggerite griglia dal catalogo/Carica file/Genera AI), click imposta PhotoUrl; sezioni analogo per nome; i18n 10 lingue. Build+restart, VERIFICA RUNTIME RPC obbligatoria, no commit.
NB: già esisteva product_catalog cross-locale (nome/ingredienti/photo_url/usage_count) + autocomplete SearchCatalog + ApplyCatalogItem auto-populate. Si aggiunge sopra.

## 2026-07-09 (20:32) — richiesta: 300 post tab PUNTI Google Sheet (Automatic Posts) — BLOCCATO su accesso
Stefano: svuotare e ripopolare tab PUNTI del Google Sheet "Automatic Posts" (doc_id 1K1uED8exsd7Jts-7wJ2WDHnCth-v1EIMHCNglym14ZY, owner imodejam@gmail.com) con 300 righe di post per pubblicizzare a ciclo i servizi Puntify + caratteristiche; tono startup innovativo/amichevole; SOLO funzioni reali (non inventare); seguire struttura righe attuali.
BLOCCO: gog supporta Sheets ma (1) foglio NON condiviso con account bot alfredopenbotti@gmail.com (unico autenticato in gog, ha scope sheets) → 404; (2) API Google Sheets DISABILITATA sul progetto OAuth 419651587509 → 403; imodejam non autenticato in gog. Opzioni date a Stefano (msg 5672): A) condividere foglio con alfredopenbotti Editor + abilitare Sheets API (link console) → faccio io; B) mandarmi struttura PUNTI (header+esempi) → preparo CSV/incollo. ATTENDO scelta. Contenuto: attenersi a servizi reali (fidelity punti/prenotazioni/menu/ordini/elimina code+totem/Nemi voce+chat/social studio/app cliente).
